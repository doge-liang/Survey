A Universal Load Balancing Principle and Its
Application to Large Language Model Serving
Zixi Chen3, Tianci Bu2†, Chendong Song1†, Xin Lu2*,
Yinyu Ye1,4*, Zijie Zhou1*
1*Department of Industrial Engineering and Decision Analytics, HKUST,
Clear Water Bay, Hongkong, China.
2Department of System Engineering, National University of Defense
Technology, 109 Deya Road, Changsha, 410073, Hunan, China.
3School of Mathematical Sciences, Peking University, Yiheyuan Road,
100871, Beijing, China.
4Department of Management Science and Engineering, Stanford
University, 450 Jane Stanford Way, Stanford, 94305, California, United
States.
*Corresponding author(s). E-mail(s): xin.lu.lab@outlook.com;
yyye@ust.hk; jerryzhou@ust.hk;
Contributing authors: chenzixi22@stu.pku.edu.cn; btc010001@gmail.com;
songcd@ust.hk;
†These authors contributed equally to this work.
Abstract
Over 40% of computational power in Large Language Model (LLM) serving sys-
tems can be systematically wasted—not from hardware limits, but from load
imbalance in barrier-synchronized parallel processing. When progress is gated by
the slowest worker at each step, heterogeneous and evolving workloads create
persistent stragglers; faster workers idle while drawing power, producing noth-
ing. In large language model inference alone, this translates to gigawatt-hours
of wasted electricity daily. Here we develop a universal load-balancing principle
for barrier-synchronized systems with non-migratable state. We prove worst-case
theoretical guarantees: imbalance reduction grows with system scale, and the
resulting energy savings can exceed 52% for modern hardware at fleet scale.
Experiments corroborate the theory, demonstrating 28% energy reduction along-
side substantial throughput and latency improvements. Formulated as an online
integer optimization with provable guarantees, the principle extends beyond LLM
1
arXiv:2601.17855v2  [cs.DC]  1 Feb 2026


serving to broad classes of barrier-synchronized parallel systems, establishing a
theoretical foundation for sustainable high-performance computing.
1 Introduction
Load balancing—the problem of distributing work across parallel resources to mini-
mize latency, energy, or cost—is a central challenge across the natural and engineering
sciences and in large-scale service systems. In large-scale molecular dynamics and
biomolecular simulations, dynamic load-balancing schemes are essential to maintain
scalability as particles move and local density fluctuates [1, 2]. In climate and weather
models, specialized load-balancing strategies are required to handle localized, high-
intensity physics kernels and heterogeneous meshes, improving time-to-solution for
global simulations and regional storm-surge predictions [3, 4]. Similar issues arise in
cloud computing platforms [5, 6], manufacturing systems [7, 8], and emergency medi-
cal management [9, 10], where workloads and resource availability fluctuate over time.
In this work, we study a new instance of this broad class: load balancing in large-scale
LLM (Large Language Model) serving. This setting adds stringent constraints—jobs
whose processing times are unknown and evolve as computation proceeds, per-request
state that cannot be migrated between workers without prohibitive cost, and step-wise
synchronization barriers across workers—that make it substantially more challenging
than many classical load-balancing problems. Our goal is to develop a universal and
scientifically sound load balancing principle that not only resolves this emerging
bottleneck in LLM serving, but also applies, in appropriately simplified form, to more
traditional load-balancing tasks.
Modern generative LLMs [11–15] have transformed artificial intelligence by achiev-
ing unprecedented performance in natural-language generation, reasoning, and task
generalization across diverse domains. LLM serving refers to the process of host-
ing these models in production environments to process user prompts and generate
responses in real time. Meeting this demand requires vast networks of AI datacenters
that deliver timely and reliable inference at massive scale [16, 17]. Even consider-
ing OpenAI alone, its LLM serving services handle at least 2.5 billion user prompts
per day [18], corresponding to roughly on the order of gigawatt-hour of electricity
consumption daily [19]. As LLM usage continues to accelerate, the energy and cool-
ing resources required for such inference are approaching practical and environmental
limits—often referred to as the emerging energy wall [20]. Despite remarkable advances
in model architecture and hardware acceleration, the overall eﬀiciency and energy use
of LLM serving remains constrained by how computational workloads are balanced
across parallel workers.
In LLM serving, inference unfolds in two stages: a prefill stage that encodes the
input prompt and initializes internal state, followed by a decode stage that generates
the response one token at a time. In modern deployments, decoding is executed in
parallel across many computational workers, and each token step typically includes a
2


coordinated stage across workers (e.g., collective communication in model-parallel exe-
cution), which induces barrier synchronization: the step completes when the slowest
worker finishes. As active requests evolve and complete at different times, per-worker
workloads drift, creating persistent stragglers and forcing faster workers to idle.
Figure 1 quantifies the resulting ineﬀiciency in a real industrial trace: both the mean
and median barrier-induced idle time exceed 40% per decode step, wasting more
than two-fifths of aggregate compute on average. This ineﬀiciency scales directly into
energy cost at fleet scale. Because GPUs draw substantial power even while waiting
at synchronization barriers—idle time consumes energy [21] without producing useful
computation—the 40% barrier-induced idle in Figure 1 represents not merely wasted
time but wasted watts. Figure 2 demonstrates this concretely: reducing decode-stage
imbalance via the approach introduced in this paper yields a 28.2% reduction in
total energy consumption relative to the default serving policy over this trace, with
the improvement growing as system scale increases. At the scale of modern LLM
deployments—where inference alone can consume gigawatt-hours of electricity daily—
such reductions translate to megawatt-hour savings with proportionate reductions in
cooling load and carbon footprint. We next unpack the decode-side mechanics that
create this imbalance and clarify why routing decisions are both critical and diﬀicult.
Fig. 1: Workload imbalance and idle time in 436 decoding steps over 3 minutes from
a real industrial trace. (Left):
Dynamic total workload across 32 GPUs. At each
step, the completion time for all GPUs is dictated by the heaviest workload unit,
demonstrating significant and persistent workload imbalance. (Right): Per-step idle
time resulting from this imbalance. The yellow bars represent the average percentage
of time that GPUs spend waiting during each decode step. With a mean (and median)
idle time of 40% (and 41%), more than two-fifths of the aggregate computational
resources are wasted on average per step.
To make the bottleneck precise, we outline the decode-side mechanics relevant
to load balance. Prefill–decode (PD) disaggregation is the most common structure in
LLM serving, where prompt encoding (prefill) and token-by-token generation (decode)
run on distinct workers. When a request finishes prefill, the router assigns it to a
decode worker; from that handoff onward the request remains on that decode worker.
3


Fig. 2: Comparison of energy and power consumption over the default policy and the
approach introduced in this paper (BF-IO). (Left): Instantaneous GPU power con-
sumption over time under the default policy (orange curve) and BF-IO (blue curve).
Total energy, computed as the time integral of instantaneous power, is 29.1 MJ for the
default and 20.9 MJ for BF-IO —a 28.2% reduction. BF-IO draws slightly higher instan-
taneous power (reflecting improved GPU utilization from reduced barrier idling), but
completes the same workload in less time; the resulting shorter integration window
yields lower total energy consumption. (Right): Energy consumption over increasing
system scale (number of GPUs). The energy reduction percentage keeps increasing
when the system becomes larger.
During decoding, each newly generated token appends one unit of key–value (KV)
vectors to the per-request KV cache, so the cache grows linearly with response length.
In practice, the decode stage is organized via data parallelism (DP): each worker
maintains a batch of active requests and their KV caches; migrating a request would
require transferring its entire cache and is therefore impractical—assignments are
effectively sticky, making the assignment decision crucial. At each decode step t,
worker g performs the local attention compute for its request batch; the runtime
T (g)
local(t) is approximately linear with the aggregate resident KV it must read and carry
over. Once the local computation finishes, the system executes a model-parallel stage—
such as expert parallelism (EP) or tensor parallelism (TP)—in which the forward
pass is split across workers because the model (or its expert set) is partitioned across
devices. This stage requires collective communication and is therefore synchronous
across workers, taking time Tsync(t). The per-step wall-clock time is thus
Tstep(t) = max
g
T (g)
local(t) + Tsync(t).
As response lengths diverge across requests, these local times diverge, so the max-
imization term governs progress and induces persistent idle time for lightly loaded
workers. Figure 3 illustrates the LLM serving system architecture and the load
balancing barrier.
Balancing decode-time work is intrinsically diﬀicult for three coupled reasons. (i)
Unknown and evolving workloads. The response length of a request is unknown
at assignment. Although several studies predict decode length from prefill features
[22–26], the inherent randomness of generation and the limited information in these
4


Fig. 3: Prefill–decode serving and the decode-stage load-balancing barrier. Left
(Decode Step t):
Prefill workers build each request’s KV cache and hand the
request to the scheduler, which assigns it to a decode worker; the assignment is
sticky because migrating the KV cache is impractical. Each decode worker processes
a batch of requests during the local compute stage (Multi-head Latent Attention).
Gray squares indicate the resident KV cache per request. After local compute, all
workers must begin the EP stage synchronously. The step time is therefore governed
by the slowest local stage: here, Worker 2 must wait for Worker 1, exposing the load-
balancing bottleneck. Right (Decode Step t+1): Every active request produces one
token, appending to its KV cache (red square). Completed requests leave the batch
(e.g., Request 4), and newly prefetched requests may be admitted by the scheduler
to a worker (e.g., Request 7 with green square). Because request lengths differ and
assignments are sticky, per-worker loads drift from step to step, creating persistent
imbalance that limits throughput and energy eﬀiciency. Legend: gray = existing KV
cache; red = KV added by the current step; green = newly admitted request after
prefill.
features result in low prediction accuracy. (ii) Online, non-stationary arrivals.
Requests finish prefill and arrive to the router sequentially, so decisions must be made
without knowledge of the future mix of lengths. Moreover, the frequency of arrival
varies over time and is hard to model, so even an arrival distribution for decode is
unavailable. (iii) Sticky assignments with drifting load. Once placed, a request
cannot be migrated without moving its entire KV cache. Each request’s decode is
processed on a single decode worker for its entire lifetime. Meanwhile, per-worker
5


load changes at every step (Figure 3): continuing requests add one token’s workload,
completed requests remove their entire workload, and newly admitted requests add
their KV cache from the prefill step. Early placement errors therefore persist and
compound.
Together, these constraints define an online routing problem with unknown, time-
varying job sizes under hard synchronization barriers, in which classical policies
such as round-robin or join-shortest-queue can be systematically misaligned with
the true objective and perform poorly. Because the barrier-induced bottleneck is
architectural and the workload mix is inherently non-stationary, improvements that
depend on a particular trace or dataset are insuﬀicient; what is needed is a univer-
sal load-balancing principle that is robust across deployments and adversarial arrival
patterns. Appendix A.1 reviews related work, including the limitations of classical
load-balancing policies in this setting.
2 Results
We summarize the main findings of this work in two parts. First, we introduce a
universal load-balancing principle for sticky assignment systems with barrier syn-
chronization and show that it admits a clean optimization formulation and rigorous
worst-case guarantees. Second, we instantiate the principle in the decode-stage DP
bottleneck of LLM serving, design a sustainable routing policy, and demonstrate
substantial improvements on both public and proprietary workloads.
2.1 A universal load-balancing principle: Balance a Short
Future
Scientific structure: a step-wise integer-optimization formulation.
At each assignment step, the Balance Future with Integer Optimization principle (BF-
IO) chooses assignments that minimize a short-horizon prediction of future imbalance
(Section 4). We write this decision as a finite-horizon integer optimization over binary
assignment variables, subject to per-worker capacity constraints and full utilization of
available slots, in the spirit of classical linear- [27, 28] and integer-optimization [29]
formulations used in scientific and engineering applications. Concretely, the step-k
decision is characterized by the integer program (IO), whose objective is the accu-
mulated predicted imbalance over a short window and whose feasible set encodes
the sticky assignment rule and slot constraints. This formulation makes explicit that
decode-time load balancing is not merely a heuristic dispatch problem: it is an online
sequence of structured combinatorial optimization problems with a barrier-driven
objective.
A counterintuitive information suggested: short lookahead, not full-job
prediction.
A central insight is that effective load balancing in barrier-synchronized systems does
not require accurate prediction of the total remaining workload of newly arriving
jobs—a task that is notoriously unreliable in LLM inference and many other domains.
6


Instead, our BF-IO principle relies on a weaker and more robust signal: a short looka-
head description of the near-future evolution of the currently active jobs, not the newly
arriving jobs. Intuitively, the barrier cost at time k is dominated by which workers
are likely to become stragglers in the next few steps; decisions can be revisited at sub-
sequent steps as new arrivals are observed, so errors about the distant future can be
corrected by future routing choices. This is precisely the regime where short-horizon
prediction is feasible: while forecasting the full completion time of a long job may be
diﬀicult, predicting whether an ongoing job will finish within a small window is often
considerably easier (and can be performed using lightweight signals, or even manual
rules in some settings).
2.2 Provable theoretical guarantees under worst-case settings
Guaranteed load balancing improvement in the LLM serving model.
We build a mathematical model in Section 3 for the LLM DP decode load imbalance
problem, where each active request contributes an increasing KV workload until ter-
mination. Then, we provide worst-case theoretical guarantees, where arrivals may be
chosen adversarially in Section 5. We rigorously prove that even under this worst-case
arrival model, BF-IO provably reduces long-run imbalance relative to the real base-
line algorithm by a factor Ω
 √B log G

that grows with system scale (Theorem 1,
Theorem 2), where B is the per-worker batch size and G is the number of workers.
This scaling is significant: it shows that the benefit of principled balancing increases
with cluster size and batching, precisely the regime in which industrial huge serving
systems operate.
From imbalance reduction to energy savings.
Beyond eﬀiciency metrics, we establish a rigorous theoretical connection between
load balancing and energy consumption. Theorem 4 proves that any guaranteed
improvement in imbalance translates into an explicit guaranteed improvement in total
synchronized-phase energy, with constants depending on GPU power model parame-
ters and request distribution. Instantiating this result with our imbalance guarantees
for BF-IO yields Corollary 1: as system scale G →∞, the energy saving fraction
approaches a hardware-dependent constant that exceeds 52% for modern GPUs. This
provides, to our knowledge, the first worst-case theoretical guarantee linking schedul-
ing policy to energy eﬀiciency in LLM serving, establishing a principled foundation
for sustainable inference at scale.
Universality beyond LLMs: a broad class of drifting workloads.
The LLM analysis exploits the fact that per-request workload drifts by one unit dur-
ing processing, but the same phenomenon occurs far more broadly. We therefore
extend the theory to a general non-decreasing drift model in which all active jobs
share a bounded, time-varying workload increment sequence (δk)k≥1, encompassing
the classical constant-workload case (δk ≡0), standard LLM decoding with unit-step
KV growth (δk ≡1), speculative decoding where multiple tokens may be accepted
7


per step (δk ≥1) and and memory-eﬀicient architectures employing cache compres-
sion or sparse attention (Section 5.1). Theorem 3 shows that the same scale-sensitive
improvement persists in this general setting: BF-IO achieves a worst-case imbalance-
reduction factor of order Ω(√B log G) relative to the baseline under the worst-case
arrival sequences.
Beyond its immediate implication for system design, the mathematical proof intro-
duces a technically involved analytical route for studying online load balancing under
stickiness and barrier coupling; we view this as a novel and reusable perspective for
future theoretical work on load balancing in modern, stateful parallel systems.
2.3 Resolving a decode-stage load balancing bottleneck in
LLM serving
Decode-stage load imbalance as a dominant eﬀiciency bottleneck
We establish that decode-stage load imbalance constitutes a dominant eﬀiciency bot-
tleneck in production-scale LLM serving with large-scale EP/TP synchronization. In
such deployments, each token step is barrier-gated by the slowest data-parallel worker;
heterogeneity in resident KV footprints therefore translates directly into idle time on
faster workers while they await synchronization. Our analysis of real industrial traces
quantifies this ineﬀiciency: Figure 1 shows that the average fraction of compute time
lost per decode step exceeds 40%, representing more than two-fifths of aggregate GPU
resources wasted at barriers, which results in a 28.2% energy waste. Crucially, the
magnitude of this waste varies sharply with arrival patterns and request characteris-
tics, indicating that improvements tied to any single trace or dataset are insuﬀicient—a
finding that motivates our focus on a universal principle with worst-case guarantees.
Empirical results on open and proprietary workloads.
We evaluate BF-IO through extensive numerical simulations to assess whether the
theoretical improvements translate into practical eﬀiciency gains. We evaluate BF-
IO on (i) public datasets and (ii) proprietary industrial workloads under matched
serving configurations (Section 6). Across these settings, BF-IO consistently reduces
imbalance and improves system eﬀiciency relative to the default baselines used in real
industries. Concretely, on the public workload, BF-IO improves throughput by 90%,
reduces latency by 44%, and reduces energy consumption by 28%. The proprietary
workload has been verified by a major LLM developer industry in China.
3 Model and Baseline Policy
We consider a general online assignment problem with G computational workers. Each
worker g ∈[G] has a maximum concurrency of B ∈N, meaning that at most B
requests can be processed simultaneously on g. The discrete time horizon is indexed by
k ∈{0, 1, . . . , K}, where K can be taken large (and later set to infinity in asymptotic
analyses).
8


Arrival instance and workload profiles.
Let I denote the set of all requests in a given arrival instance. Each request i ∈I
arrives at some step ki ∈{0, . . . , K} and may be assigned to a worker at any step
xi ≥ki. Once assigned, the request is processed on a single worker until completion;
we do not allow migration between workers (assignments are sticky) and we do not
preempt processing.
The processing requirement of request i is represented by a workload profile
Wi =
 w(1)
i
, w(2)
i
, . . . , w(oi)
i

,
where oi ∈N is the total number of processing steps for request i, and w(j)
i
≥0
denotes the workload contributed by i in its j-th processing step. Thus, once i starts
processing at time xi, it occupies exactly oi consecutive steps xi, xi +1, . . . , xi +oi −1,
and its workload at time k (while active) is w(k−xi+1)
i
.
Two simple examples illustrate this notation. If a request i has constant workload
5 in each of 3 steps, then Wi = (5, 5, 5). In LLM inference, if a request i has a KV-
cache load that grows linearly with the number of generated tokens and starts with
prefill size 3, then a run of 4 decoding steps corresponds to Wi = (3, 4, 5, 6). In general,
the full profile Wi is unknown to the scheduler at arrival time; it is treated as fixed
but unobserved.
Policies and per-step workloads.
A non-anticipative policy π specifies, for each request i and each step k ≥ki, whether
to assign i at time k, and if so to which worker g ∈[G], subject to the capacity
constraint that at most B requests are actively processed on each worker at every
step. Let g(i) denote the worker to which request i is assigned under policy π, and xi
its assignment time.
For each worker g and step k, define the active set
Ag(k) =

i ∈I : g(i) = g, xi ≤k < xi + oi
	
,
i.e., the set of requests that are currently being processed on worker g at time k. The
instantaneous workload of worker g at time k is then
Lg(k) =
X
i∈Ag(k)
w(k−xi+1)
i
.
(1)
We denote by
Lg∗(k) = max
g∈[G] Lg(k)
the maximum workload across all workers at time k.
Imbalance and objective.
In many parallel systems, including data-parallel LLM decoding, the processing time
of a step on a worker is linear in its workload, and all workers must wait for the slowest
9


worker at each step (a barrier). Thus, the idle work at step k—the total amount of
work that other workers could have done if they were as heavily loaded as the busiest
worker—is proportional to
Imbalance(k; π) =
G
X
g=1
 Lg∗(k) −Lg(k)

= G · Lg∗(k) −
G
X
g=1
Lg(k),
(2)
where we make the dependence on the policy π explicit. Over a finite horizon
{1, . . . , K}, we define the average imbalance of a policy π as
AvgImbalance(π) =
1
K
K
X
k=1
Imbalance(k; π).
Our goal is to design an assignment policy π that makes AvgImbalance(π) as small
as possible. Intuitively, this corresponds to maximizing the amount of “useful” work
performed each step and minimizing the total idle time induced by waiting for the
slowest worker at the barrier.
4 Method: A Universal Load Balance Principle
In the fully agnostic model of Section 3, where the scheduler has no information about
Wi beyond arrivals and completions, no policy can differentiate between two requests
with the same arrival time if their future workloads are indistinguishable. In practice,
however, many systems expose partial information about the near future. Our goal in
this section is to formalize a minimal and broadly feasible form of such information,
and then to develop a load-balancing principle that uses it in a disciplined way.
Short lookahead workload information.
We introduce a short lookahead window of length H ∈N and assume that, at each step
k, the scheduler may observe for every active request i an estimate of its contributions
to future workloads over the next H steps after the current one. Formally, let
c
W H
i (k) =
 bw(1)
i
(k), bw(2)
i
(k), . . . , bw(H)
i
(k)

denote any prediction of the workloads that request i will contribute in its next H
processing steps, i.e., at times k+1, . . . , k+H, conditional on the information available
at time k. When i finishes before H further steps, we interpret the remaining entries as
zeros. The special case H = 0 corresponds to having no lookahead beyond the current
step: the scheduler only knows the current workloads {Lg(k)} and has no predictive
signal about k + 1, k + 2, . . .
This interface captures situations where the total workload of a job is hard to
predict at arrival, but its short-term behavior is easier to infer once the job has
progressed. In LLM serving, for example, the total number of decode steps for a
request is diﬀicult to predict accurately from the prompt alone, yet once a response
10


is near completion (e.g., exhibits “in conclusion”, closing punctuation, or termination
tokens), it becomes much easier to estimate whether the request will complete within
the next H steps. In the LLM serving setting, the per-step workload is driven by the
KV cache, which grows by one token per decode step until termination, so c
W H
i (k)
effectively reduces to a prediction of whether and when a request will finish within the
lookahead window. As another example, consider a multi-stage document-processing
pipeline where early stages have highly variable runtimes, but once a document reaches
the final stage, sensors or partial results reveal that only a short, nearly deterministic
tail of work remains; here, c
W H
i (k) summarizes this remaining tail, even if the total
processing time from arrival was unpredictable.
The universal Balance Future with Integer Optimization principle (BF-IO).
Given access to short lookahead workload information, we now state our universal
load-balancing principle. At a high level, the scheduler should choose assignments that
make the future workloads across workers as balanced as possible, rather than greedily
equalizing only the current step. To make this precise, fix a window length H ≥0
and consider step k. Let Rwait(k) be the set of waiting requests, and let cap[g](k) be
the number of free slots on worker g at that time. An allocation at step k consists of
disjoint subsets {Sg(k)}g∈[G] of Rwait(k), with
Sg(k) ⊆Rwait(k),
Sg(k) ∩Sg′(k) = ∅for g ̸= g′,
|Sg(k)| ≤cap[g](k) for all g.
We write
S(k) =
[
g∈[G]
Sg(k)
for the set of all requests admitted at step k. For any such allocation S(k), the induced
per-worker sets {Sg(k)} determine an updated active set {Ag(k)} and hence a pre-
dicted load trajectory over the next H steps, using the profile estimates {c
W H
i (k)} for
all active and newly admitted jobs.
Let Lg(k) denote the current workload on worker g at time k and Lg(k + h) the
predicted workload at time k+h under allocation S(k) for h ≥1, and let Imbalance(k+
h) be the corresponding imbalance as in Equation (2). The Balance–Future principle
is then to choose, at each step k, an allocation S(k) that minimizes the accumulated
predicted imbalance over the window:
J
 S(k)

=
H
X
h=0
Imbalance(k + h),
where h = 0 uses the current workloads and h ≥1 uses the predicted workloads based
on {c
W H
i (k)}.
This minimization can be achieved via an integer optimization. Introduce binary
decision variables xig ∈{0, 1} indicating whether request i ∈Rwait(k) is assigned to
11


worker g at step k, and define
Sg(k; x) :=

i ∈Rwait(k) : xig = 1
	
,
S(k; x) :=
[
g∈[G]
Sg(k; x),
so that S(k; x) is the admitted set induced by the assignment matrix x = {xig}. We
write Jk(x) := J
 S(k; x)

for the accumulated predicted imbalance cost at step k. Let
U(k) := min
n
|Rwait(k)|,
G
X
g=1
cap[g](k)
o
denote the number of slots that can be filled at step k (either all waiting requests, or
all free capacity, whichever is smaller). Then the optimal minimization choice at step
k is the solution of the following integer optimization:
min
{xig}
Jk(x) = J
 S(k; x)

s.t.
G
X
g=1
xig ≤1,
∀i ∈Rwait(k)
(each request assigned to at most one worker),
X
i∈Rwait(k)
xig ≤cap[g](k),
∀g ∈[G]
(per-worker capacity constraints),
G
X
g=1
X
i∈Rwait(k)
xig = U(k),
(full utilization of available slots or waiting requests),
xig ∈{0, 1},
∀i ∈Rwait(k), ∀g ∈[G].
(IO)
An optimal solution x⋆to (IO) yields the selected allocation {S⋆
g(k)}g∈[G] with
S⋆
g(k) := Sg
 k; x⋆
=

i ∈Rwait(k) : x⋆
ig = 1
	
.
In words, at each step BF-IO solves an integer optimization that simultaneously assigns
requests to workers and minimizes a finite-horizon prediction of future imbalance.
We capture this procedure in the following step-wise pseudocode. Conceptually,
the “inner loop” that enumerates feasible allocations and chooses the one minimizing
J corresponds exactly to solving the integer optimization (IO).
Although BF-IO (Algorithm 1) is not globally optimal over the entire time horizon
{0, . . . , K}, it explicitly balances across the near future rather than acting myopi-
cally. When H = 0, the window collapses to the current step and J
 S(k)

reduces
to Imbalance(k); the algorithm selects the assignment that best equalizes the next
workloads {Lg(k)}, using no lookahead beyond the current step. Larger H allows the
scheduler to anticipate near-term completions and avoid assignments that look good
now but create pronounced imbalance a few steps later.
12


Algorithm 1: Balance–Future Algorithm (BF-IO)
Input: Waiting set Rwait(k), active sets {Ag(k)}, free slots {cap[g](k)},
lookahead window length H
Output: Assignments {Sg(k)}g∈[G] at step k
Compute the number of admissible assignments:
U(k) ←min
n
|Rwait(k)|,
X
g
cap[g](k)
o
;
Enumerate all feasible allocations {Sg(k)} with Sg(k) ⊆Rwait(k),
|Sg(k)| ≤cap[g](k) for all g, Sg(k) ∩Sg′(k) = ∅for g ̸= g′, and
S
g Sg(k)
 = U(k);
for each feasible allocation {Sg(k)} do
Form S(k) ←S
g∈[G] Sg(k);
Simulate the predicted load trajectory {Lg(k + h)}H
h=0 for this allocation
using {c
W H
i (k)};
Compute the imbalance cost
J
 S(k)

=
H
X
h=0
Imbalance(k + h) ;
Select an allocation {S⋆
g(k)} that minimizes J
 S(k)

(equivalently, solves the
integer optimization (IO));
Assign requests according to {S⋆
g(k)};
Intuition and choice of window length H.
The Balance–Future principle can be understood as a compromise between two
extremes. When H is very small (e.g., H = 0), the scheduler only equalizes the current-
step workloads; this is cheap and prediction-free but can be short-sighted, because it
ignores imminent completions that would quickly relieve heavy workers or jobs that
are about to enter the system. At the other extreme, taking H very large is also unde-
sirable: the algorithm optimizes the effect of current assignments over a long window
without knowing future arrivals or the future assignment decisions that will be made
within that window, so its predictions become increasingly speculative and dominated
by unmodeled randomness. In practice, a moderate window H—large enough to see a
few steps of meaningful tail behavior, but small enough that predictions remain infor-
mative and computational costs stay low—strikes the right balance (See Figure 4).
Our theory in Section 5 shows that even the purely myopic case H = 0 already yields
strong worst-case gains over FCFS-type policies, and our experiments in Section 6
confirm that modest H > 0 can further improve performance when realistic predictive
signals are available.
13


0
20
40
60
80
100
Window Length (H)
2
4
6
8
10
Avg Imbalance (×108)
±1 std
Mean imbalance
Fig. 4: Intuition for selecting the window length H, evaluated across values from 0 to
100. This analysis uses an implementation of the Algorithm BF-IO in the LLM serving
context. Further implementation and experimental details are provided in Sections 6.
5 Theoretical Guarantee of the Load Balance
Principle in LLM Inference
In Section 4, we introduced the Balance–Future (BF-IO) as a universal principle for
load balancing under sticky assignments and barrier synchronization. In this section,
we provide theoretical guarantee for its effectiveness by analyzing its performance
in the LLM decode serving setting and then discussing extensions to more general
workload patterns. Our primary focus is the data-parallel (DP) decoding bottleneck in
LLM serving: we show that, under a realistic overloaded regime and for any adversarial
arrival instance, BF-IO achieves significantly smaller imbalance than the baseline First-
Come-First-Serve (FCFS) policy used in many real systems. A detailed description
of the baseline FCFS policy can be found in Appendix B. We then outline how the
analysis framework extends beyond LLM serving to a broader family of load balancing
problems.
LLM serving-specific workload model.
We specialize the general model of Section 3 to the DP decode stage of LLM serving.
Recall that G denotes the number of computational workers (e.g., GPUs) and B the
maximum batch size (number of requests that can be processed simultaneously on a
single worker). We assume that
√
G = ˜o(B), which is an extremely mild assumption
in practice: typical batch sizes are B ∈[64, 128], whereas the number of workers G is
at most in the hundreds in real deployments.
14


As in Section 3, each request i is represented by a workload profile
Wi =
 w(1)
i
, w(2)
i
, . . . , w(oi)
i

,
where oi ∈N is the total number of processing steps for request i, and w(j)
i
≥0 denotes
the workload contributed by i in its j-th processing step. In the LLM setting, the
initial workload w(1)
i
corresponds to the prefill size, which we denote by si. We assume
that the prefill lengths {si} are i.i.d. draws from an unknown bounded distribution
Dpreﬁll supported on {1, . . . , smax}, and let σs be the standard deviation of Dpreﬁll.
We impose a non-degeneracy condition: there exists κ0 ∈(0, 1
2] such that
κ0 ≤
σs
smax
≤1
2.
The upper bound follows from Popoviciu’s inequality, which implies σs/smax ≤1/2
for any distribution supported on an interval of length smax. The lower bound κ0 > 0
simply rules out pathological cases where almost all prompts have essentially identical
prefill length.
After prefill, LLM decoding proceeds token by token. Once request i starts decod-
ing, it occupies exactly oi −1 consecutive decode steps and its k-th decode step
(k ∈[oi −1]) requires workload si + k, reflecting the growth of its KV cache. We
assume that the decode lengths {oi} are i.i.d. draws from an unknown distribution
Ddecode on {1, 2, . . .}. Thus the full workload profile of request i takes the form
Wi =
 si, si + 1, . . . , si + oi −1

.
Overloaded arrival instances.
We now formalize the overloaded regime, which is the relevant regime for LLM serving:
if the system were lightly loaded, all requests could be processed immediately without
sophisticated scheduling. Intuitively, an arrival instance is overloaded if, at every step,
there are “many more” pending requests than free slots. Moreover, we assume that
no single prefill-length class dominates the pool. The latter condition reflects the
empirical fact that real workloads contain a variety of prompt lengths rather than
being concentrated on a single fixed prefill size.
Formally, at each step k, let Ik denote the set of requests that are pending in the
waiting pool, and let
Rk = {(si, oi) : i ∈Ik}
denote the multiset of their prefill and decode lengths. For ℓ∈{1, . . . , smax}, define
the length class
Rk(ℓ) := { i ∈Ik : si = ℓ}.
Let Ck = PG
g=1 cg(k) denote the total number of free slots at step k, where cg(k) is
the number of available batch slots on worker g.
15


Definition 1 (Overloaded arrival instance family I) An arrival instance I is called overloaded
if, for every step k,
min
ℓ∈{1,...,smax}
 Rk \ Rk(ℓ)


≥Ck.
Let I denote the family of all overloaded arrival instances.
In words, even after removing all requests belonging to whichever single prefill-
length class is most numerous at step k, the remaining pool still contains at least Ck
requests and can therefore fill all free slots freed in step k. This condition is typically
satisfied in heavily loaded real systems: prompt lengths exhibit substantial diversity,
and the number of pending requests is far larger than the instantaneous capacity.
Specialization of BF-IO to H = 0.
As discussed in Section 4, BF-IO can, in principle, use a short lookahead window
of length H ≥0. From an analytical perspective, however, rigorous control of the
dynamics for H ≥1 is extremely involved, because current assignments interact with
future arrivals and future decisions within the window. In this section we therefore
focus on the case H = 0, where BF-IO reduces to selecting, at each step, the assignment
that minimizes the current-step imbalance. As we will show, even this purely myopic
variant achieves substantial improvement over FCFS in the LLM decode model. This is
fantastic since we demonstrate that modest H > 0 yields further gains when realistic
predictive signals are available in Section 4.
Imbalance improvement ratio (IIR).
We compare BF-IO and FCFS via an imbalance improvement ratio, which measures, in
the worst arrival instance, the long-run factor by which BF-IO reduces average imbal-
ance relative to FCFS. For a given arrival instance I ∈I and policy π ∈{BF-IO, FCFS},
let AvgImbalance(π; I) denote the time-average imbalance defined in Section 3. We
define
IIR = inf
I∈I
 
lim
K→∞
E

AvgImbalance(FCFS; I)

E

AvgImbalance(BF-IO; I)

!
,
where the expectation is taken over the randomness in the prefill and decode lengths
(si, oi) drawn from Dpreﬁll and Ddecode. The arrival sequence I itself is chosen adver-
sarially from I; in particular, the adversary can choose any number of arrivals at each
step, subject only to the overloaded condition. Larger values of IIR correspond to
larger worst-case improvements of BF-IO over FCFS.
Directly lower bounding IIR in full generality is challenging. To build intuition,
we first study a warm-up setting in which all decode lengths are homogeneous: oi = o
for every request i. In this case, the dynamics simplify substantially: once GB jobs
are admitted, they run in perfect lockstep for o decode steps and then all complete
simultaneously. Under the overloaded condition (so that every worker runs at full
batch capacity), the per-step imbalance within a round of o steps is constant; optimiz-
ing long-run average imbalance reduces to optimizing the imbalance right after each
batch admission.
16


Fig. 5: Empirical distributions of decode lengths from production LLM traces [30–
33]. Decode lengths exhibit the geometric (discrete-exponential) pattern.
Theorem 1 (Warm-up: Homogeneous decode lengths) Consider the homogeneous-decode
model oi = o for all requests i. There exists a universal constant c > 0 such that
IIR ≥c κ0
p
B log G ·
G
G −1 = Ω
 p
B log G

.
(3)
Proof intuition: In the homogeneous-output setting, each admission round is struc-
turally simple: once GB jobs are admitted, they run in perfect lockstep for o token
steps and then all finish simultaneously. As a result, all steps in the round share the
same inter-device imbalance, and minimizing the long-run time-average imbalance is
equivalent to minimizing the imbalance immediately after admission in each round.
On the BF-IO side, this reduction means that BF-IO is effectively solving a static
bin-packing problem in every round: partition the GB prompt lengths into G groups of
size B to minimize the maximum per-device load. A simple exchange argument shows
that any such optimal assignment must be smax-balanced: the difference between the
heaviest and lightest devices can never exceed smax. This immediately implies that
the expected BF-IO imbalance per round is at most (G −1)smax, independent of the
detailed shape of the prefill-length distribution.
On the FCFS side, the story is almost the opposite. Under size-agnostic FCFS
admission and an overloaded pool, each device simply sees B i.i.d. prompt lengths.
The per-device loads Sg = PB
j=1 sg,j are thus i.i.d. with variance σ2
sB. For large
B, the central limit theorem (and a Berry–Esseen bound) shows that Sg behaves
approximately Gaussian. Taking the maximum over G such devices amplifies these
fluctuations: with constant probability, at least one device deviates above the mean by
order σs
√B log G. This produces an expected FCFS imbalance of order Gσs
√B log G,
while BF-IO keeps the imbalance of order (G −1)smax. The non-degeneracy condi-
tion σs/smax ≥κ0 > 0 then yields an imbalance-reduction factor of order √B log G,
uniformly over all overloaded arrival instances. The complete proof is deferred to
Appendix C.1.
□
Inhomogeneous setting.
The homogeneous-decode assumption oi = o in Theorem 1 serves as a warm-up: it
is analytically convenient but clearly unrealistic for LLM inference, whose responses
exhibit substantial variability in length. Empirical studies in Fig 5 on both open-
source ([30–33]) and production traces show that decode lengths typically follow a
17


heavy right-skewed pattern well approximated by a geometric (or discrete exponential)
distribution: most responses terminate quickly, while a non-negligible tail runs for
many tokens. Motivated by this evidence, we now consider an inhomogeneous setting
in which decode lengths are random and independent across requests. Specifically, we
assume
oi ∼Ddecode = Geo(p)
i.i.d. for each request i,
for some p ∈(0, 1), so that the mean and variance of oi are 1/p and (1 −p)/p2,
respectively. Under this more realistic decode model, the next theorem shows that
BF-IO still achieves a worst-case imbalance improvement factor of order Ω
 √B log G

,
with an explicit dependence on both the prefill and decode variability.
Theorem 2 (Inhomogeneous decode lengths) Consider the inhomogeneous-decode model in
which oi ∼Geo(p) i.i.d. for each request i with some p ∈(0, 1). Then, there exists a universal
constant c > 0 such that
IIR ≥c
p
smax
r
σ2s + 1 −p
p2
G
G −1
p
B log G = Ω
 p
B log G

.
(4)
Proof intuition
Under geometric output lengths with unit per-step growth, the stochastic dynamics
of loads under FCFS and BF-IO differ sharply. On the FCFS side, each slot behaves as
a simple renewal process: its size increases deterministically by one each step until a
geometric completion occurs, at which point it resets to a fresh prompt length S. In
stationarity, a snapshot of a slot therefore looks like
(prompt) S + (age) A,
where S has variance σ2
s and A is geometric with mean (1−p)/p and variance (1−p)/p2,
independent of S. This yields a per-slot “snapshot” variance
σ2
snap = σ2
s + 1 −p
p2
.
Aggregating B i.i.d. slots on each device gives per-device loads with variance σ2
snapB,
and comparing G such independent devices, standard Gaussian extreme-value the-
ory shows that at least one device sits of order σsnap
√B log G above the mean with
constant probability. This creates a typical FCFS imbalance of order G σsnap
√B log G.
On the BF-IO side, the analysis from Part 1 shows that, despite the asynchronous
completions, the myopic balancing rule enforces a contraction of the inter-device gap:
in each step, pre-admission loads shrink by a factor roughly (1−p), and the subsequent
assignment step of BF-IO prevents the gap from growing by more than smax, up to
small error events. Over time, this yields a steady-state average gap of order smax/p,
independent of (B, G) up to lower-order terms. Since instantaneous imbalance is at
most (G −1) times the gap, the long-run average imbalance under BF-IO is at most
18


(G −1)smax/p, while FCFS incurs imbalance of order Gσsnap
√B log G. The resulting
imbalance–reduction ratio is therefore of order
G σsnap
√B log G
(G −1) smax/p
≍p σsnap
smax
p
B log G,
matching the homogeneous-output case in √B log G scaling, but with the snapshot
variance σ2
snap capturing the additional variability introduced by geometric completion
times. The complete proof can be found in Appendix C.2.
5.1 Theoretical Guarantee of BF-IO in General Cases
The LLM-specific analysis above exploits a particular structure of the workload
profiles,
Wi =
 si, si + 1, . . . , si + oi −1

,
where each alive request gains one unit of KV load per decode step. This captures
current DP decoding architectures, but many other load-balancing problems do not
obey this linear drift. A classical example is the constant-workload setting, where each
job has a fixed processing requirement per step and
Wi =
 si, si, . . . , si

(oi copies of si),
which arises naturally in traditional CPU or manufacturing workloads. Even within
LLM serving, future systems may reduce or compress KV usage dynamically so that
per-step cost does not grow exactly by +1; instead, every alive request may experi-
ence a common but time-varying increment that reflects changes in the underlying
architecture or memory-management policy.
To encompass these scenarios, we consider a more general class of non-decreasing
workload drift models. We retain the prefill and decode assumptions from the inho-
mogeneous LLM model—namely, si drawn from a bounded prefill distribution with
variance σ2
s and decode lengths oi ∼Geo(p) i.i.d. for some p ∈(0, 1)—but relax the
per-step growth pattern as follows.
Definition 2 (Non-decreasing Workload Drift) There exists a deterministic sequence
(δk)k≥1 with
0 ≤δk ≤δmax
for all k ≥1,
where δmax is a non-negative constant independent of (B, G), such that for every request i,
w(1)
i
= si,
w(j)
i
= si +
j−1
X
t=1
δt,
j = 2, . . . , oi.
In words, all alive requests share the same nonnegative increment δk at global
step k; workloads are non-decreasing over time and their per-step growth is uniformly
bounded. The LLM decode model with linear KV growth corresponds to the special
case δk ≡1 for all k, while the constant-workload model corresponds to δk ≡0. More
19


sophisticated architectures, in which per-step KV work is throttled or sparsified over
time, are captured by intermediate patterns with 0 < δk < 1 or time-varying δk.
The following theorem shows that BF-IO continues to enjoy a worst-case imbalance
improvement factor of order √B log G relative to FCFS in this more general setting.
Theorem 3 (General non-decreasing workload drift) Consider the inhomogeneous-decode
model in which oi ∼Geo(p) i.i.d. for each request i with some p ∈(0, 1), and assume the
non-decreasing drift structure in Definition 2. Then there exists a universal constant c > 0,
depending only on (σ2s, p, δmax, smax), such that
IIR = inf
I∈I
 
lim
K→∞
E

AvgImbalance(FCFS; I)

E

AvgImbalance(BF-IO; I)

!
≥c p σs
smax
G
G −1
p
B log G = Ω
 p
B log G

.
(5)
Proof intuition: Compared with the “+1 per step” case, the core structure of the
BF-IO analysis remains unchanged: the gap contraction still comes entirely from com-
pletions, not from the deterministic per-step growth. When every survivor gains a
common increment δk at step k, each device’s load is shifted by exactly Bδk, so the
inter-device gap D(k−1) is unaffected by this drift; only the random removals from
completions change the spread. As a result, the separation lemma (which enforces
D(k) ≲Apre(k) −Bp on good completion-count events) still holds since it depends
only on prompt sizes and binomial completions, not on the specific value of δk. The
main technical adjustments are (i) in the concentration lemma, where the per-slot
summands are now bounded by Λ(k) + δmax instead of Λ(k), so the Bernstein bound
is applied with a slightly larger envelope, and (ii) in the envelope/age accounting,
where we use that an alive job of age r has size at most smax + δmaxr, so the excess
Λ(k) −smax is controlled by δmax times the maximum age. The geometric tail of the
output length then yields the same exponential decay in age (up to constants depend-
ing on δmax), and hence the same O(smax/p) bound on the long-run average gap as
in the +1 case. In other words, as long as the per-step increments are common across
jobs and uniformly bounded, the contraction mechanism and the averaging argument
that underpin the BF-IO upper bound survive unchanged; only the constants in the
concentration and tail estimates are rescaled by δmax.
In the constant +1 model, the slot-level process under FCFS is a homogeneous
Markov chain that admits a clean stationary representation: at any snapshot, each slot
contains “prompt + geometric age”, and the age has variance (1−p)/p2. With a time-
varying drift sequence (δk), the chain for Yg,b(k) becomes non-homogeneous, but the
key stochastic structure survives: completions still occur as i.i.d. Bernoulli(p) trials,
independent of job sizes and of the drift. This means that at any step k, the prompt
length of the current job in a slot is still an independent copy of S, and the number
of drift steps it has accumulated—its age—still has geometric tails with parameter p,
regardless of the past. The per-slot snapshot can therefore be written as
(prompt) + (sum of the last Ak drifts),
20


where Ak has uniformly bounded moments and δk is uniformly bounded. This imme-
diately implies that the per-slot variance is uniformly bounded below by σ2
s (from
the prompt) and the third moment is uniformly bounded above, independent of time.
Summing B such independent snapshots on each device yields loads with variance
of order B, and a Berry–Esseen bound (in its non-identical version) shows that each
device’s load fluctuates on the
√
B scale and is close to Gaussian. Taking the max-
imum over G devices then produces an excess of order √B log G above the average
device load, with constant probability, exactly as in the homogeneous case. Thus,
even though the per-slot chain is no longer time-homogeneous and lacks a simple sta-
tionary law, the bounded and common per-step drift leaves the order of the FCFS
imbalance unchanged: its expected size remains Θ
 G√B log G

. This concludes that
the imbalance-reduction factor is at least on the order of √B log G. The complete
proof can be found in Appendix C.3.
□
5.2 Theoretical Guarantee of BF-IO on Energy Consumption
We measure total energy consumption (in Joules) as the time integral of instantaneous
GPU power:
E =
Z T
0
P
 mfu(t)

dt,
(6)
where mfu(t) denotes the Model FLOPs Utilization (mfu), i.e., the ratio of observed
computational throughput to the GPU’s theoretical peak FLOPs capacity. Empirical
measurements in [21] validate that instantaneous GPU power is a sublinear function
of utilization:
P(mfu) = Pidle + (Pmax −Pidle)
 mfu
mfusat
γ
,
γ ∈(0, 1),
(7)
where Pidle is idle power, Pmax is peak power, mfusat is a saturation threshold, and γ <
1 captures the ineﬀiciency that GPUs draw substantial power even at low utilization.
Synchronized phase and utilization model.
To isolate the impact of barrier-induced waiting, we focus on the synchronized phase
of each decoding step (e.g., the attention computation) during which all workers
wait for the slowest one. Other phases (e.g., all-to-all EP/TP communication) can be
incorporated additively but are omitted here for clarity.
Recall from Section 3 that at discrete step k the instantaneous workload on worker
g under policy π is Lg(k), and Lg∗(k) = maxg∈[G] Lg(k). The wall-clock duration of
the synchronized attention phase in step k is linear in the maximal workload, namely,
τk(π) = κATT · Lg∗(k), for some constant κATT > 0 (seconds per unit workload).
During this synchronized phase, worker g performs useful attention computation for
κATTLg(k) seconds and waits at the barrier for κATT(Lg∗(k) −Lg(k)) seconds. Hence
its utilization fraction within the synchronized phase is
ug(k) :=
Lg(k)
Lg∗(k) ∈[0, 1].
(8)
21


As the utilization fraction equals to the instant throughput fraction, mfug(t)
mfusat , we have
mfug(t)
mfusat
= ug(k),
t during the synchronized phase of step k.
(9)
Under (7)–(9), the per-worker power in step k equals Pidle + (Pmax −Pidle) ug(k)γ.
Total synchronized-phase energy.
Fix an arrival instance I and a policy π. Let Kπ(I) denote the number of decoding
steps until all requests in I complete under π. The total energy consumed in the
synchronized attention phase across all G workers is
E(π; I) :=
G
X
g=1
Z Tπ
0
P(mfug(t)) dt
=
Kπ(I)−1
X
k=0
G
X
g=1
τk(π)

Pidle + (Pmax −Pidle) ug(k)γ
= κATT
Kπ(I)−1
X
k=0
Lg∗(k)
G
X
g=1

Pidle + (Pmax −Pidle) ug(k)γ
,
(10)
where Tπ = PKπ(I)−1
k=0
τk(π) is the corresponding makespan of the synchronized phase.
Define the policy-independent total attention workload of the instance I as:
W(I) :=
X
i∈I
oi
X
j=1
w(j)
i
=
Kπ(I)−1
X
k=0
G
X
g=1
Lg(k),
(11)
where the second equality holds for every policy π because each request contributes
its full workload profile exactly once regardless of assignment timing or placement.
Also define the cumulative imbalance up to completion:
ImbTot(π; I) :=
Kπ(I)−1
X
k=0
Imbalance(k; π) =
Kπ(I)−1
X
k=0
G
X
g=1
 Lg∗(k) −Lg(k)

.
(12)
Finally, for a baseline policy π0, define the normalized imbalance level
ηsum(π0; I) := E

ImbTot(π0; I)

E

W(I)

.
(13)
Energy savings from imbalance reduction.
We now show that any guaranteed improvement in imbalance yields an explicit
guaranteed improvement in total synchronized-phase energy (as a percentage), with
constants depending only on the power model parameters.
22


Theorem 4 (Energy saving guarantee from imbalance reduction) Let π0 be any baseline
policy and π1 any improved policy. Suppose the imbalance improvement factor satisfies
IIR = inf
I∈I
 
lim
K→∞
E

AvgImbalance(π0; I)

E

AvgImbalance(π1; I)

!
≥α
for some α > 1.
(14)
Define
Cγ := (1 −γ)Pmax + γPidle,
Dγ := (1 −γ)(Pmax −Pidle),
(15)
then the synchronized-phase energy saving fraction satisfies
E

E(π0; I)

−E

E(π1; I)

E

E(π0; I)

≥
1
Pmax/ηsum(π0; I) + Cγ

Pidle

1 −1
α

−Dγ
α

.
(16)
Proof sketch.
Equation (10) expresses E(π; I) as a sum over steps of the barrier duration
κATTLg∗(k) times power. Writing ug(k)γ = ug(k) + (ug(k)γ −ug(k)) and using
Lg∗(k)ug(k) = Lg(k) yields an exact decomposition:
E(π; I) = κATTPmaxW(I) + κATTPidleImbTot(π; I) + (concavity correction).
Since γ ∈(0, 1), uγ is concave on [0, 1]; the tangent bound at u = 1 implies that the
concavity correction is nonnegative and at most κATTDγ ImbTot(π; I). This shows
E(π; I) is sandwiched between two aﬀine functions of ImbTot(π; I) with the same
policy-independent baseline term κATTPmaxW(I). Combining these bounds with the
imbalance ratio assumption (14) and normalizing by W(I) yields (16). The complete
proof can be found in Appendix C.4.
Remark 1 (Instantiation to π0 = FCFS and π1 = BF-IO in our LLM model) In the inho-
mogeneous LLM decode model of Section 5, if we take π0 = FCFS and π1 = BF-IO, then
(14) holds with α = c
p
smax
q
σ2s + 1−p
p2
G
G−1
√B log G, and thus Theorem 4 yields a non-
trivial constant lower bound on the synchronized-phase energy saving fraction once IIR is
suﬀiciently large. Moreover, under the overloaded regime and geometric output lengths, the
normalized imbalance level ηsum(FCFS; I) admits an explicit lower bound:
ηsum(FCFS; I) ≳
σsnap
µs + 1−p
p
r
log G
B
,
σ2
snap = σ2
s + 1 −p
p2
.
(17)
Plugging (17) and α = c
p
smax
q
σ2s + 1−p
p2
G
G−1
√B log G into (16) yields an explicit energy
saving guarantee in terms of (p, σs, smax, B, G) and (Pidle, Pmax, γ). The complete proof of
Inequality (17) can be found in Appendix C.5
Corollary 1 (Asymptotic energy saving at scale). Under the conditions of Remark 1,
as the number of workers G →∞, the guaranteed energy saving fraction of BF-IO
relative to FCFS converges to
lim
G→∞
E

E(FCFS; I)

−E

E(BF-IO; I)

E

E(FCFS; I)

≥
Pidle
(1 −γ)Pmax + γPidle
.
(18)
23


Remark 2 (Concrete instantiation for modern GPUs) For NVIDIA A100 GPUs, empirical
measurements in [21] report Pidle = 100W, Pmax = 400W, and γ = 0.7. Substituting into
(18) in Corollary 1:
Pidle
(1 −γ)Pmax + γPidle
=
100
0.3 × 400 + 0.7 × 100 = 100
190 ≈52.6%.
That is, in large-scale deployments, the principled load balancing introduced in this work can
reduce synchronized-phase energy consumption by more than half compared to the widely-
deployed FCFS baseline. This asymptotic guarantee underscores the sustainability impact of
addressing the decode-stage load imbalance bottleneck at fleet scale.
6 Experiments
We evaluate the proposed routing strategies through discrete-event simulation of
GPU-based LLM request processing. This section describes the experimental setup,
evaluation metrics, and numerical results.
6.1 Dataset and Request Generation
To simulate realistic LLM inference workloads, we use request traces derived from
LongBench [34], a challenging benchmark designed for long-context language mod-
els. LongBench encompasses diverse long-text application scenarios—single-document
and multi-document question answering, summarization, few-shot learning, synthetic
tasks, and code completion—and reflects production-relevant settings in which input
prompts are substantially longer than typical benchmarks. Each request is character-
ized by a pair (si, oi), where si denotes the prefill (input prompt) length and oi the
decode (output) length; the resulting distributions are shown in Figure 6. Requests
arrive according to a stationary Poisson process at a rate exceeding the system’s pro-
cessing capacity, ensuring that the system operates in the overloaded regime central
to our theoretical analysis. For comparison, we also evaluate on BurstGPT [35], a
lighter-load trace; these results appear in Appendix D.2.
Fig. 6: Prefill and decode length distribution of LongBench.
24


6.2 Simulation Environment
The router simulator models an online setting with G = 256 A100 GPU workers, each
supporting up to B = 72 concurrent requests (batch size). Key components include:
• Undiscovered queue: Requests not yet revealed to the router.
• Wait queue: Candidate requests available for routing decisions.
• Active sets Ag: Requests currently executing on GPU g.
• Load tracking Lg: Total token load on GPU g, defined in Equation (1).
Time Progression Model.
Each simulation step advances time by:
∆t = C + tℓ· max
g∈[G] Lg
(19)
where C = 9.775 · 10−3 is a fixed overhead constant and tℓ= 1.005 · 10−7 is the
per-token generation latency. The values of C and tℓare derived from a minimum
mean square error regression on real-world traces. During each step, all active requests
generate one token. A request i completes when τi ≥oi.
Routing Policies.
We evaluate four routing policies: (i) FCFS, (ii) Join-Shortest-Queue (JSQ), (iii) BF-
IO (H = 0), (iv) BF-IO (H = 20), (vi) BF-IO (H = 40), (vii) BF-IO (H = 60), (viii)
BF-IO (H = 80), (ix) BF-IO (H = 100).
6.3 Evaluation Metrics
We evaluate routing performance using four metrics. Let G denote the number of
GPUs, A(k)
g
the active requests on GPU g at step k, and Lg(k) = P
i∈A(k)
g
w(k−xi+1)
i
the instantaneous workload on worker g, where w(j)
i
is the workload contributed by
request i in its j-th processing step.
Average Imbalance.
Following the theoretical framework, we adopt the average imbalance as the primary
metric. At each step k, the instantaneous imbalance is defined as:
AvgImbalance = 1
K
K
X
k=1
Imbalance(k)
(20)
In addition to the theoretically motivated imbalance metric, we evaluate two
metrics that directly reflect real-world LLM inference eﬀiciency:
25


Throughput.
Throughput measures the token processing rate (tokens/second):
Throughput =
PK
k=1
A(k)
PK
k=1 ∆t(k)
(21)
where A(k) = S
g A(k)
g
denotes all active requests at step k, and ∆t(k) = C + tℓ·
maxg Lg(k) is the wall-clock duration of step k. Since step duration is governed by
the maximum load across workers, reducing imbalance directly increases throughput.
Time Per Output Token (TPOT).
TPOT captures per-request latency as perceived by users:
TPOT = 1
N
N
X
i=1
tfinish
i
−tstart
i
oi
(22)
where tstart
i
and tfinish
i
are the start and completion times of request i, and oi is its
output length. Requests assigned to heavily loaded GPUs experience longer per-step
durations due to the barrier synchronization, resulting in higher TPOT. This metric
is widely used in production LLM serving systems to measure user-facing latency.
Energy Consumption.
As is mentioned in Section 5.2, the total energy consumption (in Joules) is the time
integral of instantaneous GPU power (Equation (6)), where the instantaneous GPU
power can be expressed by Equation (7). Details of MFU computation and parameter
calibration are provided in Appendix D.
6.4 Results
We present quantitative comparisons across routing policies, supported by visualiza-
tion of load dynamics and power consumption patterns.
Load Trajectory Analysis.
Figure 7 presents per-worker load trajectories over the processing interval. To maintain
visual clarity with G = 256 workers, we display a random sample of 16 GPUs. Under
the baseline policies FCFS and JSQ, worker loads exhibit pronounced fluctuations
ranging from 10M to 35M tokens during stable decoding, with high-frequency oscil-
lations reflecting severe and persistent imbalance across the cluster. BF-IO (H = 0)
substantially compresses the load spread to approximately 12M–23M tokens. With a
40-step lookahead, BF-IO (H = 40) achieves markedly tighter clustering (16M–17M
tokens) despite the challenging workload characteristics, demonstrating the robustness
of the lookahead mechanism under high-variance conditions.
26


Fig. 7: Per-worker token load trajectories under four routing policies (16 randomly
sampled workers from G = 256). FCFS and JSQ exhibit extreme fluctuations (10M–
35M tokens), whereas BF-IO policies maintain substantially tighter distributions, with
BF-IO (H = 40) achieving near-uniform loads around 16M–17M tokens.
Power Consumption Analysis.
Figure 8 illustrates the temporal dynamics of average GPU power consumption,
revealing how routing policies affect both aggregate energy and instantaneous power
stability.
During stable decoding (excluding initial ramp-up), BF-IO maintains consistently
high instantaneous power (395–400 W), indicating balanced utilization across all
devices. For reference, the NVIDIA A100 GPU has a maximum power draw of
Pmax = 400 W (Appendix C.4), implying that BF-IO operates near full computa-
tional capacity throughout execution. In contrast, the baseline FCFS policy oscillates
between 270–360 W, reflecting periods in which subsets of GPUs remain underutilized
due to uneven workload distribution.
Crucially, although BF-IO operates at higher instantaneous power, it achieves
substantially lower total energy consumption (20.9 MJ vs. 29.1 MJ for FCFS). This
apparent paradox resolves by recognizing that total energy is the time integral of
instantaneous power: by maintaining all GPUs at high utilization, BF-IO completes
the workload faster, reducing total execution time and thereby the integral. In effect,
balanced scheduling eliminates idle power—energy expended without productive
computation—yielding significant net savings.
27


Fig. 8: Average GPU power consumption over time. BF-IO sustains consistently high
power draw (395–400 W), indicating near-full utilization, and completes the workload
faster. The baseline FCFS policy exhibits substantial fluctuations (270–360 W) due to
load imbalance. Despite higher instantaneous power, BF-IO achieves 30% lower total
energy consumption (20.9 MJ vs. 29.1 MJ) by eliminating idle time.
Quantitative Metrics Comparison.
Table 1 summarizes performance across routing policies. BF-IO consistently out-
performs baselines across all metrics for every choice of lookahead window H
tested.
Table 1: Performance comparison on LongBench. Arrows indicate preferred
direction. Best values in each column are highlighted.
Policy
Avg Imbalance↓
Throughput↑
TPOT↓
Energy↓
(×108)
(103 tok/s)
(s/tok)
(MJ)
FCFS
37.0
5.47
2.97
29.1
JSQ
28.5
6.15
2.64
27.8
BF-IO (H = 0)
9.94
8.55
1.93
23.4
BF-IO (H = 20)
3.03
10.1
1.70
21.3
BF-IO (H = 40)
2.49
10.5
1.67
20.6
BF-IO (H = 60)
2.44
10.4
1.66
20.9
BF-IO (H = 80)
2.43
10.4
1.70
21.0
BF-IO (H = 100)
2.51
10.4
1.67
20.9
28


The optimal lookahead window is H = 40. Relative to FCFS, BF-IO (H = 40)
reduces average imbalance by a factor of 15×, improves throughput by 92%, reduces
time-per-output-token (TPOT) by 44%, and lowers energy consumption by 29%.
These gains are substantial and consistent across metrics.
Comparison across BF-IO variants reveals a tradeoff in selecting H (Figure 9).
When H is too small, the policy optimizes myopically, balancing loads only over
the immediate future and missing opportunities to anticipate near-term completions.
When H is too large, the optimization horizon extends beyond the range of reli-
able prediction—since arrivals are online and unpredictable, the objective becomes
biased toward stale information. Additionally, larger H incurs greater computational
overhead per decision. Empirically, performance saturates around H = 40; further
increases yield no benefit and marginally degrade some metrics.
0
20
40
60
80
100
Window Length (H)
2
4
6
8
10
Avg Imbalance (×108)
±1 std
Mean imbalance
0
20
40
60
80
100
Window Length (H)
8.5
9.0
9.5
10.0
10.5
Throughput (×103)
±1 std
Mean imbalance
0
20
40
60
80
100
Window Length (H)
1.6
1.7
1.8
1.9
2.0
TPOT
±1 std
Mean imbalance
0
20
40
60
80
100
Window Length (H)
20.5
21.0
21.5
22.0
22.5
23.0
23.5
Energy (MJ)
±1 std
Mean imbalance
Fig. 9: Effect of lookahead horizon H on key performance metrics. All metrics improve
rapidly as H increases from 0 to 40, then plateau. The optimal operating point
balances prediction reliability against computational overhead; beyond H ≈40, addi-
tional lookahead provides no further benefit.
29


6.5 Ablation Study
Scalability with Cluster Size.
To understand how BF-IO scales with increasing computational resources, we conduct
an ablation study varying the number of GPU workers G from 16 to 224 while keep-
ing the workload fixed (Longbench dataset). Figures 10 and 11 present the scaling
behavior of BF-IO compared to FCFS.
Fig. 10: Scalability analysis with varying cluster size G. Left: Average imbalance
grows super-linearly under FCFS but remains bounded under BF-IO. Right: BF-IO
achieves near-linear throughput scaling while FCFS exhibits diminishing returns.
The left panel of Figure 10 reveals that the average imbalance under FCFS grows
super-linearly with cluster size, increasing from approximately 3 × 108 at G = 16 to
over 3.5 × 109 at G = 224—a 12× increase for a 7× scale-up in resources. In contrast,
BF-IO maintains imbalance below 3 × 108 across all configurations, demonstrating
that workload-aware scheduling effectively contains the coordination overhead even
at scale. At G = 224, BF-IO achieves 14× lower imbalance than FCFS.
The right panel shows that throughput scaling exhibits fundamentally different
characteristics between the two strategies. BF-IO achieves near-linear scaling, increas-
ing from approximately 1k tokens/s at G = 16 to over 9k tokens/s at G = 224.
Meanwhile, FCFS throughput grows sub-linearly, reaching only about 4.5k tokens/s at
the largest scale—less than half of BF-IO’s throughput. This divergence stems directly
from the imbalance growth: as more workers are added under FCFS, the probability
of severe load skew increases, causing stragglers that bottleneck overall progress.
Figure 11 highlights the operational eﬀiciency gains of BF-IO. While FCFS energy
consumption remains relatively flat around 31–34 MJ across different cluster sizes,
BF-IO energy decreases steadily from approximately 29 MJ at G = 16 to 22 MJ at
G = 224. The energy reduction percentage (green line) increases monotonically with
cluster size, rising from 12% at small scales to 30% at G = 224. This widening gap
demonstrates that the benefits of intelligent scheduling compound at larger scales: not
30


Fig. 11: Energy consumption scaling with cluster size. BF-IO (blue) achieves con-
sistently lower energy than FCFS (orange), with energy reduction (green, right axis)
increasing from 12% at G = 16 to 30% at G = 224.
only does BF-IO complete workloads faster, but it also avoids the energy waste from
idle GPUs waiting at synchronization barriers.
7 Discussion and Conclusion
7.1 Summary of contributions
This work develops a universal load-balancing principle for barrier-synchronized sys-
tems with sticky assignment decisions. We formalize the principle as BF-IO, which
selects assignments by minimizing a short-horizon prediction of the imbalance metric
induced by the slowest worker via an integer optimization. We establish worst-case,
scale-sensitive guarantees for BF-IO under adversarial arrivals in the LLM decode
model and extend the analysis to a broader class of non-decreasing workload drift
processes. Crucially, we prove that this imbalance reduction yields rigorous energy sav-
ings guarantees: as system scale grows, the guaranteed energy reduction approaches
52% for modern GPU hardware. This result establishes, to our knowledge, the first
theoretical link between load balancing policy and provable energy eﬀiciency in LLM
serving. On the empirical side, extensive numerical experiments on both public bench-
marks and proprietary industrial traces demonstrate that BF-IO substantially reduces
imbalance, improves throughput, and lowers energy consumption relative to widely
deployed baselines.
31


7.2 Broader relevance beyond LLM serving
Although the LLM serving bottleneck motivates our study, the abstraction captured
by BF-IO is broader: many scientific and engineering workloads evolve in discrete
steps, include at least one synchronization point (explicitly or implicitly), and bind
work to a device or process for eﬀiciency reasons (data locality, memory residency, or
stateful execution). Two representative examples illustrate how the short-lookahead
philosophy transfers.
Large-scale linear algebra and matrix computation.
Many high-performance linear algebra kernels (e.g., blocked matrix factorizations,
tiled matrix multiplication, and iterative solvers) execute as task graphs over sub-
matrices/tiles. Even when per-kernel costs are relatively stable, effective completion
times of “jobs” (usually several hours) are hard to predict far in advance due to
cache effects, contention, and dependence constraints. Nevertheless, short lookahead
information is often readily available from the runtime: tile-based systems maintain
progress counters and DAG readiness queues, and a node can infer whether it will fin-
ish a set of outstanding tile tasks within the next few scheduling quanta by tracking
(i) the number of remaining tiles on the critical path, (ii) the fraction of tiles already
completed in the current panel/update phase, and (iii) convergence indicators for iter-
ative methods (e.g., residual norms approaching tolerance). In such settings, BF-IO
can be applied by treating each core/GPU as a worker, using short-horizon comple-
tion signals for currently running tasks, and routing or admitting new tasks to reduce
load imbalance.
Adaptive multi-physics simulations with mesh refinement.
A second example arises in physics and geoscience simulations that use adaptive
mesh refinement (AMR) or adaptive particle resolution [36, 37]. Here the per-step
workload on each rank/accelerator can drift as refinement/coarsening changes the
number of cells/particles and as localized physics kernels activate (e.g., shocks, phase
boundaries, or localized forcing). Long-run workload is diﬀicult to forecast because
refinement decisions depend on evolving solution features, but near-term evolution
is often predictable: refinement criteria are computed at regular intervals from local
error indicators, gradients, or thresholded physical diagnostics, so it is feasible to esti-
mate whether a region will remain refined (or be coarsened) over the next few time
steps. Because time stepping typically includes synchronization (global reductions,
halo exchanges, or coordinated stage boundaries), the slowest rank gates progress.
Under these conditions, a short-lookahead policy like BF-IO is natural: it uses near-
term refinement/coarsening signals of ongoing regions to guide assignment of newly
generated work units (e.g., blocks, patches, or particle batches) so as to reduce
barrier-induced idle time.
These examples share the key property exploited by BF-IO: while predicting the
total remaining runtime of a newly arriving unit of work is unreliable, predicting
whether ongoing work will finish within a short window is often feasible from progress
signals, diagnostics, or domain structure. This makes short lookahead a practical and
portable information interface across scientific workloads.
32


7.3 Limitations and future directions
Several limitations suggest clear directions for future work.
System interfaces and buffering.
Our strongest theoretical guarantees are developed under a centralized waiting-pool
abstraction. Some serving engines bind requests immediately to per-worker queues,
which reduces the scheduler’s ability to reshape batches at slot-release times and can
weaken future-aware balancing when queueing delays exceed the lookahead window.
A promising direction is to develop a full theory for the “instant-dispatch” inter-
face (routing to per-worker FIFO queues) and to quantify how short-horizon signals
degrade as activation delays grow.
Model scope.
Our general theory covers a broad class of non-decreasing workload drift processes,
motivated by stateful execution where work grows with accumulated state. Some
systems exhibit decreasing or highly non-monotone per-step workloads (e.g., aggres-
sive pruning, adaptive compression, or multi-phase jobs with alternating heavy/light
stages). Extending worst-case theory to such regimes, and characterizing when
monotonicity is essential, remains open.
Multi-objective serving.
Real deployments often optimize multiple objectives simultaneously (tail latency, fair-
ness across tenants, service-level objective compliance, and energy caps). Although
the BF-IO objective aligns well with barrier-induced idle reduction, integrating fair-
ness and service-level objective constraints into the short-horizon optimization—while
preserving millisecond decision budgets—deserves dedicated study.
Appendix A
Supplementary Materials for Section 1
A.1
Literature Review
A.1.1
Load Balancing Problem
In this section, we briefly review classical load balancing algorithm structures [38]
(except for First-come-first-serve (FCFS) [39–41], which serves as the benchmark in
the paper and is discussed in Section 3) and elucidate their fundamental ineﬀiciency
or incompatibility in the LLM inference problem:
• Join Shortest Queue (JSQ). JSQ [42–44] routes each arriving request to the worker
with the smallest queue length (ties arbitrary). In practice, LLM serving systems
in widely used (e.g., vLLM [45], SGLang [46]) implement a JSQ-style policy, so
we take JSQ as the primary baseline. The core diﬀiculty is that, in decode-time
DP with sticky assignments, “queue length” is a poor proxy for per-step work:
true workloads are unknown at assignment time and evolve as KV caches grow, so
production systems typically measure the number of requests rather than their total
workload. This makes JSQ brittle. Consider G workers and an adversarial arrival
33


sequence with “heavy” requests of unknown long length L and “short” requests of
length s ≪L. Because a heavy counts as one request, JSQ will place a heavy on
the worker that currently has the fewest requests; subsequent bursts of many short
requests inflate the counts on other workers while leaving the heavy worker’s count
small, so the next heavy again goes to the same worker. After k such events, that
worker holds k heavies while others hold many shorts. Since per-step time under
the barrier model is
Tstep(t) = max
g
T (g)
local,
we have T JSQ
step (t) ≥kc(L), where c(L) is the per-step local cost of a single heavy
(proportional to its resident KV). A balanced assignment would yield T ⋆
step(t) ≤
⌈k/G⌉c(L), so the ratio is Ω(G). The problem persists because assignments are non-
preemptive: as heavies generate more tokens, their per-step cost grows, yet JSQ
cannot correct earlier placements. Hence JSQ targets the wrong surrogate and can
be highly ineﬀicient in the decode-stage, barrier-synchronized setting.
• Round-Robin (RR).
RR [47–49] dispatch assigns the i-th arriving (post-prefill)
request to worker ((i −1) mod G) + 1, cycling through the G workers irrespective
of job size, resident KV, or drift. Under sticky, non-preemptive assignments, this
determinism is exploitable. Consider an online sequence in which requests with
indices i ∈1, 1 + G, 1 + 2G, . . . have long decode length L (heavy), and all others
have short length s ≪L. RR places all heavy jobs on worker 1 and none on workers
2, . . . , G. Let H be the number of heavy jobs that arrive before any heavy completes
(this is typical when L is large). Then, for each decode step in that interval, the
per-step makespan under RR satisfies
T RR
step(t) ≥H · c(L),
where c(L) is the per-step local cost contributed by one heavy (proportional to its
resident KV), because the max local time occurs on worker 1 carrying H heavies.
By contrast, an assignment that evenly spreads heavies yields
T ⋆
step(t) ≤
l
H
G
m
· c(L),
so the ratio T RR
step/T ⋆
step ≥
H
⌈H/G⌉≥G. This gap persists across many steps because
jobs are sticky and heavy requests evolve slowly, so RR is not competitive for our
LLM inference load balancing problem under adversarial arrivals.
• Power-of-d.
At each arrival, Power-of-d [50–52] samples d ≪G workers uni-
formly at random and routes the request to the one with the smallest queue length
among the d sampled. This reduces coordination overhead from O(G) to O(d) per
arrival and, in classical migratable/size-known settings, retains JSQ-like balance
with strong tail guarantees. However, in decode-time DP with sticky assignments
and barrier synchronization, power-of-d inherits the core JSQ shortcomings: queue
length is a poor surrogate for workload when job sizes are unknown and evolve
with KV growth; placements are non-preemptive, so early mistakes persist; and
34


minimizing counts does not control the per-step maximum local time that gates
progress.
• Min-min.
The Min–Min scheduler [53–55] builds an “earliest completion time”
(ECT) matrix and repeatedly assigns the task that can finish soonest on some
worker. Concretely, for each unscheduled task i and worker g, compute ECTig =
rg+pig, where rg is the worker’s ready time and pig is the estimated processing time
of task i on g. Choose i⋆= arg mini ming ECTig, assign it to g⋆= arg ming ECTi⋆g,
update rg⋆, and repeat. This strategy presumes reasonably accurate, worker-specific
pig and is effective for static batches on heterogeneous clusters. However, it is
incompatible in our decode-time setting it is ill-suited: (i) processing times are
unknown and evolve token-by-token with KV growth, so pig is neither observable
nor stationary; (ii) assignments are sticky (KV migration is impractical), so Min–
Min’s advantage of revising choices before start or splitting jobs is unavailable; (iii)
computing and maintaining a full ECT matrix is computationally prohibitive. Con-
sequently, Min–Min cannot address the constraints of decode-stage load balancing
in LLM serving.
• Opportunistic Load Balancing and Load Balancing Min-Min (OLB+LBMM). This
hybrid couples opportunistic load balancing with a load-balancing Min–Min refine-
ment [56–58]. OLB greedily dispatches arrivals to the first idle or least-busy worker
(size-agnostic) to keep resources utilized; periodically, LBMM builds an earliest-
completion-time table over the waiting pool (often after chunking tasks) and
reassigns pending or not-yet-started chunks to heterogeneous workers that minimize
predicted completion time. This can reduce transient idleness relative to pure Min–
Min. However, it still relies on (i) accurate per-chunk processing-time estimates, (ii)
the ability to split work before start, and (iii) periodic ECT recomputation over-
head. Crucially, in our decode-time regime requests are indivisible: token generation
is sequential and bound to a single KV cache, so pre-chunking across workers is not
possible. Therefore, the OLB+LBMM architecture remains incompatible with the
constraints in LLM serving.
• Max–Min. Max–Min [59–61] is the dual of Min–Min: for each unscheduled task i
and worker g, compute the earliest–completion time ECTig = rg + pig. For each
task, take its best machine g(i) = arg ming ECTig with best time mi = ming ECTig.
Max–Min then selects i⋆= arg maxi mi (the task whose best predicted finish time
is largest), assigns it to g(i⋆), updates rg(i⋆), and repeats. This choice favors long
tasks early to mitigate starvation on heterogeneous clusters. In decode-time LLM
serving, the same incompatibilities as Min–Min apply: pig is unknown and evolves
token by token (KV growth), assignments are sticky and requests are indivisible
(no pre-chunking or migration), and maintaining ECT tables within millisecond is
impractical. Consequently, Max–Min is not suitable for the decode-stage, barrier-
synchronized LLM serving setting.
• Ant Colony Optimization (ACO).
ACO [62–64] is a constructive metaheuristic
that repeatedly builds task–worker assignments by sampling edges with proba-
bility Pig ∝τ α
igηβ
ig, where τig (pheromone) summarizes past good solutions and
ηig is a heuristic desirability. After evaluating an assignment, pheromones evap-
orate and are reinforced, and the process iterates over many ants/iterations. In
35


our decode-time setting ACO is incompatible for two core reasons. Information:
ACO needs informative processing-time estimates pig, but decode workloads are
unknown at assignment and drift token by token with KV growth, so the signals
required to guide sampling are unreliable. Computation: Even if perfect predictions
existed, ACO requires multiple ants and iterations and is typically batch oriented;
with online, non-stationary arrivals and a millisecond decision budget, recomputing
pheromones and re-sampling assignments per arrival is computationally infeasible.
Consequently, ACO and its variants are ill-suited to decode-stage load balancing.
• Honey Bee Foraging Algorithm (HBFA).
HBFA-style schedulers [65, 66] emulate
bee foraging: “employed” workers advertise their current load and the number of
pending priority tasks (a waggle-dance–like signal), while “onlooker” tasks proba-
bilistically select a destination with high advertised fitness (e.g., few priority tasks,
low queue length, suﬀicient CPU/memory). This algorithm aims to raise utilization
and cut mean response time on heterogeneous clusters. In decode-time LLM serv-
ing, HBFA is incompatible for the same structural reasons as ACO: (i) it relies on
queue-length/priority signals, but per-request workloads are unknown at assignment
and evolve token by token; (ii) its population-style selection/updates are infeasible
to a millisecond scheduling budget under online, non-stationary arrivals. Conse-
quently, HBFA does not satisfy the information, control, and timing constraints in
decode-stage load balancing.
• Throttled Load Balancing (TLB). TLB [67, 68] maintains a catalog of workers with
capability vectors and enforces a per-worker concurrency threshold Θ; an arriv-
ing request is routed to the first worker that is under its threshold and appears
“suitable” given current residual capacity. While this capacity-aware gating can out-
perform RR, it is misaligned with decode-time DP. First, TLB presumes a known,
stable requirement vector d(r) per request, but in decode the effective demand is
unknown at admission and drifts token by token as the KV cache grows, so eligi-
bility tests based on initial size or queue length are unreliable. Second, assignments
are sticky: early misplacements cannot be corrected as d(r) inflates. Third, throt-
tling caps concurrency rather than minimizing the per-step maximum local work;
depending on Θ, it can even increase idle time by admitting fewer short requests
to otherwise underloaded workers while a heavy request gates progress. So, TLB is
ineffective for decode-stage load balancing under unknown, evolving workloads and
synchronous barriers.
• Carton. Carton [69–71] combines load balancing with distributed rate limiting: a
controller spreads incoming work across workers, while each worker enforces a local
rate cap so that measured performance levels are equalized with minimal coordina-
tion. This can work well when workloads and rate signals are stable. In decode-time
DP, however, Carton is structurally incompatible for two reasons. First, the signals
it relies on—per-worker rates, queue lengths, or smoothed performance counters—
are poor surrogates for decode-time work: per-request workloads are unknown at
admission and drift token by token as KV caches grow, so rate-based estimates
become stale on the step-by-step timescale. Second, assignments are sticky: once
a request is placed on a decode worker, its KV cache is not migrated, so DRL
cannot reallocate in-flight work to correct early imbalances. Heavy requests then
36


continue to dominate a worker’s local time while others idle at barriers, leaving Car-
ton’s LB+DRL control loop without the information or actuation needed to repair
decode-stage imbalance.
A.1.2
LLM Serving and Energy Consumption
Recent work on the energy footprint of LLM serving falls into two complementary
directions: (i) characterization and accounting of energy, and (ii) control/optimization
of GPU power under service-level objectives (SLOs). On characterization, [72] profile
serving across batching and parallelism knobs and show stage-specific energy–latency
trade-offs; [73] frame sustainability as a system-level challenge, highlighting that serv-
ing can rival or exceed training in operational energy; [74] quantify both operational
and embodied emissions, showing that hardware generation and batching interact
non-trivially with energy eﬀiciency; and [21] model the fundamental power-utilization
relationship, establishing that GPU power scales sublinearly with utilization—idle
states draw substantial power without contributing throughput. On control, [75] build
workload-based energy/runtime models to derive offline energy-optimal serving poli-
cies on heterogeneous systems, while iteration-level dynamic voltage frequency scaling
(DVFS) emerges as a runtime lever: [76] propose SLO-aware GPU frequency control
tailored to autoregressive decoding, and [77] separate prefill and decode into distinct
control loops to reduce energy under trace replays without throughput loss. Together,
these studies establish that serving’s energy footprint is large, that idle power is
non-negligible, that prefill versus decode differ in performance–energy behavior, and
that SLO-aware frequency control and hardware/batching choices are potent—but
workload- and system-specific—tools
The studies above optimize per-device power or fleet-level energy/carbon via fre-
quency control, hardware selection, or offline scheduling models. None addresses the
decode-stage barrier objective where per-step time is governed by the maximum local
workload across data-parallel workers—precisely the imbalance our paper targets.
Our universal load-balancing principle is orthogonal and complementary: by provably
shrinking the per-step maximum (thus reducing systematic idle at barriers), it con-
verts wasted idle power into useful computation, lowering energy per token regardless
of which DVFS or hardware policy is deployed. Moreover, it applies under unknown,
drifting decode lengths where prediction-based controllers degrade. In short, prior
work characterizes and tunes how fast and how green a given worker runs; we address
how evenly the decode-time work is distributed across workers—an upstream lever
that tightens the energy/latency envelope that DVFS and carbon-aware schedulers
can then exploit.
A.1.3
LLM Serving Optimization
Recent systems work has rearchitected the serving stack to expose structural levers
that raise throughput and reduce latency/queuing contention. Engine designs such as
vLLM introduce PagedAttention to virtualize and compact the KV cache, eliminat-
ing fragmentation and enabling large, flexible batches with near-zero KV waste [45].
SGLang provides a program/runtime interface for multi-call LLM applications (tools,
control flow, structured I/O), co-designing execution with batching and caching to
37


keep the accelerator pipeline busy [46]. At the cluster level, prefill–decode (PD) dis-
aggregation separates prompt encoding from autoregressive decoding across GPUs to
remove cross-phase interference and to co-optimize latency targets for TTFT (pre-
fill) and TPOT (decode) [78, 79]. These structural choices interact with parallelism:
decode commonly uses data parallelism (DP) over requests, while model parallelism
mixes tensor and pipeline partitioning for large models and expert parallelism (MoE)
for sparse activation [80–83]. Together, this body of work establishes the modern
serving substrate—memory-eﬀicient KV management, program-aware engines, PD
disaggregation, and composable DP/TP/PP/EP—as the structural foundation on
which scheduling and load-balancing policies operate.
Besides optimization in architecture of LLM serving, many recent works design
scheduling algorithms to make LLM serving more eﬀicient. Production LLM engines
adopt iteration-level scheduling with continuous batching as the default control
primitive: Orca introduced iteration-level scheduling and selective batching to keep
accelerators busy despite heterogeneous request lengths [84], and vLLM co-designs
Paged-Attention with preemptive request scheduling to admit and retire sequences at
iteration granularity [45]. Building on these primitives, recent systems explore com-
plementary schedulers: Sarathi-Serve proposes stall-free scheduling via chunked prefill
to relieve the prefill–decode interference and improve the throughput–latency frontier
[85, 86]; Fast-Serve adds token-granular preemption with a skip-join multi-level feed
back queue (MLFQ) to reduce job completion time under bursty arrivals [87]. A paral-
lel line uses learned or proxy signals to approximate shortest-first: proxy-model-based
predictors to rank jobs and avoid head-of-line blocking [23], learned-to-rank policies
that plug into vLLM [24], and embedding-based estimators with preemptive sched-
ulers [26]. Fairness-aware policies integrate service fairness into continuous batching
without preemption [88]. Beyond single-instance engines, decentralized or cross-replica
schedulers have been proposed for heterogeneous or edge settings [89].
A growing subset of works tackles load balancing explicitly. ShuffleInfer disag-
gregates prefill and decode instances and introduces a two-level scheduler to spread
mixed workloads across instances, reporting latency gains but relying on instance-level
migration [90]. For MoE inference, HarMoEny balances expert load via dynamic token
redistribution and asynchronous expert prefetching across GPUs [91], while DuoServe-
MoE separates expert scheduling for prefill versus decode to match distinct activation
patterns [92]. At the router layer, performance-aware load balancers combine response-
length prediction with mixing strategies to steer flows across replicas [93]. Systems
surveys and empirical analyses further catalog scheduling variants and highlight the
gap between academic schedulers and deployable, low-overhead policies [94]. Many
of these works target expert-parallel (EP) balancing rather than data-parallel (DP)
balancing of per-request decode work; the latter is more challenging because job sizes
are unknown and evolving, assignments are sticky (KV non-migratable), and step
progress is gated by synchronous barriers. Among approaches that could be adapted to
DP, most are engine- or trace-specific heuristics without theoretical guarantees; their
effectiveness varies across datasets and configurations and thus does not constitute a
universal load-balancing principle.
38


Moreover, a recent theory stream formalizes LLM serving as online scheduling with
KV-cache constraints and provides provable policies at the single-worker level. [95]
develop a benchmark via a hindsight-optimal integer program, prove impossibility in
the fully online adversarial setting (no constant competitive ratio for any algorithm),
and design algorithms that are near-optimal in a semi-online model. Following this
model, [96, 97] relax several assumptions in the model. They study heterogeneous pre-
fill and unknown decode lengths with only interval prediction information. In parallel,
[98] introduce a fluid-guided framework with memory constraints, deriving Nested-
WAIT policies that are near-optimal against a fluid benchmark in heavy traﬀic. These
works provide principled scheduling for batching and scheduling under a single worker
and do not address problems under multiple workers.
A.1.4
Assignment and Scheduling Problem
Our decode worker selection problem is an instance of the online assignment paradigm:
jobs (requests) arrive sequentially, some side information may be observed, and the
decision maker must irrevocably assign each job to a resource so as to optimize
a system objective. Foundationally, online assignment connects to online bipartite
matching and budgeted allocation (AdWords): the classical RANKING algorithm
achieves the optimal (1 −1/e) competitive ratio in adversarial arrival models for
bipartite matching, while primal–dual methods underlie many extensions [99–102].
Subsequent work generalizes from linear objectives to concave returns and to learning
or estimation within the primal–dual framework, as well as fairness-aware variants
[103–107]. Beyond the classic “offline resources fixed, online jobs arrive” setting, fully
online models allow both sides to arrive and sharpen competitive limits [108]. Par-
allel lines model task heterogeneity and worker qualities in crowdsourcing markets,
formalizing how partial information at arrival influences assignment quality [109].
Taken together, these literatures provide baseline guarantees for irrevocable decisions
under uncertainty, typically in profit/throughput objectives and without per-step
synchronization.
A newer strand studies reusable resource allocation, where an assigned resource is
occupied for some duration and then returns to the pool; this captures ridesharing,
healthcare beds, and inventory with returns [110]. Recent results establish near-
optimal guarantees in adversarial or stochastic arrivals by exploiting fluid or learning
approximations and carefully designed dual controls [111–114]. These models are
closer to our setting (decode workers are reusable) but still differ in two crucial ways:
(i) they optimize cumulative reward (linear) while our objective is the load balancing
(non-linear); and (ii) they typically assume either known or stationary service-time
models, whereas decode workloads are unknown at admission and evolve token-by-
token. Thus, classical online assignment and reusable-resource frameworks inform
admission/routing under uncertainty, but they do not directly address data-parallel
decode with sticky assignments and synchronous barriers that gate progress each step.
The classical online scheduling literature studies how a decision maker allocates
processing time to jobs that arrive sequentially, aiming to optimize latency/through-
put objectives under limited lookahead [115–122]. Two strands are especially relevant
to LLM inference on a single engine. First, batch scheduling groups jobs to amortize
39


setup or memory costs and to raise effective utilization; models and algorithms range
from static batch formation to fully online batching with competitive guarantees [123–
127]. Second, precedence-constrained scheduling captures intra-job dependencies; here
each task must respect a partial order, and objectives include makespan and total
weighted completion time [128–131]. LLM decoding on a single worker fits this mold:
tokens within a request obey a strict chain precedence (each token depends on its
prefix), batching trades latency for throughput, and memory (KV-cache) constraints
couple sequencing and admission. Results from these lines provide principled policies
for admission, batching, and service order when processing times are known or learn-
able, but they operate at the level of one engine and do not account for cross-worker
synchronization.
Multi-processor scheduling generalizes to identical/related/unrelated machines
and mixes preemptive and non-preemptive models [116–118, 120]. Canonical
approaches (e.g., list scheduling, longest-processing-time rules, or migration-based
balancing) assume either known processing times or allow job preemption/migration
to correct early placements; performance is typically analyzed for makespan or flow-
time objectives. These models illuminate when per-machine queues equalize and when
migration is essential, yet they remain misaligned with LLM decode in two ways: (i)
unknown and evolving processing times undermine size-aware or learned dispatching;
(ii) sticky assignments remove the main lever used to fix early mistakes. Classic multi-
worker scheduling therefore informs local ordering and (where allowed) migration, but
it does not address the barrier-synchronized, unknown-size, non-preemptive regime
that governs data-parallel decoding; this gap motivates our universal load-balancing
principle.
A.1.5
Online Algorithms under Adversarial Arrivals
An online algorithm chooses actions sequentially as inputs arrive, without seeing the
future; an adversarial arrival model allows the input sequence to be chosen by an
adversary, often either oblivious (fixes the entire sequence before seeing the algo-
rithm’s random bits) or adaptive (may react to the algorithm’s past random choices).
Performance is assessed by competitive analysis: finding out the largest gap between
the performance of the online algorithm and the hindsight optimal algorithm (which
has full access to all arrivals in advance).
Beyond our online assignment domain, a broad literature studies online algorithms
under adversarial arrivals. In online resource allocation, booking-limit and protection-
level policies [132, 133], together with primal–dual methods [134–137], yield constant
or logarithmic competitive ratios for covering/packing and related network-allocation
problems [138]. In online caching and paging, classic results show least-recently-used
(LRU) is k-competitive, and randomized marking schemes achieve O(log k) competi-
tiveness against an oblivious adversary [139–141]. The k-server problem and metrical
task systems capture stateful movement on metric spaces; the work-function algorithm
attains (2k−1)-competitiveness for k-server on general metrics [142, 143]. In online
convex optimization, regret-minimizing methods (e.g., online gradient and mirror
descent) guarantee O(
√
T) adversarial regret [144]. In online facility location, Meyer-
son’s randomized algorithm is O(log n)-competitive in random-order models, whereas
40


Algorithm 2: FCFS Baseline
Input: Waiting queue Rwait(k) (arrival order), worker concurrency B, active
sets {Ag(k)}g∈[G]
Output: Assignments at step k
Compute remaining slots on each worker: cap[g] ←B −|Ag(k)| for all g ∈[G];
if P
g∈[G] cap[g] = 0 or Rwait(k) = ∅then
return
while Rwait(k) is not None do
Pop the oldest request i from Rwait(k);
Select g∗∈arg maxg cap[g] with maximal free slots;
Assign i to worker g∗: Ag∗(k) ←Ag∗(k) ∪{i};
cap[g∗] ←cap[g∗] −1;
under oblivious adversaries the tight rate is Θ(log n/ log log n) [145, 146]. Our anal-
ysis likewise adopts an adversarial-arrival viewpoint—inputs (arrivals and lengths)
are determined by an oblivious adversary—and establishes competitive optimality for
decode-stage load balancing under synchronous barriers.
Appendix B
Baseline Policy: First-Come-First-Serve
(FCFS)
In this subsection, we formalize the baseline policy used throughout the paper: First-
Come-First-Serve (FCFS). FCFS is widely deployed in real LLM serving systems’
scheduler [45, 46].
At each step k, the scheduler maintains a waiting queue Rwait(k) that stores all
requests that have arrived by time k but have not yet been assigned to any worker.
Under FCFS, the scheduler scans workers in increasing index g = 1, 2, . . . , G and fills
any available slots on each worker up to the batch capacity B. Requests are always
taken from Rwait(k) in strict arrival order (the head of the queue), and no request
that arrived later may be scheduled before an earlier one. The capacity constraint
|Ag(k)| ≤B is always respected, and the loop terminates as soon as either all workers
are full (no remaining capacity) or the waiting queue is empty.
This policy is extremely fast and deterministic (for a fixed arrival sequence), and it
does not rely on any information about the workload profile Wi of individual requests.
Precisely because it ignores workload heterogeneity, FCFS can easily accumulate many
heavy requests on a single worker while others receive only light ones, leading to
large per-step imbalance and, consequently, substantial idle time at the barrier. The
pseudocode used in our analysis and implementation is given in Algorithm 2.
41


Appendix C
Supplementary Materials for Section 5
C.1
Proof of Theorem 1
Proof of Theorem 1: We first reduce the statement to a single admission round, and then
analyze BF-IO and FCFS separately.
Reduction to a single admission round. In the homogeneous-output setting oi = o for all
i, the evolution naturally decomposes into rounds. At the beginning of a round, the cluster is
empty; GB jobs are admitted (up to capacity), with B jobs sent to each worker. All admitted
jobs have the same decode length o, so they generate tokens in lockstep and terminate
simultaneously after o steps. No further jobs are admitted until all GB jobs complete, at
which point the next round begins.
Within a round, the instantaneous imbalance is the same at each of the o steps, because
all workers advance in parallel, and the per-step workloads merely translate in time. Thus,
the time-average imbalance over a long horizon is proportional to the average imbalance per
admission round. Formally, let Imb(r)(π) be the (step-wise) imbalance in round r under a
policy π immediately after the GB jobs of that round have been admitted. As argued above,
this quantity also captures the imbalance at each step within the round, up to the fixed
multiplicative factor o.
Under the prefill-length model in Section 5, and given any arrival instance I that satisfies
the overloaded condition at every step, the multiset of prefill lengths entering each round
consists of GB i.i.d. draws from the distribution of s; different rounds use disjoint sets of
jobs, and so the collections of GB prompts across rounds are independent. In particular, the
random variables {Imb(r)(π)}r≥1 are i.i.d. for each fixed policy π, and their law does not
depend on the specific overloaded arrival instance I.
Let AvgImbalance(π; I) denote the time-average imbalance under policy π for arrival
instance I as in Section 3. Over a long horizon, the law of large numbers implies that, for
any π and any I ∈I,
lim
K→∞E

AvgImbalance(π; I)

= E

Imb(1)(π)

,
because each round contributes o identical steps, and the rounds form an i.i.d. sequence.
Consequently, the imbalance improvement ratio IIR simplifies to the ratio of one-round
expectations:
IIR = inf
I∈I
limK→∞E[AvgImbalance(FCFS; I)]
limK→∞E[AvgImbalance(BF-IO; I)] =
E

Imb(1)(FCFS)

E

Imb(1)(BF-IO)
.
Thus, it suﬀices to lower bound the ratio
E[Imb(FCFS)]
E[Imb(BF-IO)]
in a single admission round, where we write Imb(π) for Imb(1)(π) for brevity.
Step A: BF-IO is smax-balanced. Fix a realization of the GB prefill lengths in the cur-
rent round. Since prefill lengths are independent across jobs and arrival times do not carry
additional information about their values, we may regard these as an unordered multiset of
GB numbers in [0, smax]. Any assignment that fills all devices in the round corresponds to
a partition of this multiset into G disjoint groups S1, . . . , SG, each of cardinality |Sg| = B.
Define the per-device loads
Lg :=
X
s∈Sg
s,
M :=
max
1≤g≤G Lg,
m :=
min
1≤g≤G Lg.
42


Because all jobs have identical decode length o, the per-step imbalance within the round is
constant and proportional to the deviation of the Lg from M. In particular, minimizing the
sum of future imbalances over the round is equivalent to minimizing the maximum load M
across devices in that round. Let {S⋆g}G
g=1 be any assignment that minimizes M.
Lemma 1. For any optimal assignment {S⋆g} minimizing M, we have
M −m ≤smax.
Proof of Lemma 1 Suppose for contradiction that M −m > smax. Let p ∈arg maxg Lg and
q ∈arg ming Lg, so Lp = M and Lq = m. Because Lp −Lq > M −m > smax and every
element of S⋆p ∪S⋆q lies in [0, smax], there must exist x ∈S⋆p and y ∈S⋆q with x > y; otherwise
we would have max S⋆p ≤min S⋆q , which together with |S⋆p| = |S⋆q | = B would imply
Lp ≤B max
s∈S⋆
p
s ≤B min
s∈S⋆
q
s ≤Lq,
contradicting Lp > Lq.
Construct a new assignment by swapping x and y between devices p and q:
S′
p := (S⋆
p \ {x}) ∪{y},
S′
q := (S⋆
q \ {y}) ∪{x},
and S′r := S⋆r for all r /∈{p, q}. The new loads are
L′
p = Lp −(x −y) < Lp,
L′
q = Lq + (x −y) ≤Lq + smax < Lp,
and L′r = Lr for r /∈{p, q}. Thus every new load L′r is strictly less than the old maximum
Lp = M except possibly those devices r ̸= p that already had Lr = M before the swap. In
particular, if p was the unique maximizer, then
max
g
L′
g < max
g
Lg = M,
which contradicts the optimality of {S⋆g} as a minimizer of M.
If instead there were multiple devices achieving the maximum M before the swap, then
after the swap, device p falls below M, while all other devices r ̸= p maintain their previous
loads L′r ≤M. Hence
max
g
L′
g = M,
but the number of devices attaining this maximum strictly decreases. Consequently, starting
from any optimal assignment that is also lexicographically minimal in the pair
 max
g
Lg, #{g : Lg = M}

,
we cannot have M −m > smax, since the above swap produces another assignment with
either a smaller maximum load or the same maximum load but fewer maximizers. This
contradicts lexicographic minimality. Therefore M −m ≤smax must hold for every optimal
assignment.
□
For any assignment, the round-level imbalance is
Imb =
G
X
g=1
(M −Lg).
For the optimal assignment {S⋆g}, choose p ∈arg maxg Lg and note that Lp = M. Then
Imb =
X
g̸=p
(M −Lg) ≤
X
g̸=p
(M −m) = (G −1)(M −m) ≤(G −1) smax
43


by Lemma 1. Since this bound holds for every realization of the GB prompt lengths in the
round, taking expectations yields
E

Imb(BF-IO)

≤(G −1) smax.
(C1)
Step B: FCFS lower bound. We now derive a lower bound on the expected imbalance
under FCFS in one round. Because of the overloaded assumption and size-agnostic FCFS
admission, the GB jobs admitted in a round can be viewed as an unordered multiset of i.i.d.
prefill lengths with law s, after which FCFS fills the G devices in arrival order. Conditioned
on this multiset, the distribution of the B prompts assigned to each device is that of B
samples drawn uniformly without replacement from the multiset. Since the underlying pool
itself comprises GB i.i.d. draws from s, a standard exchangeability argument implies that
the prompts (sg,j)B
j=1 on each device g can be represented as B i.i.d. draws from s, and that
the collections {(sg,j)B
j=1 : g ∈[G]} are independent across g.
Let
Sg :=
B
X
j=1
sg,j,
µ := E[s],
σ2
s := Var(s) > 0.
Then S1, . . . , SG are i.i.d. with
E[Sg] = µB,
Var(Sg) = σ2
sB.
Define the standardized sums
Zg := Sg −µB
σs
√
B
,
g = 1, . . . , G.
Because s ∈[0, smax], the random variable s has a bounded third absolute moment, and the
Berry–Esseen theorem (for sums of i.i.d. real variables) yields the existence of a universal
constant CBE such that
sup
x∈R
 Pr(Zg ≤x) −Φ(x)
 ≤CBE
√
B
,
(C2)
where Φ is the standard normal distribution function.
Set zG :=
q
log G
2
and consider the threshold zG. For a standard normal Z ∼N(0, 1) and
large G, the Gaussian tail bound implies
Pr(Z ≥zG) ≥
1
√
2π
 1
zG
−1
z3
G

exp

−z2
G
2

≥
1
2
√
2π zG
G−1/4.
Combining this with (C2), we obtain
Pr(Zg ≥zG) ≥
1
2
√
2π zG
G−1/4 −CBE
√
B
.
Since
√
G log G = o(B), there exists B0(G) such that for all B ≥B0(G),
CBE
√
B
≤1
2 ·
1
2
√
2π zG
G−1/4,
and hence
Pr(Zg ≥zG) ≥
1
4
√
2π zG
G−1/4 =: pG.
(C3)
By independence across g, we have
Pr

max
1≤g≤G Zg ≥zG

≥1 −
 1 −pG
G.
44


Using the inequality 1 −x ≤e−x for x ≥0, we find
1 −
 1 −pG
G ≥1 −exp(−GpG).
From (C3), GpG ≥
1
4
√
2π zG G3/4, which is bounded away from zero for every fixed G ≥2.
Thus, there exists a universal constant c1 ∈(0, 1) (for instance c1 := 1 −e−1/(4
√
2π)) such
that
Pr

max
1≤g≤G Zg ≥zG

≥c1
for all log G ≥4 and all B ≥B0(G).
(C4)
We now translate this into a lower bound on E[maxg Sg]. For any t > 0, an elementary
inequality yields
E
h
max
1≤g≤G Sg
i
= µB + E
h
max
1≤g≤G(Sg −µB)
i
≥µB + t Pr

max
g (Sg −µB) ≥t

.
Choose t := σs
√
BzG, so that the event {maxg(Sg−µB) ≥t} coincides with {maxg Zg ≥zG}.
Combining with (C4), we obtain
E
h
max
g
Sg
i
≥µB + σs
√
BzG c1 = µB + c1
√
2
σs
p
B log G.
Since E[PG
g=1 Sg] = GµB, it follows that the expected imbalance in the round under FCFS
satisfies
E

Imb(FCFS)

= G E
h
max
g
Sg
i
−E
h G
X
g=1
Sg
i
≥c2 G σs
p
B log G,
(C5)
for all B ≥B0(G), where c2 := c1/
√
2 > 0 is a universal constant.
Conclusion. Combining the upper bound (C1) for BF-IO with the lower bound (C5) for
FCFS, we find that for all B ≥B0(G),
E[Imb(FCFS)]
E[Imb(BF-IO)] ≥c2 G σs
√B log G
(G −1) smax
=

c2
σs
smax
 p
B log G ·
G
G −1.
By the non-degeneracy assumption in Section 5, the ratio σs/smax is bounded below by
κ0 > 0. Setting c := c2 and recalling that IIR equals the ratio of one-round expectations, we
conclude that
IIR ≥c κ0
p
B log G ·
G
G −1,
which is the claimed lower bound (3). This completes the proof.
□
C.2
Proof of Theorem 2
Proof of Theorem 2: We work on a probability space (Ω, F, P) that supports the arrival
instance I, the prefill lengths Dpreﬁll, the geometric decode lengths Ddecode = Geo(p), and
the completion indicators. For each step k ∈[K], we define the natural filtration
Fk := σ

all arrivals, prefill, decode lengths, completions, and BF-IO assignments up to step k

,
and write E[· | Fk] for conditional expectations.
At the end of step k −1 (after BF-IO admission at that step), each device g ∈{1, . . . , G}
holds exactly B active requests, indexed by i = 1, . . . , B, with sizes ag,i(k−1) ∈N (including
prompt tokens and already generated output tokens). Let
Lg(k −1) :=
B
X
i=1
ag,i(k −1),
D(k −1) := max
g
Lg(k −1) −min
g
Lg(k −1),
45


and
Λ(k) := max
g,i ag,i(k −1),
be, respectively, the device loads, the inter-device gap, and the one-step envelope at the end
of step k −1. These quantities are Fk−1-measurable.
At the beginning of step k, every active request grows by exactly one token, then completes
independently with probability p at the end of the step. Let ξg,i(k) ∼Bernoulli(p) be the
completion indicator of slot (g, i) in step k, independent of Fk−1 and across (g, i). The number
of completions (free slots) on device g in step k is
cg(k) :=
B
X
i=1
ξg,i(k) ∼Binomial(B, p),
independent across g and of Fk−1, and we write Ck := PG
g=1 cg(k) for the total number of
completions.
Before admitting new requests at step k, the total pre-admission load on device g is
apre
g
(k) := Lg(k −1) + B −Rg(k),
Rg(k) :=
B
X
i=1
ξg,i(k)
 ag,i(k −1) + 1

,
where Rg(k) is the load removed by completions (each surviving job contributes +1 token).
The corresponding pre-admission spread is
Apre(k) := max
g
apre
g
(k) −min
g
apre
g
(k).
After BF-IO admission at step k, we write Lg(k) for the post-admission loads and
D(k) := max
g
Lg(k) −min
g
Lg(k)
for the post-admission gap. Finally, we set
Λ(k + 1) := max
g,i ag,i(k),
the one-step envelope at the end of step k. By construction, Lg(k), D(k), and Λ(k + 1) are
Fk-measurable.
Part 1 (Upper Bound of Imbalance of BF-IO)
Step 1.A: Separation and one-step drift
The first lemma is a geometric statement about the BF-IO assignment at step k.
Lemma 2 (Separation and gap reduction). Fix δ ∈
 0, (2smax −1)−1
and set
γ := 1 −δ(2smax −1) ∈(0, 1).
Then, for every k ≥1,
P

D(k) ≤max

smax, Apre(k) −γBp
	  Fk−1

≥1 −2G exp

−δ2
3 Bp

.
(C6)
Proof of Lemma 2: Fix a step k and condition on Fk−1. Given Fk−1, the collection of sizes
{ag,i(k−1)} and loads {Lg(k−1)} is deterministic, and the randomness at step k comes only
from the completion indicators {ξg,i(k)} and the prompt lengths of newly admitted requests.
Let
Ecnt(k) :=

|cg(k) −Bp| ≤δBp for all g ∈{1, . . . , G}
	
.
46


The event Ecnt(k) depends only on {ξg,i(k)}, and thus is independent of Fk−1. By the
Chernoff bound for binomial variables,
P
 Ecnt(k)c | Fk−1

= P
 Ecnt(k)c
≤2G exp

−δ2
3 Bp

.
On the event Ecnt(k), we have
cmin(k) := min
g
cg(k) ≥(1 −δ)Bp,
cmax(k) := max
g
cg(k) ≤(1 + δ)Bp.
Fix an outcome ω ∈Ecnt(k) and consider deterministically the BF-IO assignment at step k
on that outcome. Let Sg(k) denote the multiset of prompt sizes of the new requests admitted
to device g at step k; by construction |Sg(k)| = cg(k), and
Lg(k) = apre
g
(k) +
X
s∈Sg(k)
s.
We first establish a separation property for the minimizer of D(k). For each feasible
family of multisets {Sg(k)}G
g=1 with |Sg(k)| = cg(k) and using only jobs from the waiting
pool, define
Lg(k) := apre
g
(k) +
X
s∈Sg(k)
s,
D(k) := max
g
Lg(k) −min
g
Lg(k).
Among all such assignments, BF-IO chooses one that minimizes D(k). Let {S⋆g(k)} be such
a minimizer, and let
L⋆
g(k) := apre
g
(k) +
X
s∈S⋆
g (k)
s,
D⋆(k) := max
g
L⋆
g(k) −min
g
L⋆
g(k),
denote the corresponding loads and gap. If D⋆(k) ≤smax, then D(k) = D⋆(k) ≤smax and
the claimed inequality (C6) trivially holds on this outcome. Hence it suﬀices to analyze the
case D⋆(k) > smax.
Let p ∈arg maxg L⋆g(k) and q ∈arg ming L⋆g(k), so that
L⋆
p(k) −L⋆
q(k) = D⋆(k) > smax.
We first show that, under this strict gap and the optimality of {S⋆g(k)}, the assignment must
be “separated” between any such heavy–light pair (p, q). That is, for the sets S⋆p(k) and S⋆q (k)
there exists an integer θ ∈{0, . . . , smax −1} such that
s ≤θ
for all s ∈S⋆
p(k),
s ≥θ + 1
for all s ∈S⋆
q (k).
We prove by contradiction. Suppose not, then there exist x ∈S⋆p(k) and y ∈S⋆q (k) with
x > y. Consider the assignment {eSg(k)} obtained by swapping x and y between devices p
and q,
eSp(k) := (S⋆
p(k) \ {x}) ∪{y},
eSq(k) := (S⋆
q (k) \ {y}) ∪{x},
and eSr(k) := S⋆r (k) for all r /∈{p, q}. This swap preserves |Sg(k)| = cg(k) for every g and uses
only jobs from the waiting pool (by the overloaded definition 1, the pool contains suﬀiciently
many jobs of each size to support such exchanges). The new loads are
eLp(k) = apre
p
(k) +
X
s∈eSp(k)
s = L⋆
p(k) −(x −y) < L⋆
p(k),
and
eLq(k) = apre
q
(k) +
X
s∈eSq(k)
s = L⋆
q(k) + (x −y) ≤L⋆
q(k) + smax < L⋆
p(k),
47


using x −y ≤smax and L⋆p(k) −L⋆q(k) > smax. All other loads eLr(k) equal L⋆r(k) ≤L⋆p(k).
If p is the unique maximizer of {L⋆g(k)}, then
max
g
eLg(k) < max
g
L⋆
g(k),
contradicting the minimality of D⋆(k). If there are multiple maximizers, then the swap strictly
reduces the number of devices attaining the maximum load while keeping the maximum value
at most L⋆p(k). Repeating this argument finitely many times, we either strictly reduce the
maximum load or obtain a contradiction to the choice of {S⋆g(k)} as a minimizer with minimal
number of maximizers. Hence no such pair (x, y) can exist, and the desired threshold θ exists.
Condition on Ecnt(k) and fix an outcome with D⋆(k) > smax. Using the separation
property, for device p every s ∈S⋆p(k) satisfies s ≤θ, while for q every s ∈S⋆q (k) satisfies
s ≥θ + 1. Therefore, X
s∈S⋆
p(k)
s ≤θ cp(k),
X
s∈S⋆
q (k)
s ≥(θ + 1) cq(k),
and hence
D⋆(k) = L⋆
p(k) −L⋆
q(k)
=

apre
p
(k) +
X
s∈S⋆
p(k)
s

−

apre
q
(k) +
X
s∈S⋆
q (k)
s

≤apre
p
(k) −apre
q
(k) + θcp(k) −(θ + 1)cq(k).
Using cp(k) ≤cmax(k) and cq(k) ≥cmin(k), we obtain
θcp(k) −(θ + 1)cq(k) ≤θcmax(k) −(θ + 1)cmin(k) ≤θ(1 + δ)Bp −(θ + 1)(1 −δ)Bp
= Bp
 θ(1 + δ) −(θ + 1)(1 −δ)

= Bp
 −1 + δ(2θ + 1)

≤Bp
 −1 + δ(2smax −1)

= −γBp,
where we used θ ≤smax −1 and the definition of γ. Consequently,
D⋆(k) ≤apre
p
(k) −apre
q
(k) −γBp ≤Apre(k) −γBp.
Combining this with the trivial bound D⋆(k) ≤smax in the case D⋆(k) ≤smax, and recalling
that D(k) = D⋆(k) under BF-IO, we obtain, for every outcome in Ecnt(k),
D(k) ≤max

smax, Apre(k) −γBp
	
.
Since Ecnt(k) is independent of Fk−1, taking conditional probability given Fk−1 yields
P

D(k) ≤max

smax, Apre(k)−γBp
	  Fk−1

≥P
 Ecnt(k)
 Fk−1

≥1−2G exp

−δ2
3 Bp

,
as claimed.
□
Step 1.B: Concentration of pre-admission spread
We now control Apre(k) around its conditional baseline (1 −p)D(k −1).
Define the range functional r(x) := maxg xg −ming xg for x ∈RG. For step k, recall that
Rg(k) =
B
X
i=1
ξg,i(k)
 ag,i(k−1)+1

,
µg(k) := E

Rg(k)
 Fk−1

= p
B
X
i=1
(ag,i(k−1)+1),
and set
Zg(k) := Rg(k) −µg(k),
Z(k) := (Zg(k))G
g=1.
The one-step envelope
Λ(k) := max
g,i ag,i(k −1)
is Fk−1-measurable and bounds every summand in Rg(k).
48


Lemma 3 (One-step concentration of Apre(k)). For each step k ≥1 and every Fk−1-
measurable Λ(k), define
αB := 3 log(GB),
uA(k) := 8 Λ(k) max
np
p(1 −p) B αB, αB
o
.
Then, for all k ≥1,
P

Apre(k) −(1 −p)D(k −1) > uA(k)
 Fk−1

≤2G e−αB ≤2 G−2B−3.
(C7)
Proof of Lemma 3: Given Fk−1, the vector of loads L(k −1)
=
(Lg(k −1))G
g=1 is
deterministic, and
apre
g
(k) = Lg(k −1) + B −Rg(k) = (1 −p)
 Lg(k −1) + B

−Zg(k).
Let Xg := (1 −p)(Lg(k −1) + B) and X := (Xg)G
g=1. Then
Apre(k) = r
 apre(k)

= r(X −Z(k)).
For any vectors x, z ∈RG, comparing coordinates achieving the maximum and minimum of
x −z gives the elementary inequality
r(x −z) ≤r(x) + 2∥z∥∞,
where ∥z∥∞:= maxg |zg|. Applying this with x = X and z = Z(k), and using r(X) =
(1 −p)D(k −1), we obtain
Apre(k) −(1 −p)D(k −1) = r(X −Z(k)) −r(X) ≤2∥Z(k)∥∞= 2 max
g
|Zg(k)|.
Thus, for any u > 0,
P

Apre(k) −(1 −p)D(k −1) > u
 Fk−1

≤
G
X
g=1
P

|Zg(k)| > u/2
 Fk−1

.
It remains to control each tail P(|Zg(k)| > u/2 | Fk−1). Conditional on Fk−1, the vari-
ables {(ξg,i(k)−p)(ag,i(k −1)+1)}B
i=1 are independent, mean-zero, and bounded in absolute
value by Λ(k) + 1 ≤2Λ(k). Their conditional variance satisfies
Var
 B
X
i=1
(ξg,i(k)−p)(ag,i(k−1)+1)
 Fk−1

=
B
X
i=1
p(1−p) (ag,i(k−1)+1)2 ≤p(1−p) B 4Λ(k)2.
By Bernstein’s inequality for bounded summands, for every g and u > 0,
P

|Zg(k)| > u/2
 Fk−1

≤2 exp

−min

(u/2)2
16 p(1 −p) B Λ(k)2 , 3 (u/2)
8 Λ(k)

.
Summing over g = 1, . . . , G and recalling the earlier inequality, we obtain
P

Apre(k)−(1−p)D(k−1) > u
 Fk−1

≤2G exp

−min

(u/2)2
16 p(1 −p) B Λ(k)2 , 3 (u/2)
8 Λ(k)

.
Choosing
u = uA(k) = 8 Λ(k) max
np
p(1 −p) B αB, αB
o
,
forces
(u/2)2
16 p(1 −p) B Λ(k)2
≥αB,
3 (u/2)
8Λ(k)
≥αB,
so that the minimum in the exponent is at least 2αB. This yields
P

Apre(k) −(1 −p)D(k −1) > uA(k)
 Fk−1

≤2Ge−αB.
With αB = 3 log(GB) we have 2Ge−αB ≤2G−2B−3, completing the proof.
□
49


Step 1.C: Average gap bound for BF-IO
We now combine Lemmas 2 and 3 to obtain a uniform bound on the long-run average of
D(k). Recall that at each step, the instantaneous imbalance under any policy satisfies
Imbalance(k; BF-IO) =
G
X
g=1
 max
h
Lh(k)−Lg(k)

≤(G−1)

max
h
Lh(k)−min
h Lh(k)

= (G−1) D(k).
Lemma 4 (Average gap under geometric outputs for BF-IO). Let δ := 1/(4smax), set γ :=
1 −δ(2smax −1) ∈(0, 1), and let αB := 3 log(GB). Define
x⋆(B, G) :=
l3
p log(GB)
m
,
UB := 8 (smax + x⋆(B, G)) max
np
p(1 −p) B αB, αB
o
,
and
εB := 2G e−(δ2/3)Bp + 2 G−2B−3.
Take B is large enough (depending on (p, smax, G)) so that
γBp ≥2UB.
(C8)
Then, under the overloaded-arrival regime, the BF-IO policy with H = 0 satisfies
lim sup
K→∞
1
K
K−1
X
k=0
E[D(k)] ≤smax
p
+ oB,G(1),
(C9)
where oB,G(1) ≥0 depends only on (B, G, p, smax) and oB,G(1) →0 whenever B, G →∞
with
√
G = o(B).
Proof of Lemma 4: Fix B, G, and simplify the notation x⋆:= x⋆(B, G). For each step k
define the events
Ecnt(k) :=

|cg(k) −Bp| ≤δBp ∀g
	
,
Esnap(k) := {Λ(k) ≤smax + x⋆},
and
Edev(k) :=

Apre(k) −(1 −p)D(k −1) ≤UB
	
.
By Lemma 2,
P
 Ecnt(k)c | Fk−1

≤2G exp

−δ2
3 Bp

,
and by Lemma 3 (applied with x⋆and using Λ(k) ≤smax + x⋆on Esnap(k)),
P
 Edev(k)c ∩Esnap(k) | Fk−1

≤2 G−2B−3.
Abbreviate
εB := 2G exp

−δ2
3 Bp

+ 2 G−2B−3.
On the “good” event
Egood(k) := Ecnt(k) ∩Esnap(k) ∩Edev(k),
we combine Lemma 2 and the bound on Apre(k) to obtain
D(k) ≤max
n
smax, (1 −p)D(k −1) + UB −γBp
o
.
As γBp ≥2UB, we have
(1 −p)D(k −1) + UB −γBp ≤(1 −p)D(k −1) −UB ≤(1 −p)D(k −1),
and hence
D(k) ≤smax + (1 −p)D(k −1)
on Egood(k).
(C10)
50


On the complement Egood(k)c, we use a uniform bound. At the beginning of step k,
before completions, every alive request grows by one token and new arrivals have size at most
smax, so
Λ(k + 1) ≤max{Λ(k) + 1, smax} ≤smax +
 Λ(k) + 1 −smax

+.
Therefore,
B Λ(k + 1) ≤B (smax + x⋆) + B
 Λ(k) + 1 −smax −x⋆

+,
and since each device holds B requests after admission, we have
D(k) ≤B Λ(k + 1)
on Egood(k)c.
Combining the two cases and using the union bound 1Egood(k)c ≤1Ecnt(k)c +1Esnap(k)c +
1Edev(k)c∩Esnap(k), we obtain the pathwise decomposition
D(k) ≤
 smax + (1 −p)D(k −1)

1Egood(k)
+ B (smax + x⋆)
 1Ecnt(k)c + 1Edev(k)c∩Esnap(k) + 1Esnap(k)c

+ B
 Λ(k) + 1 −smax −x⋆

+.
Taking conditional expectations given Fk−1 and using the bounds on event probabilities,
we have
E

D(k) | Fk−1

≤
 smax + (1 −p)D(k −1)

+ εBB (smax + x⋆) + B (smax + x⋆) P
 Esnap(k)c | Fk−1

+ B E

(Λ(k) + 1 −smax −x⋆)+ | Fk−1

.
(C11)
We use the simple bound, 1{Λ(k)>smax+x⋆} ≤(Λ(k) + 1 −smax −x⋆)+, and hence
P
 Esnap(k)c | Fk−1

≤E

(Λ(k) + 1 −smax −x⋆)+ | Fk−1

.
Substituting into (C11) and taking unconditional expectations yields the one-step inequality
E[D(k)] ≤

smax + (1 −p)E[D(k −1)]

+ εBB (smax + x⋆)
+ B (smax + x⋆+ 1) E

(Λ(k) + 1 −smax −x⋆)+

.
(C12)
To complete the proof, we bound the horizon average of the envelope term. In any step
k, the size of an alive request equals the sum of its prompt length (at most smax) and the
number of tokens it has generated up to step k (its age). Thus Λ(k) −smax is bounded by
the maximal age among alive requests at step k. Summing over the horizon and exchanging
the order of summation (first over requests, then over the steps they are alive), we obtain
K−1
X
k=0
 Λ(k) + 1 −smax −x⋆

+ ≤
X
j
oj−1
X
r=0
(r + 1 −x⋆)+,
where the outer sum is over all requests j whose lifetime intersects [0:K −1], and oj is the
(geometric) output length of request j. For a single geometric random variable oj ∼Geo(p),
E
h oj−1
X
r=0
(r + 1 −x⋆)+
i
=
∞
X
r=x⋆+1
(r −x⋆)P(oj ≥r) =
∞
X
r=x⋆+1
(r −x⋆)(1 −p)r−1 = (1 −p)x⋆
p2
.
Let NK be the number of distinct requests whose lifetime intersects the horizon [0:K −1].
At each step there are exactly GB alive items, and each item counted in NK is alive for at
least one step, so deterministically NK ≤GBK and hence NK/K ≤GB. By linearity of
expectation,
1
K
K−1
X
k=0
E

(Λ(k) + 1 −smax −x⋆)+

≤E

NK

K
· (1 −p)x⋆
p2
≤GB (1 −p)x⋆
p2
.
51


Averaging (C12) over k = 0, . . . , K −1 and rearranging, we obtain
1
K
K−1
X
k=0
E[D(k)] ≤

smax + (1 −p) 1
K
K−1
X
k=0
E[D(k −1)]

+ εBB (smax + x⋆) + GB2
p2
(smax + x⋆+ 1) (1 −p)x⋆.
(C13)
The sequence {E[D(k)]}k≥0 is nonnegative, so its Cesàro average is bounded, and the right-
hand side of (C13) is an aﬀine contraction in the horizon average. Letting K →∞and using
(1 −p) < 1, we obtain
lim sup
K→∞
1
K
K−1
X
k=0
E[D(k)] ≤
smax + εBB (smax + x⋆) + GB2
p2
(smax + x⋆+ 1) (1 −p)x⋆
p
.
Finally, for x⋆(B, G) = ⌈3 log(GB)/p⌉, the term (1 −p)x⋆decays fast in GB; more
precisely,
(1 −p)x⋆≤exp(−px⋆) ≤(GB)−3,
so
GB2
p2
(smax + x⋆+ 1) (1 −p)x⋆= oB,G(1)
whenever B, G →∞. Similarly, by definition of εB and the assumption log G = o(B),
εBB (smax + x⋆) = oB,G(1).
Absorbing these vanishing terms into oB,G(1), we obtain (C9). This completes the proof of
Part 1.
□
Part 2 (Lower Bound of Imbalance of FCFS)
We now analyze the lower bound of average imbalance of the FCFS baseline. Under the
overloaded arrival definition, FCFS is size-agnostic: whenever a completion occurs in any slot,
the slot is immediately refilled by the earliest pending request, whose prompt length is i.i.d.
distributed by the distribution S = Dprefill with E[S] = µs and Var(S) = σ2s > 0.
Step 2.A: Slot-level recursion and Markov structure.
Index slots by (g, b) with g ∈{1, . . . , G} and b ∈{1, . . . , B}. Let Yg,b(k) denote the post-
admission size in slot (g, b) at the end of step k. Under FCFS, we have the slot-level recursion
Yg,b(k) = (1 −ξg,b(k))
 Yg,b(k −1) + 1

+ ξg,b(k) Sg,b(k),
k ≥1,
(C14)
where, for each (g, b, k),
ξg,b(k) ∼Bernoulli(p),
Sg,b(k) i.i.d.
∼S = Dprefill,
and all these random variables are mutually independent across (g, b, k) and independent of
the initial configuration {Yg,b(0)}. Thus, for each fixed slot (g, b), the process (Yg,b(k))k≥0
is a time-homogeneous Markov chain on N, and the chains corresponding to different slots
evolve independently.
For each device g, the post-admission load at step k is
Sg(k) :=
B
X
b=1
Yg,b(k),
52


and the instantaneous FCFS imbalance at step k is
Imbalance(k; FCFS) := G · max
1≤g≤G Sg(k) −
G
X
g=1
Sg(k).
By symmetry across devices, for any k and any initial configuration,
E

Imbalance(k; FCFS)

= G

E

max
g
Sg(k)

−E

S1(k)

.
Step 2.B: Single-slot stationary law.
Fix a slot (g, b) and suppress the indices. The chain (Yk)k≥0 defined by (C14) satisfies, for
every measurable A ⊆N and y ∈N,
P(Y1 ∈A | Y0 = y) = P
 (1 −ξ1)(y + 1) + ξ1S1 ∈A

≥P(ξ1 = 1, S1 ∈A) = p P(S ∈A).
Thus the transition kernel admits a Doeblin minorization with parameter p > 0. In particular,
(Yk)k≥0 is uniformly ergodic and admits a unique stationary distribution, which we denote
by U. To identify U explicitly, it is convenient to track the age process:
Ak := number of steps elapsed since the current job in the slot started,
k ≥0.
Each time a completion occurs (ξk = 1), a fresh job with prompt Sk ∼S starts in that slot
and the age resets to zero; otherwise, the job survives and the age increases by one. The age
process alone evolves via
Ak+1 =
(
0,
with probability p,
Ak + 1,
with probability 1 −p.
This is a Markov chain on N with transition kernel
P(Ak+1 = 0 | Ak = a) = p,
P(Ak+1 = a + 1 | Ak = a) = 1 −p.
A direct calculation shows that the geometric distribution
A ∼Geo(p) −1,
P(A = a) = p(1 −p)a,
a = 0, 1, . . . ,
is invariant for the age chain:
P(Ak+1 = a) = p P(Ak ≥a) = p
∞
X
j=a
p(1 −p)j = p(1 −p)a = P(A = a).
Since the prompts {Sk} are i.i.d. and independent of the completions {ξk}, in stationarity
the prompt of the current job Scur and the age A are independent, with Scur
d= S and
A ∼Geo(p) −1. At a stationary snapshot, the slot size U has the representation
U
d= Scur + A,
Scur
d= S,
A ∼Geo(p) −1,
Scur ⊥⊥A.
Consequently,
µU := E[U] = µs + 1 −p
p
,
σ2
snap := Var(U) = σ2
s + 1 −p
p2
.
(C15)
Because S is bounded and A has geometric tails, U has finite moments of all orders, in
particular E[|U|3] < ∞.
53


Step 2.C: Device-level loads at stationarity.
Since the slot chains are independent and identically distributed, and their transition kernels
do not couple different slots, the stationary distribution of the entire collection {Yg,b(k)}g,b
is the product measure under which all Yg,b are i.i.d. with law U. Let (Ug,b)g,b be i.i.d. copies
of U and define the stationary per-device loads
Sst
g :=
B
X
b=1
Ug,b,
g = 1, . . . , G.
Then {Sst
g }G
g=1 are i.i.d. with
E[Sst
g ] = µUB,
Var(Sst
g ) = σ2
snapB.
We now invoke the same Berry–Esseen and extreme-value argument as in the
homogeneous-output case, but with U replacing s. Define the standardized sums
Wg := Sst
g −µUB
σsnap
√
B
,
g = 1, . . . , G.
Because U has finite third absolute moment, the Berry–Esseen theorem (for sums of i.i.d.
real variables) guarantees the existence of a universal constant CBE such that
sup
x∈R
P(Wg ≤x) −Φ(x)
 ≤CBE
√
B
,
for each g, where Φ is the standard normal distribution function.
Set zG :=
q
log G
2
and consider the threshold zG. For a standard normal Z ∼N(0, 1) and
all suﬀiciently large G (say log G ≥4), the Gaussian tail bound implies
P(Z ≥zG) ≥
1
√
2π
 1
zG
−1
z3
G

exp

−z2
G
2

≥
1
2
√
2π zG
G−1/4.
Combining this with Berry–Esseen, we obtain
P
 Wg ≥zG

≥
1
2
√
2π zG
G−1/4 −CBE
√
B
.
Since
√
G = ˜o(B), there exists B0(G) such that, for all B ≥B0(G),
CBE
√
B
≤1
2 ·
1
2
√
2π zG
G−1/4,
and hence
P
 Wg ≥zG

≥
1
4
√
2π zG
G−1/4 =: pG.
(C16)
By independence across g, we have
P

max
1≤g≤G Wg ≥zG

≥1 −
 1 −pG
G.
Using the inequality 1 −x ≤e−x for x ≥0, we find
1 −
 1 −pG
G ≥1 −exp(−GpG).
From (C16), GpG ≥
1
4
√
2π zG G3/4, which is bounded away from zero for every fixed G ≥2.
Thus, there exists a universal constant c1 ∈(0, 1) (for instance c1 := 1 −e−1/(4
√
2π)) such
that
P

max
1≤g≤G Wg ≥zG

≥c1
for all log G ≥4 and all B ≥B0(G).
54


Finally, for any t > 0,
E
h
max
1≤g≤G Sst
g
i
= µUB + E
h
max
1≤g≤G(Sst
g −µUB)
i
≥µUB + t P
 max
g (Sst
g −µUB) ≥t

.
Choosing t := σsnap
√
B zG, so that the event {maxg(Sst
g −µUB) ≥t} coincides with
{maxg Wg ≥zG}, we obtain
E
h
max
1≤g≤G Sst
g
i
≥µUB + σsnap
√
B zG c1 = µUB + c′ σsnap
p
B log G,
for some absolute constant c′ > 0. Since E[PG
g=1 Sst
g ] = GµUB, the stationary expected
imbalance under FCFS satisfies
E

Imbalancest
:= E
h
G max
g
Sst
g −
G
X
g=1
Sst
g
i
≥c′ G σsnap
p
B log G,
(C17)
for all G ≥2 and all B ≥B0(G), where B0(G) is as above.
Step 2.D: Time-average imbalance under FCFS.
The global collection of slot loads
Y (k) :=
 Yg,b(k)

g∈[G], b∈[B]
is a Markov chain on NGB with transition kernel induced by (C14). By the Doeblin minoriza-
tion on each coordinate, the product chain is uniformly ergodic and admits the unique
stationary distribution Π, which is precisely the product law of i.i.d. copies of U. Moreover,
by uniform ergodicity, for any initial configuration Y (0) and any integrable function f,
lim
k→∞E

f(Y (k))

= EΠ[f(Y )].
Applying this to f(Y ) = Imbalance(k; FCFS), which has finite second moment under Π in
view of (C15), we deduce that
lim
k→∞E

Imbalance(k; FCFS)

= E

Imbalancest
.
By Cesàro averaging, it follows that the long-run expected time-average imbalance under
FCFS,
AvgImbalance(FCFS; I) :=
lim
K→∞
1
K
K−1
X
k=0
E

Imbalance(k; FCFS)

,
exists and equals the stationary expectation E

Imbalancest
, independently of the arrival
instance I and the initial configuration. Combining with (C17), we obtain
AvgImbalance(FCFS; I) ≥c′ G σsnap
p
B log G,
σ2
snap = σ2
s + 1 −p
p2
,
(C18)
for all G ≥2 and all B suﬀiciently large, uniformly over all overloaded arrival instances I.
Part 3: BF-IO vs. FCFS
We now combine the BF-IO average-gap bound from Lemma 4 with the FCFS lower
bound (C18) to obtain the imbalance–reduction rate in the geometric-output setting. By
Lemma 4, for any overloaded arrival instance I we have
lim sup
K→∞
1
K
K−1
X
k=0
E[D(k)] ≤smax
p
+ oB,G(1),
with oB,G(1) →0 as B, G →∞.
55


Since, for every step k and any policy,
Imbalance(k; BF-IO) =
G
X
g=1
 max
h
Lh(k) −Lg(k)

≤(G −1) D(k),
it follows that
AvgImbalance(BF-IO; I) = lim sup
K→∞
1
K
K−1
X
k=0
E

Imbalance(k; BF-IO)

≤(G−1) smax
p
+ oB,G(1),
with the same oB,G(1).
On the other hand, by (C18), for every overloaded arrival instance I,
AvgImbalance(FCFS; I) ≥c′ G σsnap
p
B log G,
σ2
snap = σ2
s + 1 −p
p2
,
for some universal constant c′ > 0 and all B suﬀiciently large. Therefore, for every such I,
AvgImbalance(FCFS; I)
AvgImbalance(BF-IO; I) ≳G σsnap
√B log G
(G −1) smax/p
= c p σsnap
smax
·
G
G −1
p
B log G,
for a possibly smaller universal constant c > 0, where we absorb the oB,G(1) term into c for
B large enough. Since σsnap ≥√1 −p/p, the prefactor p σsnap
smax
is strictly positive (up to the
fixed scale smax), and the scaling √B log G is inherited from the FCFS lower bound. Taking
the infimum over all overloaded arrival instances I completes the proof.
□
C.3
Proof of Theorem 3
Proof of Theorem 3 We work on a probability space (Ω, F, P) that supports the arrival
instance I, the prefill lengths Dpreﬁll, the geometric decode lengths Ddecode = Geo(p), and
the completion indicators. For each step k ∈N we define the natural filtration
Fk := σ

all arrivals, prefill, decode lengths, completions, and BF-IO assignments up to step k

,
and write E[· | Fk] for conditional expectations.
At the end of step k −1 (after BF-IO admission at that step), each device g ∈{1, . . . , G}
holds exactly B active requests, indexed by i = 1, . . . , B, with sizes ag,i(k−1) ∈N (including
prompt tokens and already generated output tokens). Let
Lg(k −1) :=
B
X
i=1
ag,i(k −1),
D(k −1) := max
g
Lg(k −1) −min
g
Lg(k −1),
and
Λ(k) := max
g,i ag,i(k −1),
be, respectively, the device loads, the inter-device gap, and the one-step envelope at the end
of step k −1. These quantities are Fk−1-measurable.
At the beginning of step k, every active request grows by exactly δk units of workload
and then completes independently with probability p at the end of the step. Let ξg,i(k) ∼
Bernoulli(p) be the completion indicator of slot (g, i) in step k, independent of Fk−1 and
across (g, i). The number of completions (free slots) on device g in step k is
cg(k) :=
B
X
i=1
ξg,i(k) ∼Binomial(B, p),
56


independent across g and of Fk−1, and we write Ck := PG
g=1 cg(k) for the total number of
completions.
Before admitting new requests at step k, the total pre-admission load on device g is
apre
g
(k) := Lg(k −1) + Bδk −Rg(k),
Rg(k) :=
B
X
i=1
ξg,i(k)
 ag,i(k −1) + δk

,
where Rg(k) is the load removed by completions in step k. The corresponding pre-admission
spread is
Apre(k) := max
g
apre
g
(k) −min
g
apre
g
(k).
After BF-IO admission at step k, we write Lg(k) for the post-admission loads and
D(k) := max
g
Lg(k) −min
g
Lg(k)
for the post-admission gap. Finally, we set
Λ(k + 1) := max
g,i ag,i(k),
the one-step envelope at the end of step k. By construction, Lg(k), D(k), and Λ(k + 1) are
Fk-measurable.
Part 1 (Upper Bound of Imbalance of BF-IO)
Step 1.A: Separation and one-step drift
The first lemma is a geometric statement about the BF-IO assignment at step k. Note that
its proof does not depend on the specific value of δk, only on prompt sizes and completion
counts.
Lemma 5 (Separation and gap reduction). Fix δ ∈
 0, (2smax −1)−1
and set
γ := 1 −δ(2smax −1) ∈(0, 1).
Then, for every k ≥1,
P

D(k) ≤max

smax, Apre(k) −γBp
	  Fk−1

≥1 −2G exp

−δ2
3 Bp

.
(C19)
Proof of Lemma 5 Fix a step k and condition on Fk−1. Given Fk−1, the collection of sizes
{ag,i(k−1)} and loads {Lg(k−1)} is deterministic, and the randomness at step k comes only
from the completion indicators {ξg,i(k)} and the prompt lengths of newly admitted requests.
Define
Ecnt(k) :=

|cg(k) −Bp| ≤δBp for all g ∈{1, . . . , G}
	
.
The event Ecnt(k) depends only on {ξg,i(k)} and thus is independent of Fk−1. By a standard
Chernoff bound for binomial variables,
P
 Ecnt(k)c | Fk−1

= P
 Ecnt(k)c
≤2G exp

−δ2
3 Bp

.
On the event Ecnt(k), we have
cmin(k) := min
g
cg(k) ≥(1 −δ)Bp,
cmax(k) := max
g
cg(k) ≤(1 + δ)Bp.
57


Fix an outcome ω ∈Ecnt(k) and consider deterministically the BF-IO assignment at step k
on that outcome. Let Sg(k) denote the multiset of prompt sizes of the new requests admitted
to device g at step k; by construction |Sg(k)| = cg(k), and
Lg(k) = apre
g
(k) +
X
s∈Sg(k)
s.
For each feasible family of multisets {Sg(k)}G
g=1 with |Sg(k)| = cg(k), define
Lg(k) := apre
g
(k) +
X
s∈Sg(k)
s,
D(k) := max
g
Lg(k) −min
g
Lg(k).
Among all such assignments, BF-IO chooses one that minimizes D(k). Let {S⋆g(k)} be such
a minimizer, and let
L⋆
g(k) := apre
g
(k) +
X
s∈S⋆
g (k)
s,
D⋆(k) := max
g
L⋆
g(k) −min
g
L⋆
g(k),
denote the corresponding loads and gap. If D⋆(k) ≤smax, then D(k) = D⋆(k) ≤smax and
the claimed inequality (C19) holds trivially on this outcome. Hence it suﬀices to analyze the
case D⋆(k) > smax.
Let p ∈arg maxg L⋆g(k) and q ∈arg ming L⋆g(k), so that
L⋆
p(k) −L⋆
q(k) = D⋆(k) > smax.
We first show that, under this strict gap and the optimality of {S⋆g(k)}, the assignment
must be “separated” between any such heavy–light pair (p, q): there exists an integer θ ∈
{0, . . . , smax −1} such that
s ≤θ
for all s ∈S⋆
p(k),
s ≥θ + 1
for all s ∈S⋆
q (k).
Suppose not. Then there exist x ∈S⋆p(k) and y ∈S⋆q (k) with x > y. Consider the
assignment {eSg(k)} obtained by swapping x and y between devices p and q,
eSp(k) := (S⋆
p(k) \ {x}) ∪{y},
eSq(k) := (S⋆
q (k) \ {y}) ∪{x},
and eSr(k) := S⋆r (k) for all r /∈{p, q}. This swap preserves |Sg(k)| = cg(k) for every g and
uses only jobs from the waiting pool (by the overloaded definition of I, the pool contains
suﬀiciently many jobs of each size to support such exchanges). The new loads are
eLp(k) = apre
p
(k) +
X
s∈eSp(k)
s = L⋆
p(k) −(x −y) < L⋆
p(k),
and
eLq(k) = apre
q
(k) +
X
s∈eSq(k)
s = L⋆
q(k) + (x −y) ≤L⋆
q(k) + smax < L⋆
p(k),
using x −y ≤smax and L⋆p(k) −L⋆q(k) > smax. All other loads eLr(k) equal L⋆r(k) ≤L⋆p(k).
If p is the unique maximizer of {L⋆g(k)}, then
max
g
eLg(k) < max
g
L⋆
g(k),
contradicting the minimality of D⋆(k). If there are multiple maximizers, then the swap strictly
reduces the number of devices attaining the maximum load while keeping the maximum value
at most L⋆p(k). Repeating this argument finitely many times, we either strictly reduce the
maximum load or obtain a contradiction to the choice of {S⋆g(k)} as a minimizer with minimal
number of maximizers. Hence no such pair (x, y) can exist, and the desired threshold θ exists.
58


Condition on Ecnt(k) and fix an outcome with D⋆(k) > smax. Using the separation
property, for device p every s ∈S⋆p(k) satisfies s ≤θ, while for q every s ∈S⋆q (k) satisfies
s ≥θ + 1. Therefore, X
s∈S⋆
p(k)
s ≤θ cp(k),
X
s∈S⋆
q (k)
s ≥(θ + 1) cq(k),
and hence
D⋆(k) = L⋆
p(k) −L⋆
q(k)
=

apre
p
(k) +
X
s∈S⋆
p(k)
s

−

apre
q
(k) +
X
s∈S⋆
q (k)
s

≤apre
p
(k) −apre
q
(k) + θcp(k) −(θ + 1)cq(k).
Using cp(k) ≤cmax(k) and cq(k) ≥cmin(k), we obtain
θcp(k) −(θ + 1)cq(k) ≤θcmax(k) −(θ + 1)cmin(k) ≤θ(1 + δ)Bp −(θ + 1)(1 −δ)Bp
= Bp
 θ(1 + δ) −(θ + 1)(1 −δ)

= Bp
 −1 + δ(2θ + 1)

≤Bp
 −1 + δ(2smax −1)

= −γBp,
where we used θ ≤smax −1 and the definition of γ. Consequently,
D⋆(k) ≤apre
p
(k) −apre
q
(k) −γBp ≤Apre(k) −γBp.
Combining this with the trivial bound D⋆(k) ≤smax in the case D⋆(k) ≤smax, and recalling
that D(k) = D⋆(k) under BF-IO, we obtain, for every outcome in Ecnt(k),
D(k) ≤max

smax, Apre(k) −γBp
	
.
Since Ecnt(k) is independent of Fk−1, taking conditional probability given Fk−1 yields
P

D(k) ≤max

smax, Apre(k)−γBp
	  Fk−1

≥P
 Ecnt(k)
 Fk−1

≥1−2G exp

−δ2
3 Bp

,
which is (C19).
□
Step 1.B: Concentration of pre-admission spread
We now control Apre(k) around its conditional baseline (1 −p)D(k −1), taking into account
the general per-step increments {δk}.
Define the range functional r(x) := maxg xg −ming xg for x ∈RG. For step k, recall that
Rg(k) =
B
X
i=1
ξg,i(k)
 ag,i(k−1)+δk

,
µg(k) := E

Rg(k)
 Fk−1

= p
B
X
i=1
(ag,i(k−1)+δk),
and set
Zg(k) := Rg(k) −µg(k),
Z(k) := (Zg(k))G
g=1.
The one-step envelope
Λ(k) := max
g,i ag,i(k −1)
is Fk−1-measurable. As 0 ≤δk ≤δmax, each summand (ξg,i(k) −p)(ag,i(k −1) + δk) is
bounded in absolute value by Λ(k) + δmax.
Lemma 6 (One-step concentration of Apre(k) under general increments). For each step
k ≥1 and every Fk−1-measurable Λ(k), define
αB := 3 log(GB),
eΛ(k) := Λ(k)+δmax,
uA(k) := C0 eΛ(k) max
np
p(1 −p) B αB, αB
o
,
where C0 > 0 is a universal numerical constant. Then, for all k ≥1,
P

Apre(k) −(1 −p)D(k −1) > uA(k)
 Fk−1

≤2G e−αB ≤2 G−2B−3.
(C20)
59


Proof of Lemma 6 Given Fk−1, the vector of loads L(k−1) = (Lg(k−1))G
g=1 is deterministic,
and
apre
g
(k) = Lg(k −1) + Bδk −Rg(k) = (1 −p)
 Lg(k −1) + Bδk

−Zg(k).
Let Xg := (1 −p)(Lg(k −1) + Bδk) and X := (Xg)G
g=1. Then
Apre(k) = r
 apre(k)

= r(X −Z(k)).
For any vectors x, z ∈RG, comparing coordinates achieving the maximum and minimum of
x −z gives the elementary inequality
r(x −z) ≤r(x) + 2∥z∥∞,
where ∥z∥∞:= maxg |zg|. Applying this with x = X and z = Z(k), and noting that r(X) =
(1−p)D(k−1) (since adding the common offset (1−p)Bδk to all coordinates does not change
the range), we obtain
Apre(k) −(1 −p)D(k −1) = r(X −Z(k)) −r(X) ≤2∥Z(k)∥∞= 2 max
g
|Zg(k)|.
Thus, for any u > 0,
P

Apre(k) −(1 −p)D(k −1) > u
 Fk−1

≤
G
X
g=1
P

|Zg(k)| > u/2
 Fk−1

.
It remains to control each tail P(|Zg(k)| > u/2 | Fk−1). Conditional on Fk−1, the
variables
Xg,i(k) := (ξg,i(k) −p)
 ag,i(k −1) + δk

,
i = 1, . . . , B,
are independent, mean-zero, and bounded in absolute value by eΛ(k) = Λ(k) + δmax. Their
conditional variance satisfies
Var
 B
X
i=1
Xg,i(k)
 Fk−1

=
B
X
i=1
p(1 −p) (ag,i(k −1) + δk)2 ≤p(1 −p) B eΛ(k)2.
By Bernstein’s inequality for bounded summands, there exists a universal constant C1 > 0
such that, for every g and u > 0,
P

|Zg(k)| > u/2
 Fk−1

≤2 exp
 
−C1 min
(
(u/2)2
p(1 −p) B eΛ(k)2 ,
u/2
eΛ(k)
)!
.
Summing over g = 1, . . . , G and recalling the earlier inequality, we obtain
P

Apre(k)−(1−p)D(k−1) > u
 Fk−1

≤2G exp
 
−C1 min
(
(u/2)2
p(1 −p) B eΛ(k)2 ,
u/2
eΛ(k)
)!
.
Choosing
u = uA(k) = C0 eΛ(k) max
np
p(1 −p) B αB, αB
o
,
with C0 suﬀiciently large (depending only on C1), forces
(u/2)2
p(1 −p) B eΛ(k)2
≥2αB,
u
2eΛ(k)
≥2αB,
so that the minimum in the exponent is at least 2αB. This yields
P

Apre(k) −(1 −p)D(k −1) > uA(k)
 Fk−1

≤2Ge−αB.
With αB = 3 log(GB) we have 2Ge−αB ≤2G−2B−3, completing the proof.
□
60


Step 1.C: Average gap bound for BF-IO under general per-step growth
We now combine Lemmas 5 and 6 to obtain a uniform bound on the long-run average of
D(k) when per-step growth is governed by (δk) so that 0 ≤δk ≤δmax. As before, at each
step the instantaneous imbalance under any policy satisfies
Imbalance(k; BF-IO) =
G
X
g=1
 max
h
Lh(k)−Lg(k)

≤(G−1)

max
h
Lh(k)−min
h Lh(k)

= (G−1) D(k).
Lemma 7 (Average gap under geometric outputs with general increments). Let δ :=
1/(4smax), set γ := 1 −δ(2smax −1) ∈(0, 1), and let αB := 3 log(GB). Define
x⋆(B, G) :=
l3
p log(GB)
m
,
UB := C2 (smax+x⋆(B, G)+δmax) max
np
p(1 −p) B αB, αB
o
,
and
εB := 2G e−(δ2/3)Bp + 2 G−2B−3,
for a universal constant C2 > 0. Assume B is large enough (depending on (p, smax, δmax, G))
so that
γBp ≥2UB.
(C21)
Then, the BF-IO policy with H = 0 satisfies
lim sup
K→∞
1
K
K−1
X
k=0
E[D(k)] ≤smax
p
+ oB,G(1),
(C22)
where oB,G(1) ≥0 depends only on (B, G, p, smax, δmax) and oB,G(1) →0 whenever B, G →
∞with
√
G log G = o(B).
Proof Fix B, G, and write x⋆:= x⋆(B, G). For each step k define the events
Ecnt(k) :=

|cg(k) −Bp| ≤δBp ∀g
	
,
Esnap(k) := {Λ(k) ≤smax + x⋆},
and
Edev(k) :=

Apre(k) −(1 −p)D(k −1) ≤UB
	
.
By Lemma 5,
P
 Ecnt(k)c | Fk−1

≤2G exp

−δ2
3 Bp

,
and by Lemma 6 (applied with eΛ(k) = Λ(k)+δmax and using Λ(k) ≤smax+x⋆on Esnap(k)),
P
 Edev(k)c ∩Esnap(k) | Fk−1

≤2 G−2B−3,
provided C2 is chosen large enough so that UB dominates the bound uA(k) from Lemma 6
on Esnap(k). Abbreviate
εB := 2G exp

−δ2
3 Bp

+ 2 G−2B−3.
On the “good” event
Egood(k) := Ecnt(k) ∩Esnap(k) ∩Edev(k),
we combine Lemma 5 and the bound on Apre(k) to obtain
D(k) ≤max
n
smax, (1 −p)D(k −1) + UB −γBp
o
.
Under (C21), γBp ≥2UB, so
(1 −p)D(k −1) + UB −γBp ≤(1 −p)D(k −1) −UB ≤(1 −p)D(k −1),
61


and hence
D(k) ≤smax + (1 −p)D(k −1)
on Egood(k).
(C23)
On the complement Egood(k)c, we use a uniform bound. At the beginning of step k,
before completions, every alive request grows by δk ≤δmax and new arrivals have size at
most smax, so
Λ(k + 1) ≤max{Λ(k) + δk, smax} ≤smax +
 Λ(k) + δmax −smax

+.
Therefore,
B Λ(k + 1) ≤B (smax + x⋆+ δmax) + B
 Λ(k) + δmax −smax −x⋆

+,
and since each device holds B requests after admission, we have
D(k) ≤B Λ(k + 1)
on Egood(k)c.
Combining the two cases and using the union bound 1Egood(k)c ≤1Ecnt(k)c +1Esnap(k)c +
1Edev(k)c∩Esnap(k), we obtain the pathwise decomposition
D(k) ≤
 smax + (1 −p)D(k −1)

1Egood(k)
+ B (smax + x⋆+ δmax)
 1Ecnt(k)c + 1Edev(k)c∩Esnap(k) + 1Esnap(k)c

+ B
 Λ(k) + δmax −smax −x⋆

+.
Taking conditional expectations given Fk−1 and using the bounds on event probabilities,
we have
E

D(k) | Fk−1

≤
 smax + (1 −p)D(k −1)

+ εBB (smax + x⋆+ δmax) + B (smax + x⋆+ δmax) P
 Esnap(k)c | Fk−1

+ B E

(Λ(k) + δmax −smax −x⋆)+ | Fk−1

.
(C24)
We use the simple bound 1{Λ(k)>smax+x⋆} ≤(Λ(k) + δmax −smax −x⋆)+, and hence
P
 Esnap(k)c | Fk−1

≤E

(Λ(k) + δmax −smax −x⋆)+ | Fk−1

.
Substituting into (C24) and taking unconditional expectations yields the one-step inequality
E[D(k)] ≤smax + (1 −p)E[D(k −1)] + εBB (smax + x⋆+ δmax)
+ B (smax + x⋆+ δmax) E

(Λ(k) + δmax −smax −x⋆)+

.
(C25)
To bound the horizon average of the envelope term, observe that in any step k, the size
of an alive request equals the sum of its prompt length (at most smax) and the cumulative
increments Page
r=1 δr over its age. Since 0 ≤δk ≤δmax, we have
size(k) ≤smax + δmax · age(k),
so Λ(k) −smax is bounded by δmax times the maximal age among alive requests at step k.
Summing over the horizon and exchanging the order of summation (first over requests, then
over the steps they are alive), we obtain
K−1
X
k=0
 Λ(k) + δmax −smax −x⋆

+ ≤δmax
X
j
oj−1
X
r=0
(r + 1 −y⋆)+,
where the outer sum is over all requests j whose lifetime intersects [0:K −1], oj ∼Geo(p)
is the decode length of request j, and y⋆:= ⌊x⋆/δmax⌋is an integer threshold on age. For a
single geometric random variable oj ∼Geo(p),
E
h oj−1
X
r=0
(r + 1 −y⋆)+
i
=
∞
X
r=y⋆+1
(r −y⋆)P(oj ≥r) =
∞
X
r=y⋆+1
(r −y⋆)(1 −p)r−1 = (1 −p)y⋆
p2
.
62


Let NK be the number of distinct requests whose lifetime intersects the horizon [0:K −1].
At each step there are exactly GB alive items, and each item counted in NK is alive for at
least one step, so deterministically NK ≤GBK and hence NK/K ≤GB. By linearity of
expectation,
1
K
K−1
X
k=0
E

(Λ(k) + δmax −smax −x⋆)+

≤δmax E[NK]
K
· (1 −p)y⋆
p2
≤GB δmax (1 −p)y⋆
p2
.
Averaging (C25) over k = 0, . . . , K −1 and rearranging, we obtain
1
K
K−1
X
k=0
E[D(k)] ≤smax + (1 −p) 1
K
K−1
X
k=0
E[D(k −1)]
+ εBB (smax + x⋆+ δmax) + GB2
p2
δmax (smax + x⋆+ δmax) (1 −p)y⋆.
(C26)
The sequence {E[D(k)]}k≥0 is nonnegative, so its Cesàro average is bounded, and the right-
hand side of (C26) is an aﬀine contraction in the horizon average. Letting K →∞and using
(1 −p) < 1, we obtain
lim sup
K→∞
1
K
K−1
X
k=0
E[D(k)] ≤
smax + εBB (smax + x⋆+ δmax) + GB2
p2
δmax (smax + x⋆+ δmax) (1 −p)y⋆
p
.
Finally, for x⋆(B, G) = ⌈3 log(GB)/p⌉, we have y⋆≥
3
pδmax log(GB), so
(1 −p)y⋆≤exp(−py⋆) ≤(GB)−3,
and thus
GB2
p2
δmax (smax + x⋆+ δmax) (1 −p)y⋆= oB,G(1)
whenever B, G →∞with
√
G log G = o(B). Similarly, by the definition of εB and the same
growth condition,
εBB (smax + x⋆+ δmax) = oB,G(1).
Absorbing these vanishing terms into oB,G(1) and dividing by p yields (C22). This completes
the proof.
□
Part 2 (Lower bound of Imbalance of FCFS)
We now extend the FCFS lower-bound analysis to the setting with general per-step drift: at
each step k, every alive request gains a common increment δk ≥0, with δk independent of
(B, G) and of the stochastic primitives. As before, each active request has an output length
o ∼Geo(p), independent across requests and of prompt lengths S ∼Dpreﬁll supported on
{0, 1, . . . , smax}, with
E[S] = µs,
Var(S) = σ2
s > 0.
Step 2.A: Slot-level dynamics under FCFS.
Index slots by (g, b) with g ∈{1, . . . , G} and b ∈{1, . . . , B}. Let Yg,b(k) denote the post-
admission size in slot (g, b) at the end of step k. Under FCFS, when the pool is overloaded
and size-agnostic, each completion in any slot is immediately refilled by the earliest pending
63


request whose prompt length is an i.i.d. draw from S. Thus, for each (g, b, k), we have the
recursion
Yg,b(k) = (1 −ξg,b(k))
 Yg,b(k −1) + δk

+ ξg,b(k) Sg,b(k),
k ≥1,
(C27)
where
ξg,b(k) ∼Bernoulli(p),
Sg,b(k) i.i.d.
∼S,
and all {ξg,b(k), Sg,b(k)}g,b,k are mutually independent and independent of the initial con-
figuration {Yg,b(0)}. The completion probability p and the drift δk do not depend on the
current size Yg,b(k −1).
For device g, define the post-admission load and the instantaneous FCFS imbalance at
step k by
Sg(k) :=
B
X
b=1
Yg,b(k),
Imbalance(k; FCFS) := G · max
1≤g≤G Sg(k) −
G
X
g=1
Sg(k).
By symmetry of the FCFS rule across devices, for any arrival instance I and any k,
E

Imbalance(k; FCFS)

= G

E

max
g
Sg(k)

−1
G E
P
g Sg(k)

.
We now show that, for every step k, the G device loads have fluctuations of order
√
B with
variance uniformly bounded away from zero, and that the maximum across devices therefore
exhibits an excess of order √B log G.
Step 2.B: Per-slot decomposition and moment bounds.
Fix a slot (g, b) and suppress its indices. For each step k, let A(k) denote the age of the current
job in this slot at the end of step k: the number of steps elapsed since the last completion
in this slot (and since time 0 if no completion has occurred). The completion indicators
{ξ(k)}k≥1 form an i.i.d. Bernoulli(p) sequence. Therefore:
• At the end of step k, the event {A(k) ≥n} implies that no completion occurred in
the last n steps, hence
P
 A(k) ≥n

≤(1 −p)n,
n ≥0, k ≥1.
(C28)
• Every time a completion occurs in this slot, the new job’s prompt length is drawn
as Sj ∼S, independently of the completion history and the past prompts. Con-
sequently, at any fixed time k, the prompt length Scur(k) of the job currently in
this slot is independent of the completion history up to time k, and in particular
independent of A(k), with
Scur(k)
d= S,
Scur(k) ⊥⊥A(k).
Given the age A(k) and the sequence (δt), the total increment accumulated since the last
completion is deterministic:
Hk(A(k)) :=
A(k)−1
X
r=0
δk−r,
with the convention that an empty sum is zero when A(k) = 0. Thus, at the end of step k,
Y (k) = Scur(k) + Hk
 A(k)

,
and Scur(k) and Hk(A(k)) are independent.
From this representation we can immediately extract uniform moment bounds.
64


Lemma 8 (Per-slot variance and third moment). For any slot and any step k ≥1,
Var
 Y (k)

≥σ2
s,
(C29)
and there exists a constant C3 < ∞depending only on (p, σ2s, δmax, smax) such that
E
 Y (k) −E[Y (k)]
3 
≤C3
for all k ≥1.
(C30)
Proof of Lemma 8 Because Scur(k) and Hk(A(k)) are independent,
Var
 Y (k)

= Var
 Scur(k)

+ Var
 Hk(A(k))

≥Var(S) = σ2
s,
establishing (C29).
For the third moment, we use that S is bounded and that Hk(A(k)) has uniformly
bounded moments. From (C28),
P
 A(k) ≥n

≤(1 −p)n,
n ≥0.
Hence, by the tail-sum formula,
E

A(k)3
≤3
∞
X
n=0
n2 P
 A(k) ≥n

≤3
∞
X
n=0
n2(1 −p)n < ∞,
and this bound is uniform in k. As 0 ≤δk ≤δmax, we have |Hk(A(k))| ≤δmaxA(k) and
therefore
E

|Hk(A(k))|3
≤δ3
max E

A(k)3
≤C′
H,
for some C′
H depending only on (p, δmax). Since Scur(k) is supported in {0, . . . , smax}, its
centered third absolute moment is bounded by a constant C′
S depending only on smax. Writing
Y (k) −E[Y (k)] =
 Scur(k) −µs

+

Hk(A(k)) −E[Hk(A(k))]

+
 µs −E[Y (k)]

,
and using the triangle inequality and the elementary bound (x + y + z)3 ≤27(x3 + y3 + z3)
for x, y, z ≥0, we obtain
E

|Y (k) −E[Y (k)]|3
≤C′′
E

|Scur(k) −µs|3
+ E

|Hk(A(k))|3
+ 1

≤C3,
for some finite constant C3 depending only on (p, σ2s, δmax, smax). This proves (C30).
□
Step 2.C: Berry–Esseen for device-level loads.
Fix a device g and a step k. The B slot sizes {Yg,b(k)}B
b=1 are independent (across b) by
construction: completions {ξg,b(k′)} and prompts {Sg,b(k′)} are independent across slots,
and the FCFS assignment is size-agnostic under the overloaded regime. Their marginal dis-
tributions may not be identical, but Lemma 8 provides uniform variance and third-moment
bounds.
Let
µg,b(k) := E

Yg,b(k)

,
Xg,b(k) := Yg,b(k) −µg,b(k),
and
Vg(k) := Var
 Sg(k)

= Var
 B
X
b=1
Yg,b(k)

=
B
X
b=1
Var
 Yg,b(k)

.
By (C29),
Vg(k) ≥B σ2
s
for all g, k.
(C31)
65


Moreover,
B
X
b=1
E

|Xg,b(k)|3
≤BC3.
Define the normalized sum
Wg(k) := Sg(k) −E[Sg(k)]
p
Vg(k)
.
By the Berry–Esseen theorem for sums of independent, non-identically distributed random
variables, there exists an absolute constant CBE ∈(0, ∞) such that, for each g, k,
sup
x∈R
P
 Wg(k) ≤x

−Φ(x)
 ≤CBE
PB
b=1 E[|Xg,b(k)|3]
Vg(k)3/2
≤CBE C3
σ3s
1
√
B
=:
C∗
√
B
, (C32)
where Φ denotes the standard normal distribution function and C∗:= CBEC3/σ3s depends
only on (p, σ2s, δmax, smax).
We now derive a uniform lower bound on the right-tail probabilities of Wg(k). Let
zG :=
r
log G
2
,
and assume log G ≥4 (the case of smaller G can be absorbed into constants). For Z ∼N(0, 1),
the Gaussian tail bound gives
P(Z ≥zG) ≥
1
√
2π
 1
zG
−1
z3
G

exp

−z2
G
2

≥
1
2
√
2π zG
G−1/4
(C33)
for all log G ≥4. Combining (C32) and (C33) yields
P
 Wg(k) ≥zG

≥
1
2
√
2π zG
G−1/4 −
C∗
√
B
.
(C34)
Since we assume
√
G = ˜o(B), there exists B0 < ∞and G0 ≥2 such that, for all G ≥G0 and
all B ≥B0 satisfying this growth condition,
C∗
√
B
≤
1
4
√
2π zG
G−1/4.
Hence, for all such (B, G) and for all devices g and steps k,
P
 Wg(k) ≥zG

≥
1
4
√
2π zG
G−1/4 =: pG.
(C35)
Step 2.D: Extreme-value lower bound across devices.
For a fixed step k, the collections {Yg,b(k)}B
b=1 are independent across g, because the slot
primitives (ξg,b(k), Sg,b(k)) are independent across both g and b and FCFS acts independently
on each device under the overloaded regime. Therefore, the device loads {Sg(k)}G
g=1 are
independent (though not necessarily identically distributed). By (C35), for each g,
P
 Wg(k) ≥zG

≥pG,
so by independence across g,
P

max
1≤g≤G Wg(k) ≥zG

= 1 −
G
Y
g=1
P
 Wg(k) < zG

≥1 −(1 −pG)G ≥1 −exp(−GpG),
(C36)
66


where we used 1 −x ≤e−x for x ≥0. From (C35),
GpG ≥
1
4
√
2π zG
G3/4,
which is bounded away from zero for every fixed G ≥2. Thus there exists an absolute
c1 ∈(0, 1) such that, for all G ≥2 and all B ≥B0,
P

max
1≤g≤G Wg(k) ≥zG

≥c1,
∀k ≥1.
(C37)
Next, define
tB,G := σs
√
B zG.
By (C31), for every g, k,
q
Vg(k) ≥σs
√
B,
so on the event {Wg(k) ≥zG} we have
Sg(k) −E[Sg(k)] = Wg(k)
q
Vg(k) ≥zG
q
Vg(k) ≥tB,G.
Therefore,

max
1≤g≤G Wg(k) ≥zG
	
⊆
n
max
1≤g≤G
 Sg(k) −E[Sg(k)]

≥tB,G
o
.
Using the standard inequality E[maxi Xi] ≥maxi ai + t P(maxi(Xi −ai) ≥t) for any family
{Xi} and any t > 0, with Xi = Si(k) and ai = E[Si(k)], we obtain
E
h
max
1≤g≤G Sg(k)
i
≥
max
1≤g≤G E[Sg(k)] + tB,G P

max
1≤g≤G
 Sg(k) −E[Sg(k)]

≥tB,G

≥
max
1≤g≤G E[Sg(k)] + tB,G P

max
1≤g≤G Wg(k) ≥zG

.
(C38)
Combining (C38) with (C37), we obtain
E
h
max
1≤g≤G Sg(k)
i
≥
max
1≤g≤G E[Sg(k)] + c1 tB,G =
max
1≤g≤G E[Sg(k)] + c1 σs
√
B zG.
(C39)
Step 2.E: Lower bound on FCFS imbalance and its time average.
For any step k, the expected imbalance under FCFS is
E

Imbalance(k; FCFS)

= G E
h
max
1≤g≤G Sg(k)
i
−
G
X
g=1
E[Sg(k)].
Writing
Mk :=
max
1≤g≤G E[Sg(k)],
mk := 1
G
G
X
g=1
E[Sg(k)],
we may rewrite
E

Imbalance(k; FCFS)

= G

E

max
g
Sg(k)

−mk

.
Using (C39) and Mk ≥mk, we obtain
E

Imbalance(k; FCFS)

≥G

Mk + c1σs
√
B zG −mk

= G
 Mk −mk

+ c1 G σs
√
B zG
≥c1 G σs
√
B zG.
(C40)
67


Recalling zG =
p
(log G)/2, we conclude that there exists a constant c > 0, depending only
on (p, σ2s, δmax, smax), such that, for all G ≥2 and all B suﬀiciently large with
√
G = ˜o(B),
E

Imbalance(k; FCFS)

≥c G σs
p
B log G,
∀k ≥1.
(C41)
Define the long-run expected time-average imbalance under FCFS and arrival instance I
by
AvgImbalance(FCFS; I) := lim inf
K→∞
1
K
K−1
X
k=0
E

Imbalance(k; FCFS)

.
Since the lower bound (C41) holds uniformly for all k ≥1, we have
AvgImbalance(FCFS; I) ≥c G σs
p
B log G,
uniformly over all overloaded arrival instances I.
In particular, as in the homogeneous-drift case, FCFS incurs imbalance of order
G√B log G, with the constant depending only on the prompt-length dispersion σ2s, the
geometric parameter p, and the drift bound δmax, but not on (B, G).
Part 3: BF-IO vs. FCFS
We now combine the BF-IO average-gap bound from Lemma 7 with the FCFS lower bound
from Part 2 to obtain the imbalance–reduction rate under geometric outputs with general
per-step growth.
Recall the definition of the long-run expected average imbalance for a policy π ∈
{BF-IO, FCFS} and an arrival instance I:
AvgImbalance(π; I) := lim sup
K→∞
1
K
K−1
X
k=0
E

Imbalance(k; π)

.
As in the homogeneous-output case, we define the imbalance–reduction ratio of BF-IO against
FCFS by
IIR := inf
I
AvgImbalance(FCFS; I)
AvgImbalance(BF-IO; I),
where the infimum ranges over all overloaded arrival instances I.
From Lemma 7, under our growth assumption
√
G = ˜o(B) we have, for any overloaded
arrival instance I,
lim sup
K→∞
1
K
K−1
X
k=0
E[D(k)] ≤smax
p
+ oB,G(1),
where oB,G(1) →0 as B, G →∞with
√
G = ˜o(B). Since for every step k,
Imbalance(k; BF-IO) =
G
X
g=1
 max
h
Lh(k) −Lg(k)

≤(G −1) D(k),
we obtain
AvgImbalance(BF-IO; I) = lim sup
K→∞
1
K
K−1
X
k=0
E

Imbalance(k; BF-IO)

≤(G−1) smax
p
+ (G−1) oB,G(1).
(C42)
On the other hand, from the FCFS analysis in Part 2 we showed that, for all G ≥2 and
all B suﬀiciently large satisfying
√
G log G = o(B), there exists a constant cF > 0 (depending
only on σ2s, p, δmax, smax) such that, uniformly over all overloaded arrival instances I,
AvgImbalance(FCFS; I) ≥cF G σs
p
B log G.
(C43)
68


Combining (C42) and (C43), we obtain, for every such I,
AvgImbalance(FCFS; I)
AvgImbalance(BF-IO; I) ≥
cF G σs
√B log G
(G −1) smax
p
+ (G −1) oB,G(1)
=
 
cF p σs
smax
G
G −1
!
√B log G
1 +
p
smax oB,G(1).
Since oB,G(1) →0, we can choose B large enough (as a function of G) so that
p
smax oB,G(1) ≤
1
2. For all such (B, G),
1
1 +
p
smax oB,G(1) ≥2
3,
and we obtain the uniform lower bound
AvgImbalance(FCFS; I)
AvgImbalance(BF-IO; I) ≥c p σs
smax
G
G −1
p
B log G,
for some universal constant c > 0 depending only on (σ2s, p, δmax, smax). Taking the infimum
over all overloaded arrival instances I yields
IIR = inf
I
AvgImbalance(FCFS; I)
AvgImbalance(BF-IO; I) ≥c p σs
smax
G
G −1
p
B log G = Ω
 p
B log G

,
which shows that BF-IO achieves the same √B log G-order improvement over FCFS in the
general per-step drift setting as in the homogeneous +1 model.
□
C.4
Proof of Theorem 4
Proof of Theorem 4 Fix π and I. For each step k, recall ug(k) = Lg(k)/Lg∗(k). By (10),
E(π; I) = κATT
Kπ(I)−1
X
k=0
Lg∗(k)
G
X
g=1

Pidle + (Pmax −Pidle) ug(k)γ
= κATT
X
k,g

PidleLg∗(k) + (Pmax −Pidle)Lg∗(k)ug(k)

(C44)
+ κATT(Pmax −Pidle)
X
k,g
Lg∗(k)
 ug(k)γ −ug(k)

,
(C45)
where P
k,g abbreviates PKπ(I)−1
k=0
PG
g=1. Since Lg∗(k)ug(k) = Lg(k), the first sum in (C45)
becomes
κATT
X
k,g

PidleLg∗(k) + (Pmax −Pidle)Lg(k)

= κATT
X
k,g

PmaxLg(k) + Pidle(Lg∗(k) −Lg(k))

= κATTPmaxW(I) + κATTPidleImbTot(π; I),
(C46)
using (11) and (12). Substituting (C46) into (C45) yields the exact identity
E(π; I) = κATTPmaxW(I)+κATTPidleImbTot(π; I)+κATT(Pmax−Pidle)
X
k,g
Lg∗(k)
 ug(k)γ−ug(k)

.
(C47)
We next bound the last term in (C47). Since γ ∈(0, 1) and u ∈[0, 1], we have uγ ≥u,
hence the last term is nonnegative. For an upper bound, concavity of uγ on [0, 1] implies the
tangent inequality at u = 1:
uγ ≤1 + γ(u −1) = (1 −γ) + γu,
69


so uγ −u ≤(1 −γ)(1 −u). Therefore,
Lg∗(k)
 ug(k)γ −ug(k)

≤(1 −γ) Lg∗(k)
 1 −ug(k)

= (1 −γ)
 Lg∗(k) −Lg(k)

.
Summing over k, g and using (12),
0 ≤
X
k,g
Lg∗(k)
 ug(k)γ −ug(k)

≤(1 −γ) ImbTot(π; I).
(C48)
Plugging (C48) into (C47) yields the sandwich bound
κATTPmaxW(I)+κATTPidleImbTot(π; I) ≤E(π; I) ≤κATTPmaxW(I)+κATTCγ ImbTot(π; I),
(C49)
where Cγ is defined in (15).
Now apply (C49) with π = π0 and π = π1. Using the upper bound for E(π0; I) and the
lower bound for E(π1; I) gives
E(π0; I) ≤κATTW(I)

Pmax + Cγ ηsum(π0; I)

,
(C50)
where we used ImbTot(π0; I) = ηsum(π0; I)W(I). For the energy difference, use (C47) and
(C48) to obtain
E(π0; I) −E(π1; I) = κATTPidle
 ImbTot(π0; I) −ImbTot(π1; I)

+ κATT(Pmax −Pidle)
 X(π0) −X(π1)

,
(C51)
where X(π) := P
k,g Lg∗(k)
 ug(k)γ −ug(k)

. By (C48), X(π0) ≥0 and X(π1) ≤(1 −
γ)ImbTot(π1; I). Hence
E(π0; I) −E(π1; I) ≥κATTPidle
 ImbTot(π0; I) −ImbTot(π1; I)

−κATT(Pmax −Pidle)(1 −γ) ImbTot(π1; I)
= κATT
 Pidle
 ImbTot(π0; I) −ImbTot(π1; I)

−Dγ ImbTot(π1; I)

,
(C52)
where Dγ is defined in (15). Under the assumption (14), ImbTot(π1; I) ≤ImbTot(π0; I)/α,
so (C52) implies
E(π0; I) −E(π1; I) ≥κATTImbTot(π0; I)

Pidle

1 −1
α

−Dγ
α

.
(C53)
Combining (C53) with (C50) and using E

ImbTot(π0; I)

= ηsum(π0; I)E

W(I)

gives (16).
Taking the expectation completes the proof.
□
C.5
Proof of Inequality (17)
Proof of Inequality (17) Under FCFS in the overloaded regime, the slot-level Markov recur-
sion (C14) (Section 5) is uniformly ergodic with unique stationary slot law U
d= Scur + A
where Scur
d= S and A ∼Geo(p) −1 independent, hence
µU := E[U] = µs + 1 −p
p
,
Var(U) = σ2
snap.
At stationarity, the total system workload in a step equals PG
g=1
PB
b=1 Ug,b, so its expectation
is GBµU. Therefore the long-run average total workload per step under FCFS equals GBµU
by uniform ergodicity and Cesàro averaging. On the other hand, Part 2 of Appendix C.2
establishes the lower bound
E

AvgImbalance(FCFS; I)

≥c′ G σsnap
p
B log G
70


for a universal constant c′ > 0 and all B suﬀiciently large. Dividing by GBµU yields
E

AvgImbalance(FCFS; I)

GBµU
≥c′ σsnap
µU
r
log G
B
.
Interpreting ηsum(FCFS; I) as the corresponding normalized imbalance level (cumulative or
long-run averaged; these coincide under stationarity), we obtain (17) (absorbing constants
into ≳).
□
Appendix D
Supplementary Materials for Section 6
D.1
Energy and MFU Computation
This appendix provides details on the energy consumption metric used in our
experiments.
Model FLOPs Utilization (MFU).
MFU measures how eﬀiciently the GPU’s computational capacity is utilized, defined
as the ratio of actual floating-point operations performed to the theoretical peak:
mfu = Observed FLOPs/second
Peak FLOPs/second
(D54)
For LLM inference, the FLOPs required to generate one token is approximately
6 × Nparams, arising from the matrix multiplications in the forward pass (approxi-
mately 2 × Nparams multiply-accumulate operations, each counting as 2 FLOPs, with
an additional factor from attention computation). Thus, for a throughput of T tokens
per second on a model with Nparams parameters:
mfu ≈T × 6 × Nparams
FLOPspeak
(D55)
where FLOPspeak is the GPU’s theoretical peak throughput (e.g., 312 TFLOPs for
NVIDIA A100 at FP16/BF16 precision).
Power Model and Parameters.
We adopt the power–utilization model from [21]:
P(mfu) = Pidle + (Pmax −Pidle) ·
 mfu
mfusat
γ
(D56)
The parameters are set as follows:
• Pidle = 100W: baseline power draw when the GPU is powered but not computing,
reflecting memory refresh, control logic, and leakage current.
• Pmax = 400W: peak power draw at full utilization, consistent with NVIDIA
A100/H100 thermal design power specifications.
71


• mfusat = 0.45: the utilization level at which power consumption saturates. In prac-
tice, inference workloads rarely exceed this threshold due to memory bandwidth
limitations.
• γ = 0.7: the sublinear scaling exponent, empirically calibrated in [21] for inference
workloads. The value γ < 1 implies diminishing power increase per unit utilization
gain, but also substantial power draw (Pidle) even at zero useful throughput.
Implications for Load Balancing.
The sublinear power model has a direct implication for barrier-synchronized systems.
At each decode step, the wall-clock duration is ∆t = C + tℓ· maxg Lg. Workers with
load Lg < maxg′ Lg′ complete their local computation early and idle until the barrier
clears. During this idle period, these workers draw power near Pidle while generating
zero tokens. Therefore, the total energy consumed is the discrete approximation of
the integral (Equation (6)) :
E =
K
X
k=1
G
X
g=1
h
P
 mfu(k)
g

· ∆t(k)i
(D57)
where mfu(k)
g
reflects worker g’s effective utilization at step k. Workers idling at bar-
riers have mfu(k)
g
≈0 for a fraction of the step, drawing Pidle without contributing to
throughput. By reducing load imbalance, BF-IO ensures that all workers remain pro-
ductively utilized throughout each step, converting idle power into useful computation
and thereby reducing total energy consumption.
D.2
Results on the Lighter-Load Dataset BurstGPT
We additionally evaluate our routing policy on BurstGPT [31], a lighter-load bench-
mark with shorter request lengths than LongBench. The prefill and decode length
distributions are shown in Figure D1. Given the reduced per-request workload, 16
GPUs suﬀice to process this dataset.
Fig. D1: Distribution of prefill (input) and decode (output) lengths in the BurstGPT
dataset.
72


Load Trajectory Analysis.
Figure D2 presents per-worker load trajectories for the BurstGPT workload. Under
FCFS, worker loads spread across 250k–400k tokens, with persistent inter-worker vari-
ance throughout execution. JSQ exhibits even greater imbalance, confirming that
queue length is a poor proxy for actual token load when request sizes vary substan-
tially. In contrast, BF-IO (H = 0) achieves tighter clustering around 270k–320k tokens.
With lookahead enabled, BF-IO (H = 20) produces the most balanced distribution:
all workers maintain nearly identical loads throughout execution, reducing average
imbalance by a factor of 17× compared to FCFS.
Fig. D2: Per-worker token load trajectories under four routing policies (BurstGPT,
G = 16 GPUs). FCFS exhibits persistent load spread (250k–400k tokens); JSQ shows
even greater imbalance due to its size-agnostic assignment. BF-IO (H = 0) achieves
substantially tighter clustering, and BF-IO (H = 20) produces near-uniform loads
across all workers throughout execution.
Power Consumption Analysis.
Figure D3 illustrates average GPU power consumption over time. The baseline FCFS
policy oscillates between 330–380 W, reflecting periods in which subsets of GPUs
remain underutilized. BF-IO (H = 20) maintains more stable power draw at higher
sustained utilization, completing the workload faster despite drawing similar peak
power—consistent with the energy-saving mechanism established in the main text.
73


Fig. D3: Average GPU power consumption over time (BurstGPT, G = 16 GPUs).
FCFS exhibits fluctuations (330–380 W) due to load imbalance, while BF-IO maintains
higher sustained utilization and completes the workload in less time, yielding lower
total energy consumption.
Quantitative Metrics Comparison.
Table D1 summarizes performance across routing policies. Even in this lighter-load
regime, BF-IO (H = 20) reduces average imbalance by 17× relative to FCFS, improves
throughput by 14%, reduces TPOT by 12%, and lowers energy consumption by 3.3%.
The more modest energy savings compared to LongBench (3.3% vs. 29%) reflect
the lower baseline imbalance in this easier workload; nevertheless, the improvements
remain consistent and statistically significant across all metrics.
Notably, FCFS and JSQ exhibit nearly identical performance on this dataset, con-
firming that both policies are effectively size-agnostic: JSQ balances queue lengths
rather than token loads, providing no advantage when request sizes vary.
Table D1: Performance comparison on BurstGPT (G = 16 GPUs, B = 72
batch size). Arrows indicate preferred direction.
Policy
Avg Imbalance↓
Throughput↑
TPOT↓
Energy↓
(×104)
(104 tok/s)
(10−2 s/tok)
(kJ)
FCFS
27.9
8.00
1.42
396
JSQ
28.2
7.99
1.42
396
BF-IO (H = 0)
2.92
9.03
1.26
386
BF-IO (H = 20)
1.65
9.13
1.25
383
74


References
[1] Hess, B., Kutzner, C., Spoel, D., Lindahl, E.: Gromacs 4: Algorithms for highly
eﬀicient, load-balanced, and scalable molecular simulation. Journal of Chem-
ical Theory and Computation 4(3), 435–447 (2008) https://doi.org/10.1021/
ct700301q
[2] Deng, Y., Cai, W.: An adaptive load balancing method for parallel molecu-
lar dynamics simulations. Journal of Computational Physics 161(1), 250–263
(2000) https://doi.org/10.1006/jcph.2000.6501
[3] Foster, I.T., Toonen, B.R.: Load-balancing algorithms for climate models. In:
Proceedings of IEEE Scalable High Performance Computing Conference, pp.
674–681 (1994). IEEE
[4] Rodrigues, E.R., Navaux, P.O., Panetta, J., Fazenda, A., Mendes, C.L.,
Kale, L.V.: A comparative analysis of load balancing algorithms applied to a
weather forecast model. In: 2010 22nd International Symposium on Computer
Architecture and High Performance Computing, pp. 71–78 (2010). IEEE
[5] Mishra, S.K., Sahoo, B., Parida, P.P.: Load balancing in cloud computing: a big
picture. Journal of King Saud University-Computer and Information Sciences
32(2), 149–158 (2020)
[6] Khiyaita, A., El Bakkali, H., Zbakh, M., El Kettani, D.: Load balancing cloud
computing: state of art. 2012 National Days of Network Security and Systems,
106–109 (2012)
[7] Chen, C., Lee, C., McGillem, C.: Task assignment and load balancing of
autonomous vehicles in a flexible manufacturing system. IEEE Journal on
Robotics and Automation 3(6), 659–671 (1987)
[8] Li, D., Tang, H., Wang, S., Liu, C.: A big data enabled load-balancing control
for smart manufacturing of industry 4.0. Cluster Computing 20(2), 1855–1864
(2017)
[9] Kolomvatsos, K., Panagidi, K., Hadjiefthymiades, S.: A load balancing module
for post-emergency management. Expert systems with applications 42(1), 657–
667 (2015)
[10] Friesen, M.R., McLeod, R., Strome, T., Mukhi, S.: Load balancing at emergency
departments using ‘crowdinforming’. In: 2011 IEEE 13th International Confer-
ence on e-Health Networking, Applications and Services, pp. 364–370 (2011).
IEEE
[11] Brown, T., Mann, B., Ryder, N., Subbiah, M., Kaplan, J.D., Dhariwal, P.,
Neelakantan, A., Shyam, P., Sastry, G., Askell, A., et al.: Language models
75


are few-shot learners. Advances in neural information processing systems 33,
1877–1901 (2020)
[12] Chowdhery, A., Narang, S., Devlin, J., Bosma, M., Mishra, G., Roberts, A.,
Barham, P., Chung, H.W., Sutton, C., Gehrmann, S., et al.: Palm: Scaling lan-
guage modeling with pathways. Journal of Machine Learning Research 24(240),
1–113 (2023)
[13] OpenAI: GPT-4 technical report. arxiv 2303.08774. View in Article 2(5) (2023)
[14] Kaplan, J., McCandlish, S., Henighan, T., Brown, T.B., Chess, B., Child, R.,
Gray, S., Radford, A., Wu, J., Amodei, D.: Scaling laws for neural language
models. arXiv preprint arXiv:2001.08361 (2020)
[15] Wei, J., Tay, Y., Bommasani, R., Raffel, C., Zoph, B., Borgeaud, S., Yogatama,
D., Bosma, M., Zhou, D., Metzler, D., et al.: Emergent abilities of large language
models. arXiv preprint arXiv:2206.07682 (2022)
[16] Kim, J., Lee, J., Park, G., Kim, B., Kwon, S.J., Lee, D., Lee, Y.: An inquiry
into datacenter tco for llm inference with fp8. arXiv preprint arXiv:2502.01070
(2025)
[17] Milmo, D.: Ai could account for nearly half of datacentre power usage
by end of year. https://www.theguardian.com/environment/2025/may/22/ai-
data-centre-power-consumption (2025)
[18] Roth, E.: Openai says chatgpt users send over 2.5 billion prompts every
day. https://www.theverge.com/news/710867/openai-chatgpt-daily-prompts-2-
billionutmsource=chatgpt.com (2025)
[19] You, J.: How much energy does chatgpt use? https://epoch.ai/gradient-
updates/how-much-energy-does-chatgpt-use (2025)
[20] Blunt, K., Hiller, J.: Big tech’s latest obsession is finding enough energy.
https://www.wsj.com/business/energy-oil/big-techs latest-obsession-is-finding-
enough-energy-f00055b2
[21] Özcan, M., Wiesner, P., Weiß, P., Kao, O.: Quantifying the energy consump-
tion and carbon emissions of llm inference via simulations. arXiv preprint
arXiv:2507.11417 (2025)
[22] Zheng, Z., Ren, X., Xue, F., Luo, Y., Jiang, X., You, Y.: Response length
perception and sequence scheduling: An llm-empowered llm inference pipeline.
Advances in Neural Information Processing Systems 36, 65517–65530 (2023)
[23] Qiu, H., Mao, W., Patke, A., Cui, S., Jha, S., Wang, C., Franke, H., Kalbarczyk,
76


Z.T., Başar, T., Iyer, R.K.: Eﬀicient interactive LLM serving with proxy model-
based sequence length prediction. arXiv preprint arXiv:2404.08509 (2024)
[24] Fu, Y., Zhu, S., Su, R., Qiao, A., Stoica, I., Zhang, H.: Eﬀicient llm scheduling
by learning to rank. arXiv preprint arXiv:2408.15792 (2024)
[25] Chen, S., Jiang, R., Yu, D., Xu, J., Chao, M., Meng, F., Jiang, C., Xu,
W., Liu, H.: Kvdirect: Distributed disaggregated llm inference. arXiv preprint
arXiv:2501.14743 (2024)
[26] Shahout, R., Malach, E., Liu, C., Jiang, W., Yu, M., Mitzenmacher, M.:
Don’t stop me now: Embedding based scheduling for llms. arXiv preprint
arXiv:2410.01035 (2024)
[27] Luenberger, D.G., Ye, Y., et al.: Linear and Nonlinear Programming vol. 2.
Springer, New York (1984)
[28] Dantzig, G.B.: Linear programming. Operations research 50(1), 42–47 (2002)
[29] Schrijver, A.: Theory of Linear and Integer Programming. John Wiley & Sons,
New York (1998)
[30] Wang, G., Cheng, S., Zhan, X., Li, X., Song, S., Liu, Y.: Openchat: Advanc-
ing open-source language models with mixed-quality data. arXiv preprint
arXiv:2309.11235 (2023)
[31] Wang, Y., Chen, Y., Li, Z., Kang, X., Tang, Z., He, X., Guo, R., Wang, X., Wang,
Q., Zhou, A.C., et al.: Burstgpt: A real-world workload dataset to optimize llm
serving systems. arXiv preprint arXiv:2401.17644 (2024)
[32] Zheng, L., Chiang, W.-L., Sheng, Y., Li, T., Zhuang, S., Wu, Z., Zhuang, Y.,
Li, Z., Lin, Z., Xing, E., et al.: LMSYS-Chat-1M: A large-scale real-world LLM
conversation dataset. arXiv preprint arXiv:2309.11998 (2023)
[33] Zhao, W., Ren, X., Hessel, J., Cardie, C., Choi, Y., Deng, Y.: Wildchat: 1m
chatgpt interaction logs in the wild. arXiv preprint arXiv:2405.01470 (2024)
[34] Bai, Y., Lv, X., Zhang, J., Lyu, H., Tang, J., Huang, Z., Du, Z., Liu, X., Zeng, A.,
Hou, L., et al.: Longbench: A bilingual, multitask benchmark for long context
understanding. In: Proceedings of the 62nd Annual Meeting of the Association
for Computational Linguistics (volume 1: Long Papers), pp. 3119–3137 (2024)
[35] Wang, Y., Chen, Y., Li, Z., Kang, X., Fang, Y., Zhou, Y., Zheng, Y., Tang, Z.,
He, X., Guo, R., et al.: Burstgpt: A real-world workload dataset to optimize
llm serving systems. In: Proceedings of the 31st ACM SIGKDD Conference on
Knowledge Discovery and Data Mining V. 2, pp. 5831–5841 (2025)
[36] Berger, M.J., Oliger, J.: Adaptive mesh refinement for hyperbolic partial
77


differential equations. Journal of computational Physics 53(3), 484–512 (1984)
[37] Berger, M.J., Colella, P.: Local adaptive mesh refinement for shock hydrody-
namics. Journal of computational Physics 82(1), 64–84 (1989)
[38] Aslam, S., Shah, M.A.: Load balancing algorithms in cloud computing: A sur-
vey of modern techniques. In: 2015 National Software Engineering Conference
(NSEC), pp. 30–35 (2015). IEEE
[39] Saeed, F., Javaid, N., Zubair, M., Ismail, M., Zakria, M., Ashraf, M.H., Kamal,
M.B.: Load balancing on cloud analyst using first come first serve schedul-
ing algorithm. In: International Conference on Intelligent Networking and
Collaborative Systems, pp. 463–472 (2018). Springer
[40] Rathi, R., Narkhede, N., Hasamnis, M.: Design and implementation of first-in-
first-out (fifo) buffer for distributed load balancing systems. In: International
Conference on Smart Computing and Communication, pp. 11–21 (2024).
Springer
[41] Tarandeep, Bhushan, K.: Load balancing in cloud through task scheduling.
In: Recent Trends in Communication and Intelligent Systems: Proceedings of
ICRTCIS 2019, pp. 195–204. Springer, New York (2020)
[42] Whitt, W.: Deciding which queue to join: Some counterexamples. Operations
research 34(1), 55–62 (1986)
[43] Dai, J.G., Hasenbein, J.J., Kim, B.: Stability of join-the-shortest-queue net-
works. Queueing Systems 57(4), 129–145 (2007)
[44] Foley, R.D., McDonald, D.R.: Join the shortest queue: stability and exact
asymptotics. Annals of Applied Probability, 569–607 (2001)
[45] Kwon, W., Li, Z., Zhuang, S., Sheng, Y., Zheng, L., Yu, C.H., Gonzalez,
J., Zhang, H., Stoica, I.: Eﬀicient memory management for large language
model serving with PagedAttention. In: Proceedings of the 29th Symposium on
Operating Systems Principles, pp. 611–626 (2023)
[46] Zheng, L., Yin, L., Xie, Z., Sun, C.L., Huang, J., Yu, C.H., Cao, S., Kozyrakis,
C., Stoica, I., Gonzalez, J.E., et al.: Sglang: Eﬀicient execution of structured
language model programs. Advances in neural information processing systems
37, 62557–62583 (2024)
[47] Sran, N., Kaur, N.: Comparative analysis of existing load balancing techniques
in cloud computing. International Journal of Engineering Science Invention 2(1),
60–63 (2013)
[48] Subashini, S., Kavitha, V.: A survey on security issues in service delivery models
78


of cloud computing. Journal of network and computer applications 34(1), 1–11
(2011)
[49] Swarnkar, N., Singh, A.K., Shankar, R.: A survey of load balancing techniques
in cloud computing. Int J Eng Res Technol (IJERT) 2(8), 800–804 (2013)
[50] Gardner, K., Harchol-Balter, M., Scheller-Wolf, A., Velednitsky, M., Zbarsky,
S.: Redundancy-d: The power of d choices for redundancy. Operations Research
65(4), 1078–1094 (2017)
[51] Hellemans, T., Van Houdt, B.: On the power-of-d-choices with least loaded
server selection. Proceedings of the ACM on Measurement and Analysis of
Computing Systems 2(2), 1–22 (2018)
[52] Mukherjee, D., Borst, S.C., Van Leeuwaarden, J.S., Whiting, P.A.: Universality
of power-of-d load balancing in many-server systems. Stochastic Systems 8(4),
265–292 (2018)
[53] Patel, G., Mehta, R., Bhoi, U.: Enhanced load balanced min-min algorithm for
static meta task scheduling in cloud computing. Procedia Computer Science 57,
545–553 (2015)
[54] Chen, H., Wang, F., Helian, N., Akanmu, G.: User-priority guided min-min
scheduling algorithm for load balancing in cloud computing. In: 2013 National
Conference on Parallel Computing Technologies (PARCOMPTECH), pp. 1–8
(2013). IEEE
[55] Bhoi, U., Ramanuj, P.N., et al.: Enhanced max-min task scheduling algo-
rithm in cloud computing. International Journal of Application or Innovation
in Engineering and Management (IJAIEM) 2(4), 259–264 (2013)
[56] Rewehel, E.M., Mostafa, M.-S.M., Ragaie, M.O.: New subtask load balanc-
ing algorithm based on olb and lbmm scheduling algorithms in cloud. In:
Proceedings of the 2014 International Conference on Computer Network and
Information Science, pp. 9–14 (2014)
[57] Kunwar, V., Agarwal, N., Rana, A., Pandey, J.: Load balancing in cloud—a
systematic review. Big Data Analytics: Proceedings of CSI 2015, 583–593 (2017)
[58] Kansal, N.J., Chana, I.: Cloud load balancing techniques: A step towards green
computing. IJCSI International Journal of Computer Science Issues 9(1), 238–
246 (2012)
[59] Nace, D., Pióro, M.: Max-min fairness and its applications to routing and
load-balancing in communication networks: a tutorial. IEEE Communications
Surveys & Tutorials 10(4), 5–17 (2009)
79


[60] Mao, Y., Chen, X., Li, X.: Max–min task scheduling algorithm for load balance
in cloud computing. In: Proceedings of International Conference on Computer
Science and Information Technology: CSAIT 2013, September 21–23, 2013,
Kunming, China, pp. 457–465 (2014). Springer
[61] Ghosh, T.K., Goswami, R., Bera, S., Barman, S.: Load balanced static grid
scheduling using max-min heuristic. In: 2012 2nd IEEE International Conference
on Parallel, Distributed and Grid Computing, pp. 419–423 (2012). IEEE
[62] Sim, K.M., Sun, W.H.: Ant colony optimization for routing and load-balancing:
survey and new directions. IEEE transactions on systems, man, and cybernetics-
Part A: systems and humans 33(5), 560–572 (2003)
[63] Nishant, K., Sharma, P., Krishna, V., Gupta, C., Singh, K.P., Rastogi, R., et
al.: Load balancing of nodes in cloud using ant colony optimization. In: 2012
UKSim 14th International Conference on Computer Modelling and Simulation,
pp. 3–8 (2012). IEEE
[64] Li, K., Xu, G., Zhao, G., Dong, Y., Wang, D.: Cloud task scheduling based
on load balancing ant colony optimization. In: 2011 Sixth Annual ChinaGrid
Conference, pp. 3–9 (2011). IEEE
[65] LD, D.B., Krishna, P.V.: Honey bee behavior inspired load balancing of tasks
in cloud computing environments. Applied soft computing 13(5), 2292–2303
(2013)
[66] Thapliyal, N., Dimri, P.: Load balancing in cloud computing based on honey bee
foraging behavior and load balance min-min scheduling algorithm. International
Journal of Electrical and Electronics Research (IJEER) 10(1), 1–6 (2022)
[67] Domanal, S.G., Reddy, G.R.M.: Load balancing in cloud computingusing mod-
ified throttled algorithm. In: 2013 IEEE International Conference on Cloud
Computing in Emerging Markets (CCEM), pp. 1–5 (2013). IEEE
[68] Panigrahi, A., Sahu, B., Rout, S.K., Rath, A.K.: M-throttled: Dynamic load
balancing algorithm for cloud computing. In: Intelligent and Cloud Computing:
Proceedings of ICICC 2019, Volume 1, pp. 3–10. Springer, New York (2020)
[69] Hu, J., Gu, J., Sun, G., Zhao, T.: A scheduling strategy on load balancing of
virtual machine resources in cloud computing environment. In: 2010 3rd Inter-
national Symposium on Parallel Architectures, Algorithms and Programming,
pp. 89–96 (2010). IEEE
[70] Singh, J., Blumer, T., Roy, S., Saha, K.: Carton clamp test methodologies
and the effects on load containment and retention. Packaging Technology and
Science 28(1), 15–30 (2015)
80


[71] Ramesh, D., Dey, S.: Sclba-cc: slot based carton load balancing approach
for cloud environment. In: 2018 International Conference on Current Trends
Towards Converging Technologies (ICCTCT), pp. 1–5 (2018). IEEE
[72] Stojkovic, J., Choukse, E., Zhang, C., Goiri, I., Torrellas, J.: Towards greener
llms: Bringing energy-eﬀiciency to the forefront of llm inference. arXiv preprint
arXiv:2403.20306 (2024)
[73] Ding, Y., Shi, T.: Sustainable llm serving: Environmental implications, chal-
lenges, and opportunities. In: Proceedings of the 15th IEEE International
Green and Sustainable Computing Conference (IGSC), pp. 37–38 (2024). https:
//doi.org/10.1109/IGSC64514.2024.00016
[74] Nguyen,
S.,
Zhou,
B.,
Ding,
Y.,
Liu,
S.:
Towards
sustainable
large
language
model
serving.
In:
Proceedings
of
the
3rd
Work-
shop
on
Sustainable
Computer
Systems
(HotCarbon
’24)
(2024).
https://hotcarbon.org/assets/2024/pdf/hotcarbon24-final3.pdf
[75] Wilkins, G., Keshav, S., Mortier, R.: Offline energy-optimal llm serving:
Workload-based energy models for llm inference on heterogeneous systems.
arXiv preprint arXiv:2407.04014 (2024)
[76] Kakolyris, A.K., Masouros, D., Xydis, S., Soudris, D.: Slo-aware gpu dvfs
for energy-eﬀicient llm inference serving. IEEE Computer Architecture Letters
23(2), 150–153 (2024) https://doi.org/10.1109/LCA.2024.3406038
[77] Liu, Q., Huang, D., Zapater, M., Atienza, D.: Greenllm: Slo-aware dynamic fre-
quency scaling for energy-eﬀicient llm serving. arXiv preprint arXiv:2508.16449
(2025)
[78] Zhong, Y., Liu, S., Chen, J., Hu, J., Zhu, Y., Liu, X., Jin, X., Zhang, H.: Dist-
serve: Disaggregating prefill and decoding for goodput-optimized large language
model serving. arXiv preprint arXiv:2401.09670 (2024)
[79] Patel, P., Choukse, E., Zhang, C., Shah, A., Goiri, Í., Maleki, S., Bianchini,
R.: Splitwise: Eﬀicient generative LLM inference using phase splitting. Power
400(700W), 1–75 (2023)
[80] Liu, A., Feng, B., Wang, B., Wang, B., Liu, B., Zhao, C., Dengr, C., Ruan,
C., Dai, D., Guo, D., et al.: Deepseek-v2: A strong, economical, and eﬀicient
mixture-of-experts language model. arXiv preprint arXiv:2405.04434 (2024)
[81] Shoeybi, M., Patwary, M., Puri, R., LeGresley, P., Casper, J., Catanzaro, B.:
Megatron-lm: Training multi-billion parameter language models using model
parallelism. arXiv preprint arXiv:1909.08053 (2019)
81


[82] Narayanan, D., Shoeybi, M., Casper, J., LeGresley, P., Patwary, M., Kor-
thikanti, V., Vainbrand, D., Kashinkunti, P., Bernauer, J., Catanzaro, B., et al.:
Eﬀicient large-scale language model training on gpu clusters using megatron-
lm. In: Proceedings of the International Conference for High Performance
Computing, Networking, Storage and Analysis, pp. 1–15 (2021)
[83] Fedus, W., Zoph, B., Shazeer, N.: Switch transformers: Scaling to trillion param-
eter models with simple and eﬀicient sparsity. Journal of Machine Learning
Research 23(120), 1–39 (2022)
[84] Yu, G.-I., Jeong, J.S., Kim, G.-W., Kim, S., Chun, B.-G.: Orca: A distributed
serving system for {Transformer-Based} generative models. In: 16th USENIX
Symposium on Operating Systems Design and Implementation (OSDI 22), pp.
521–538 (2022)
[85] Agrawal, A., Panwar, A., Mohan, J., Kwatra, N., Gulavani, B.S., Ramjee, R.:
Sarathi: Eﬀicient LLM inference by piggybacking decodes with chunked prefills.
arXiv preprint arXiv:2308.16369 (2023)
[86] Agrawal, A., Kedia, N., Panwar, A., Mohan, J., Kwatra, N., Gulavani, B.S.,
Tumanov, A., Ramjee, R.: Taming throughput-latency tradeoff in LLM inference
with Sarathi-Serve. arXiv preprint arXiv:2403.02310 (2024)
[87] Wu, B., Zhong, Y., Zhang, Z., Liu, S., Liu, F., Sun, Y., Huang, G., Liu, X., Jin,
X.: Fast distributed inference serving for large language models. arXiv preprint
arXiv:2305.05920 (2023)
[88] Sheng, Y., Cao, S., Li, D., Zhu, B., Li, Z., Zhuo, D., Gonzalez, J.E., Stoica,
I.: Fairness in serving large language models. In: 18th USENIX Symposium on
Operating Systems Design and Implementation (OSDI 24), pp. 965–988 (2024)
[89] Srivatsa, V., He, Z., Abhyankar, R., Li, D., Zhang, Y.: Preble: Eﬀicient dis-
tributed prompt scheduling for llm serving. arXiv preprint arXiv:2407.00023
(2024)
[90] Hu, C., Huang, H., Xu, L., Chen, X., Wang, C., Xu, J., Chen, S., Feng, H., Wang,
S., Bao, Y., et al.: Shuffleinfer: Disaggregate llm inference for mixed downstream
workloads. ACM Transactions on Architecture and Code Optimization (2025)
[91] Doucet, Z., Sharma, R., Vos, M., Pires, R., Kermarrec, A.-M., Balmau,
O.: Harmoeny: Eﬀicient multi-gpu inference of moe models. arXiv preprint
arXiv:2506.12417 (2025)
[92] Zhang, Y., Pinkert, G., Yang, N., Li, Y., Yuan, D.: Duoserve-moe: Dual-phase
expert prefetch and cache scheduling for eﬀicient moe llm inference. arXiv
preprint arXiv:2509.07379 (2025)
82


[93] Jain, K., Parayil, A., Mallick, A., Choukse, E., Qin, X., Zhang, J., Goiri, Í.,
Wang, R., Bansal, C., Rühle, V., et al.: Performance aware llm load balancer
for mixed workloads. In: Proceedings of the 5th Workshop on Machine Learning
and Systems, pp. 19–30 (2025)
[94] Kim, K., Hong, K., Gulcehre, C., Ailamaki, A.: The effect of scheduling
and preemption on the eﬀiciency of llm inference serving. arXiv preprint
arXiv:2411.07447 (2024)
[95] Jaillet, P., Jiang, J., Mellou, K., Molinaro, M., Podimata, C., Zhou, Z.:
Online scheduling for llm inference with kv cache constraints. arXiv preprint
arXiv:2502.07115 (2025)
[96] Wang, M., Ye, Y., Zhou, Z.: Llm serving optimization with variable prefill and
decode lengths. arXiv preprint arXiv:2508.06133 (2025)
[97] Chen, Z., Ye, Y., Zhou, Z.: Adaptively robust llm inference optimization under
prediction uncertainty. arXiv preprint arXiv:2508.14544 (2025)
[98] Ao, R., Luo, G., Simchi-Levi, D., Wang, X.: Optimizing llm inference:
Fluid-guided online scheduling with memory constraints. arXiv preprint
arXiv:2504.11320 (2025)
[99] Karp, R.M., Vazirani, U.V., Vazirani, V.V.: An optimal algorithm for on-
line bipartite matching. In: Proceedings of the Twenty-second Annual ACM
Symposium on Theory of Computing, pp. 352–358 (1990)
[100] Mehta, A., Saberi, A., Vazirani, U., Vazirani, V.: Adwords and generalized online
matching. Journal of the ACM (JACM) 54(5), 22 (2007)
[101] Devanur, N.R., Hayes, T.P.: The adwords problem: online keyword matching
with budgeted bidders under random permutations. In: Proceedings of the 10th
ACM Conference on Electronic Commerce, pp. 71–78 (2009)
[102] Mehta, A., et al.: Online matching and ad allocation. Foundations and Trends®
in Theoretical Computer Science 8(4), 265–368 (2013)
[103] Jaillet, P., Lu, X.: Online stochastic matching: New algorithms with better
bounds. Mathematics of Operations Research 39(3), 624–646 (2014)
[104] Gallego, G., Li, A., Truong, V.-A., Wang, X.: Online resource allocation with
customer choice. arXiv preprint arXiv:1511.01837 (2015)
[105] Ma, W., Simchi-Levi, D.: Online resource allocation under arbitrary arrivals:
Optimal algorithms and tight competitive ratios. Available at SSRN 2989332
(2017)
83


[106] Wang, L., Jiao, L., Li, J., Mühlhäuser, M.: Online resource allocation for arbi-
trary user mobility in distributed edge clouds. In: 2017 IEEE 37th International
Conference on Distributed Computing Systems (ICDCS), pp. 1281–1290 (2017).
IEEE
[107] Ma, W., Xu, P., Xu, Y.: Fairness maximization among offline agents in online-
matching markets. ACM Transactions on Economics and Computation 10(4),
1–27 (2023)
[108] Huang, Z., Kang, N., Tang, Z.G., Wu, X., Zhang, Y., Zhu, X.: Fully online
matching. Journal of the ACM (JACM) 67(3), 1–25 (2020)
[109] Ho, C.-J., Vaughan, J.: Online task assignment in crowdsourcing markets. In:
Proceedings of the AAAI Conference on Artificial Intelligence, vol. 26, pp. 45–51
(2012)
[110] Sumita, H., Ito, S., Takemura, K., Hatano, D., Fukunaga, T., Kakimura, N.,
Kawarabayashi, K.-i.: Online task assignment problems with reusable resources.
In: Proceedings of the AAAI Conference on Artificial Intelligence, vol. 36, pp.
5199–5207 (2022)
[111] Goyal, V., Iyengar, G., Udwani, R.: Online allocation of reusable resources via
algorithms guided by fluid approximations. arXiv preprint arXiv:2010.03983
(2020)
[112] Zhang, X., Cheung, W.C.: Online resource allocation for reusable resources.
arXiv preprint arXiv:2212.02855 (2022)
[113] Huo, T., Cheung, W.C.: Online reusable resource allocations with multi-class
arrivals. Available at SSRN 4320423 (2022)
[114] Ao, R., Fu, H., Simchi-Levi, D.: Two-stage online reusable resource allo-
cation:
Reservation,
overbooking
and
confirmation
call.
arXiv
preprint
arXiv:2410.15245 (2024)
[115] Chen, B., Potts, C.N., Woeginger, G.J.: A review of machine scheduling:
Complexity, algorithms and approximability. Handbook of Combinatorial Opti-
mization: Volume1–3, 1493–1641 (1998)
[116] Brucker, P., Drexl, A., Möhring, R., Neumann, K., Pesch, E.: Resource-
constrained project scheduling: Notation, classification, models, and methods.
European journal of operational research 112(1), 3–41 (1999)
[117] Xing, W., Zhang, J.: Parallel machine scheduling with splitting jobs. Discrete
Applied Mathematics 103(1-3), 259–269 (2000)
[118] Leung, J.Y.- (ed.): Handbook of Scheduling - Algorithms, Models, and
84


Performance Analysis. Chapman and Hall/CRC, Boca Raton, FL (2004).
http://www.crcnetbase.com/isbn/978-1-58488-397-5
[119] Allahverdi, A., Ng, C.T., Cheng, T.E., Kovalyov, M.Y.: A survey of scheduling
problems with setup times or costs. European journal of operational research
187(3), 985–1032 (2008)
[120] Kong, Q., Lee, C.-Y., Teo, C.-P., Zheng, Z.: Scheduling arrivals to a stochastic
service delivery system using copositive cones. Operations research 61(3), 711–
726 (2013)
[121] Mak, H.-Y., Rong, Y., Zhang, J.: Appointment scheduling with limited distri-
butional information. Management Science 61(2), 316–334 (2015)
[122] Roughgarden, T. (ed.): Beyond the Worst-Case Analysis of Algorithms.
Cambridge
University
Press,
Cambridge
(2020).
https://doi.org/10.1017/
9781108637435 . https://doi.org/10.1017/9781108637435
[123] Brucker, P., Gladky, A., Hoogeveen, H., Kovalyov, M.Y., Potts, C.N., Taut-
enhahn, T., Van De Velde, S.L.: Scheduling a batching machine. Journal of
scheduling 1(1), 31–54 (1998)
[124] Chen, B., Lee, C.-Y.: Logistics scheduling with batching and transportation.
European journal of operational research 189(3), 871–876 (2008)
[125] Lucier, B., Menache, I., Naor, J., Yaniv, J.: Eﬀicient online scheduling for
deadline-sensitive jobs. In: Proceedings of the Twenty-fifth Annual ACM
Symposium on Parallelism in Algorithms and Architectures, pp. 305–314 (2013)
[126] Im, S., Moseley, B.: Online batch scheduling for flow objectives. In: Proceedings
of the Twenty-fifth Annual ACM Symposium on Parallelism in Algorithms and
Architectures, pp. 102–104 (2013)
[127] Li, W., Wang, L., Chai, X., Yuan, H.: Online batch scheduling of simple linear
deteriorating jobs with incompatible families. Mathematics 8(2), 170 (2020)
[128] Azar, Y., Epstein, L.: On-line scheduling with precedence constraints. Dis-
crete Applied Mathematics 119(1), 169–180 (2002) https://doi.org/10.1016/
S0166-218X(01)00272-4 . Special Issue devoted to Foundation of Heuristics in
Combinatoria l Optimization
[129] Robert, J., Schabanel, N.: Non-clairvoyant scheduling with precedence con-
straints. In: Teng, S. (ed.) Proceedings of the Nineteenth Annual ACM-SIAM
Symposium on Discrete Algorithms, SODA 2008, San Francisco, California,
USA, January 20-22, 2008, pp. 491–500. SIAM, San Francisco, CA (2008).
http://dl.acm.org/citation.cfm?id=1347082.1347136
85


[130] Agrawal, K., Li, J., Lu, K., Moseley, B.: Scheduling parallel dag jobs online
to minimize average flow time. In: Proceedings of the Twenty-Seventh Annual
ACM-SIAM Symposium on Discrete Algorithms. SODA ’16, pp. 176–189.
Society for Industrial and Applied Mathematics, USA (2016)
[131] Garg, N., Gupta, A., Kumar, A., Singla, S.: Non-clairvoyant precedence
constrained scheduling. In: Baier, C., Chatzigiannakis, I., Flocchini, P.,
Leonardi, S. (eds.) 46th International Colloquium on Automata, Languages,
and Programming, ICALP 2019, July 9-12, 2019, Patras, Greece. LIPIcs,
vol. 132, pp. 63–16314. Schloss Dagstuhl - Leibniz-Zentrum für Infor-
matik, Patras, Greece (2019). https://doi.org/10.4230/LIPICS.ICALP.2019.63
. https://doi.org/10.4230/LIPIcs.ICALP.2019.63
[132] Ball, M.O., Queyranne, M.: Toward robust revenue management: Competitive
analysis of online booking. Operations Research 57(4), 950–963 (2009)
[133] Golrezaei, N., Jaillet, P., Zhou, Z.: Online resource allocation with convex-set
machine-learned advice. arXiv preprint arXiv:2306.12282 (2023)
[134] Agrawal, S., Wang, Z., Ye, Y.: A dynamic near-optimal algorithm for online
linear programming. Operations Research 62(4), 876–890 (2014)
[135] Li, X., Sun, C., Ye, Y.: Simple and fast algorithm for binary integer and online
linear programming. Advances in Neural Information Processing Systems 33,
9412–9421 (2020)
[136] Li, X., Ye, Y.: Online linear programming: Dual convergence, new algorithms,
and regret bounds. Operations Research 70(5), 2948–2966 (2022)
[137] Jiang, J., Li, X., Zhang, J.: Online stochastic optimization with wasserstein-
based nonstationarity. Management Science (2025)
[138] Buchbinder, N., Naor, J.: Online primal-dual algorithms for covering and
packing. Mathematics of Operations Research 34(2), 270–286 (2009)
[139] Sleator, D.D., Tarjan, R.E.: Amortized eﬀiciency of list update and paging rules.
Communications of the ACM 28(2), 202–208 (1985)
[140] Fiat, A., Karp, R.M., Luby, M., McGeoch, L.A., Sleator, D.D., Young, N.E.:
Competitive paging algorithms. Journal of Algorithms 12(4), 685–699 (1991)
[141] Lykouris, T., Vassilvitskii, S.: Competitive caching with machine learned advice.
Journal of the ACM (JACM) 68(4), 1–25 (2021)
[142] Koutsoupias, E., Papadimitriou, C.H.: On the k-server conjecture. Journal of
the ACM (JACM) 42(5), 971–983 (1995)
[143] Borodin, A., Linial, N., Saks, M.E.: An optimal on-line algorithm for metrical
86


task system. Journal of the ACM (JACM) 39(4), 745–763 (1992)
[144] Zinkevich, M.: Online convex programming and generalized infinitesimal gra-
dient ascent. In: Proceedings of the 20th International Conference on Machine
Learning (icml-03), pp. 928–936 (2003)
[145] Meyerson, A.: Online facility location. In: Proceedings 42nd IEEE Symposium
on Foundations of Computer Science, pp. 426–431 (2001). IEEE
[146] Fotakis, D.: On the competitive ratio for online facility location. Algorithmica
50(1), 1–57 (2008)
87